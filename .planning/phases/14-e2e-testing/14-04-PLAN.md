---
phase: 14-e2e-testing
plan: 04
type: execute
wave: 3
depends_on: ["14-02", "14-03"]
files_modified:
  - .github/workflows/e2e.yml
  - playwright.config.ts
  - docs/CI_SETUP.md
autonomous: true

must_haves:
  truths:
    - "Tests run in GitHub Actions CI on every PR"
    - "CI uses npm run build + npm run start instead of dev server"
    - "CI uploads Playwright report as artifact on failure"
    - "Required GitHub Secrets and test account setup are documented"
  artifacts:
    - path: ".github/workflows/e2e.yml"
      provides: "GitHub Actions E2E test workflow"
      contains: "playwright"
    - path: "playwright.config.ts"
      provides: "Updated config with CI-aware webServer command"
      contains: "CI"
    - path: "docs/CI_SETUP.md"
      provides: "Documentation of required GitHub Secrets and test accounts"
      contains: "E2E_CLERK"
  key_links:
    - from: ".github/workflows/e2e.yml"
      to: "playwright.config.ts"
      via: "npx playwright test"
      pattern: "playwright test"
    - from: ".github/workflows/e2e.yml"
      to: "package.json"
      via: "npm run build"
      pattern: "npm run build"
    - from: "docs/CI_SETUP.md"
      to: ".github/workflows/e2e.yml"
      via: "documents secrets referenced in workflow"
      pattern: "secrets\\."
---

<objective>
Create GitHub Actions CI workflow for E2E tests, update Playwright config for CI environment, and document required secrets and test account setup.

Purpose: E2E tests must run automatically on every PR to catch regressions before merge (TEST-07). CI uses a production build (not dev server) for realistic testing. Documentation ensures anyone setting up the repo can configure CI correctly.
Output: GitHub Actions workflow file, CI-aware Playwright config, and CI_SETUP.md documenting all required secrets.
</objective>

<execution_context>
@/home/sheldon/.claude/get-shit-done/workflows/execute-plan.md
@/home/sheldon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-e2e-testing/14-RESEARCH.md
@.planning/phases/14-e2e-testing/14-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: GitHub Actions E2E workflow and CI config updates</name>
  <files>
    .github/workflows/e2e.yml
    playwright.config.ts
  </files>
  <action>
    1. Update `playwright.config.ts` webServer section for CI awareness:
       - Change webServer command to: `process.env.CI ? 'npm run start' : 'npm run dev'`
       - Keep reuseExistingServer: `!process.env.CI`
       - Keep timeout: 120000

       NOTE on webServer behavior: Playwright's `webServer.command` field tells Playwright to spawn the server process automatically before tests run. In CI, setting command to `npm run start` means Playwright will run that command itself -- there is no need for a separate "start server" step in the GitHub Actions workflow. Playwright waits for the `url` to respond before running tests, then kills the server after tests complete. The `npm run build` step in CI must happen BEFORE `npx playwright test` so the production build exists for `npm run start` to serve.

    2. Create `.github/workflows/e2e.yml` following research CI pattern:
       ```yaml
       name: E2E Tests
       on:
         pull_request:
           branches: [main]

       jobs:
         e2e:
           timeout-minutes: 30
           runs-on: ubuntu-latest
           steps:
             - uses: actions/checkout@v4

             - uses: actions/setup-node@v4
               with:
                 node-version: 22
                 cache: 'npm'

             - name: Install dependencies
               run: npm ci

             - name: Install Playwright browsers
               run: npx playwright install --with-deps chromium webkit

             - name: Build Next.js
               run: npm run build
               env:
                 NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: ${{ secrets.CLERK_PUBLISHABLE_KEY }}
                 CLERK_SECRET_KEY: ${{ secrets.CLERK_SECRET_KEY }}
                 DATABASE_URL: ${{ secrets.DATABASE_URL }}

             - name: Run E2E tests
               run: npx playwright test
               env:
                 CI: true
                 NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: ${{ secrets.CLERK_PUBLISHABLE_KEY }}
                 CLERK_SECRET_KEY: ${{ secrets.CLERK_SECRET_KEY }}
                 E2E_CLERK_STUDENT_EMAIL: ${{ secrets.E2E_CLERK_STUDENT_EMAIL }}
                 E2E_CLERK_STUDENT_PASSWORD: ${{ secrets.E2E_CLERK_STUDENT_PASSWORD }}
                 E2E_CLERK_COACH_EMAIL: ${{ secrets.E2E_CLERK_COACH_EMAIL }}
                 E2E_CLERK_COACH_PASSWORD: ${{ secrets.E2E_CLERK_COACH_PASSWORD }}
                 ENROLLMENT_WEBHOOK_SECRET: ${{ secrets.ENROLLMENT_WEBHOOK_SECRET }}
                 DATABASE_URL: ${{ secrets.DATABASE_URL }}

             - uses: actions/upload-artifact@v4
               if: ${{ !cancelled() }}
               with:
                 name: playwright-report
                 path: playwright-report/
                 retention-days: 14
       ```

       Key decisions:
       - Use `npm ci` (not `npm install`) for reproducible installs
       - Build step needs env vars because Next.js inlines NEXT_PUBLIC_* at build time
       - Run step needs env vars for runtime (Clerk auth, DB, webhook secrets)
       - Upload playwright-report as artifact even on success (for debugging)
       - 30 minute timeout is generous but prevents runaway bills
       - Single worker in CI (`workers: 1` already in playwright.config.ts via `process.env.CI ? 1 : undefined`)
       - No separate "start server" step needed -- Playwright's webServer config handles this automatically

    3. Ensure the `.github/workflows/` directory is created (it may not exist yet).
  </action>
  <verify>
    - `ls .github/workflows/e2e.yml` shows file exists
    - `grep "playwright test" .github/workflows/e2e.yml` shows test run step
    - `grep "upload-artifact" .github/workflows/e2e.yml` shows report upload
    - `grep "CI" playwright.config.ts` shows CI-aware webServer command
    - `grep "npm run start" playwright.config.ts` shows production server command for CI
    - `grep "webServer" playwright.config.ts` confirms the webServer config exists (Playwright spawns the server automatically)
    - YAML is valid: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/e2e.yml'))"` (if python available) or visual inspection
  </verify>
  <done>GitHub Actions workflow triggers on PRs to main, builds Next.js, runs Playwright tests with all required secrets, and uploads report artifact. Playwright config uses production build in CI with automatic server startup via webServer.command.</done>
</task>

<task type="auto">
  <name>Task 2: Document required GitHub Secrets and test account setup</name>
  <files>
    docs/CI_SETUP.md
  </files>
  <action>
    Create `docs/CI_SETUP.md` that documents everything needed to set up CI for E2E tests.

    The document must include:

    1. **Required GitHub Secrets** -- a table listing every secret referenced in `.github/workflows/e2e.yml`:

       | Secret Name | Description | Where to Find |
       |-------------|-------------|---------------|
       | `CLERK_PUBLISHABLE_KEY` | Clerk publishable key for the test environment | Clerk Dashboard -> API Keys |
       | `CLERK_SECRET_KEY` | Clerk secret key for the test environment | Clerk Dashboard -> API Keys |
       | `DATABASE_URL` | Neon Postgres connection string for test/preview DB | Neon Dashboard -> Connection Details |
       | `E2E_CLERK_STUDENT_EMAIL` | Email of the test student Clerk account | Created manually (see below) |
       | `E2E_CLERK_STUDENT_PASSWORD` | Password for the test student account | Set during creation (see below) |
       | `E2E_CLERK_COACH_EMAIL` | Email of the test coach Clerk account | Created manually (see below) |
       | `E2E_CLERK_COACH_PASSWORD` | Password for the test coach account | Set during creation (see below) |
       | `ENROLLMENT_WEBHOOK_SECRET` | Shared secret for enrollment webhook auth | .env or generated value |

    2. **Creating Test Accounts in Clerk** -- step-by-step instructions:
       - Go to Clerk Dashboard for the test/development instance
       - Create a user with email `e2e-student@<yourdomain>` (or any test email)
       - Set a password (this becomes `E2E_CLERK_STUDENT_PASSWORD`)
       - Assign the "student" role (if using Clerk roles)
       - Repeat for coach: create user with coach email, assign "coach" role
       - These accounts are used by Playwright's global setup to authenticate before tests

    3. **Setting GitHub Secrets** -- brief instructions:
       - Go to GitHub repo -> Settings -> Secrets and variables -> Actions
       - Add each secret from the table above
       - For Neon DATABASE_URL, use a preview/test branch database (not production)

    4. **Local Development** -- note that for local E2E testing, the same env vars should be in `.env.local` (they are already gitignored)

    Keep the document concise and actionable. No marketing language.
  </action>
  <verify>
    - `ls docs/CI_SETUP.md` shows file exists
    - `grep "E2E_CLERK_STUDENT_EMAIL" docs/CI_SETUP.md` shows student secret documented
    - `grep "E2E_CLERK_COACH_EMAIL" docs/CI_SETUP.md` shows coach secret documented
    - `grep "DATABASE_URL" docs/CI_SETUP.md` shows database secret documented
    - `grep "ENROLLMENT_WEBHOOK_SECRET" docs/CI_SETUP.md` shows webhook secret documented
    - Document lists all 8 secrets from the workflow file
  </verify>
  <done>CI_SETUP.md documents all 8 required GitHub Secrets with descriptions and sources, includes step-by-step instructions for creating Clerk test accounts (student and coach), and explains how to configure GitHub Actions secrets.</done>
</task>

</tasks>

<verification>
- `.github/workflows/e2e.yml` exists with correct structure
- Playwright config uses `npm run start` in CI, `npm run dev` locally
- Workflow references all required GitHub Secrets
- Report artifact is uploaded on every run (not just failure)
- `docs/CI_SETUP.md` documents all 8 secrets and test account creation
</verification>

<success_criteria>
1. GitHub Actions workflow triggers on pull_request to main
2. Workflow installs deps, browsers, builds app, runs tests, uploads report
3. All required env vars are mapped from GitHub Secrets
4. Playwright config is CI-aware (production build, single worker, no server reuse, automatic server startup)
5. Tests can run locally with `npm run test:e2e` against localhost:3000 (unchanged)
6. CI_SETUP.md documents all 8 GitHub Secrets with where to find each value
7. CI_SETUP.md includes instructions for creating Clerk test accounts
</success_criteria>

<output>
After completion, create `.planning/phases/14-e2e-testing/14-04-SUMMARY.md`
</output>
