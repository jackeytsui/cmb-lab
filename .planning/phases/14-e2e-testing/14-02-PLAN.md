---
phase: 14-e2e-testing
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - e2e/tests/enrollment.spec.ts
  - e2e/tests/grading.spec.ts
  - e2e/tests/chinese-input.spec.ts
  - src/components/**/*interaction*.tsx
  - src/components/**/*feedback*.tsx
autonomous: true

must_haves:
  truths:
    - "Test verifies enrollment webhook creates student account with correct course access"
    - "Test verifies AI grading returns feedback using mocked n8n webhook response"
    - "Test verifies Chinese text input works correctly with IME composition handling"
    - "All interaction components have data-testid attributes that tests can target"
  artifacts:
    - path: "e2e/tests/enrollment.spec.ts"
      provides: "Enrollment webhook E2E test"
      contains: "enroll"
    - path: "e2e/tests/grading.spec.ts"
      provides: "AI grading E2E test with mocked webhook"
      contains: "grade"
    - path: "e2e/tests/chinese-input.spec.ts"
      provides: "Chinese IME composition E2E test"
      contains: "composit"
  key_links:
    - from: "e2e/tests/enrollment.spec.ts"
      to: "/api/webhooks/enroll"
      via: "request.post direct API call"
      pattern: "webhooks/enroll"
    - from: "e2e/tests/grading.spec.ts"
      to: "e2e/helpers/webhook-mock.ts"
      via: "import mockGradingResponse"
      pattern: "mockGradingResponse"
    - from: "e2e/tests/chinese-input.spec.ts"
      to: "e2e/helpers/composition.ts"
      via: "import typeChineseText"
      pattern: "typeChineseText"
---

<objective>
Write E2E test specs for enrollment webhook, AI grading, and Chinese IME input. First add required data-testid attributes to interaction components.

Purpose: These three tests cover TEST-02 (enrollment), TEST-04 (AI grading with mocked webhook), and TEST-06 (Chinese IME). They are API-focused or helper-focused tests that do not require complex UI navigation flows.
Output: Three passing test spec files covering critical API and input handling paths, with data-testid attributes on all targeted components.
</objective>

<execution_context>
@/home/sheldon/.claude/get-shit-done/workflows/execute-plan.md
@/home/sheldon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-e2e-testing/14-RESEARCH.md
@.planning/phases/14-e2e-testing/14-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add data-testid attributes to interaction components</name>
  <files>
    src/components/**/*interaction*.tsx (or wherever interaction UI lives)
    src/components/**/*feedback*.tsx (or wherever feedback display lives)
  </files>
  <action>
    IMPORTANT: This task MUST be completed BEFORE writing any test specs. Tests depend on these selectors existing.

    1. Search the codebase for interaction-related components:
       - `grep -r "chinese\|input.*answer\|submit.*answer\|interaction" src/components/ src/app/ --include="*.tsx" -l`
       - `grep -r "feedback\|grading\|score" src/components/ src/app/ --include="*.tsx" -l`
       - Read each found component to understand its structure

    2. Add the following `data-testid` attributes to the appropriate components. For each one, read the component file first, find the correct JSX element, and add the attribute:

       Required data-testid attributes:
       - `data-testid="interaction-area"` -- on the wrapper/container element of the interaction section (where students answer questions)
       - `data-testid="chinese-input"` -- on the text input element where students type Chinese characters
       - `data-testid="submit-answer"` -- on the button that submits the student's answer
       - `data-testid="feedback"` -- on the container that displays AI grading feedback after submission
       - `data-testid="lesson-complete"` -- on the completion badge/indicator shown when a lesson is marked complete

    3. If a component already has these attributes, leave them unchanged. If the attribute name differs slightly (e.g., `data-testid="submit-btn"` instead of `data-testid="submit-answer"`), update it to match the expected name above.

    4. Do NOT change any component logic, styling, or structure. Only add or update `data-testid` attributes on existing elements.
  </action>
  <verify>
    - `grep -r "data-testid=\"interaction-area\"" src/` returns at least one match
    - `grep -r "data-testid=\"chinese-input\"" src/` returns at least one match
    - `grep -r "data-testid=\"submit-answer\"" src/` returns at least one match
    - `grep -r "data-testid=\"feedback\"" src/` returns at least one match
    - `grep -r "data-testid=\"lesson-complete\"" src/` returns at least one match
    - `npm run build` still passes (no broken components)
  </verify>
  <done>All five data-testid attributes exist on their respective interaction components. Build passes. No component logic changed.</done>
</task>

<task type="auto">
  <name>Task 2: Write enrollment, grading, and Chinese IME test specs</name>
  <files>
    e2e/tests/enrollment.spec.ts
    e2e/tests/grading.spec.ts
    e2e/tests/chinese-input.spec.ts
  </files>
  <action>
    1. Create `e2e/tests/enrollment.spec.ts` (TEST-02):
       - Import `test, expect` from `@playwright/test` (this test uses API requests, not browser UI)
       - Test: "enrollment webhook creates student with course access"
         - POST to `/api/webhooks/enroll` with headers `x-webhook-secret` (from `process.env.ENROLLMENT_WEBHOOK_SECRET` or a test value) and `Content-Type: application/json`
         - Body: `{ email: 'e2e-test-student@example.com', name: 'E2E Test Student', courseId: '<use a known test course ID from seed data>' }`
         - Assert response status is 200 or 201
         - Assert response body contains success indicator
         - Assert response body includes the student/enrollment data
       - Test: "enrollment webhook rejects invalid secret"
         - Same POST but with wrong secret header
         - Assert 401 or 403 response
       - Test: "enrollment webhook handles missing fields"
         - POST with missing email
         - Assert 400 response with error message

       IMPORTANT: Read the actual enrollment webhook route at `src/app/api/webhooks/enroll/route.ts` FIRST to understand the exact request format, response shape, and validation logic. Match the test to the actual implementation.

    2. Create `e2e/tests/grading.spec.ts` (TEST-04):
       - Import `test, expect` from the custom fixtures (`../fixtures/auth`)
       - Import `mockGradingResponse` from `../helpers/webhook-mock`
       - Import `LessonPage` from `../pages/lesson.page`
       - Test: "AI grading returns feedback with mocked n8n response"
         - Use `mockGradingResponse(page, { score: 85, feedback: 'Great pronunciation of the tones.', corrections: [] })`
         - Also mock Mux player using `mockMuxPlayer(page)` from helpers
         - Navigate to a lesson page
         - Find the interaction input using `[data-testid="chinese-input"]` selector
         - Type an answer, click `[data-testid="submit-answer"]`
         - Assert `[data-testid="feedback"]` becomes visible with the mocked feedback text
         - Assert score is displayed
       - Test: "AI grading handles error response gracefully"
         - Mock grading endpoint to return 500
         - Submit answer
         - Assert error message is shown to user (not a crash)

       IMPORTANT: Read `src/app/api/grade/route.ts` FIRST to understand request/response format. Use the data-testid selectors added in Task 1 -- they are guaranteed to exist.

    3. Create `e2e/tests/chinese-input.spec.ts` (TEST-06):
       - Import `test, expect` from custom fixtures (`../fixtures/auth`)
       - Import `typeChineseText` from `../helpers/composition`
       - Import `mockMuxPlayer` from `../helpers/mux-mock`
       - Import `mockGradingResponse` from `../helpers/webhook-mock`

       - Test: "Chinese text input works with IME composition events"
         - Mock Mux player and grading API
         - Navigate to a lesson page that has a text interaction
         - Use `typeChineseText(page, '[data-testid="chinese-input"]', '你好', ['n', 'ni', '你', '你h', '你ha', '你hao', '你好'])`
         - Assert the input value contains '你好'
         - Click `[data-testid="submit-answer"]`
         - Assert no errors occur (the form submits successfully)

       - Test: "IME composition does not submit prematurely during composing"
         - Navigate to lesson with text interaction
         - Start composition but do not end it
         - Assert that the submit button is not triggered during composition
         - Complete composition
         - Assert input has final value
  </action>
  <verify>
    - `ls e2e/tests/enrollment.spec.ts e2e/tests/grading.spec.ts e2e/tests/chinese-input.spec.ts` shows all 3 files exist
    - `npx playwright test --list --project=chromium` shows the test names
    - `grep "data-testid" e2e/tests/grading.spec.ts` shows testid selectors are used
    - `grep "data-testid" e2e/tests/chinese-input.spec.ts` shows testid selectors are used
    - TypeScript in test files is valid (`npx playwright test --list` doesn't error)
  </verify>
  <done>Enrollment webhook test covers happy path, auth rejection, and validation. Grading test uses data-testid selectors for interaction and feedback elements. Chinese IME test covers composition event sequence and premature submission prevention.</done>
</task>

</tasks>

<verification>
- All 3 test spec files exist in e2e/tests/
- `npx playwright test --list` shows all test names across all projects
- Test files import from the helpers and fixtures created in Plan 01
- data-testid attributes exist on all 5 interaction component elements
- Tests use data-testid selectors (not fragile CSS/text selectors)
</verification>

<success_criteria>
1. All 5 data-testid attributes added to interaction components (Task 1 verified via grep)
2. enrollment.spec.ts has 3 tests: happy path, auth rejection, validation
3. grading.spec.ts has 2 tests: mocked feedback display, error handling
4. chinese-input.spec.ts has 2 tests: IME composition, premature submission prevention
5. All tests use helpers from Plan 01 (mockGradingResponse, typeChineseText, mockMuxPlayer)
6. All UI tests use data-testid selectors from Task 1
</success_criteria>

<output>
After completion, create `.planning/phases/14-e2e-testing/14-02-SUMMARY.md`
</output>
