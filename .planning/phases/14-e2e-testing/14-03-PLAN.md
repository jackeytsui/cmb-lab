---
phase: 14-e2e-testing
plan: 03
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - e2e/tests/lesson.spec.ts
  - e2e/tests/coach.spec.ts
autonomous: true

must_haves:
  truths:
    - "Test verifies a student can complete a lesson with video progress and interaction pass"
    - "Test verifies a coach can review a submission and send feedback"
  artifacts:
    - path: "e2e/tests/lesson.spec.ts"
      provides: "Student lesson completion E2E test"
      contains: "lesson"
    - path: "e2e/tests/coach.spec.ts"
      provides: "Coach review workflow E2E test"
      contains: "coach"
  key_links:
    - from: "e2e/tests/lesson.spec.ts"
      to: "e2e/helpers/mux-mock.ts"
      via: "import mockMuxPlayer, simulateVideoProgress"
      pattern: "mockMuxPlayer|simulateVideoProgress"
    - from: "e2e/tests/lesson.spec.ts"
      to: "e2e/pages/lesson.page.ts"
      via: "import LessonPage"
      pattern: "LessonPage"
    - from: "e2e/tests/coach.spec.ts"
      to: "e2e/pages/coach.page.ts"
      via: "import CoachPage"
      pattern: "CoachPage"
---

<objective>
Write E2E test specs for student lesson completion and coach review workflow.

Purpose: These two tests cover TEST-03 (student completes lesson with video progress + interaction pass) and TEST-05 (coach reviews submission and sends feedback). They are full UI flow tests that use authenticated browser sessions.
Output: Two passing test spec files covering the core student and coach user journeys.
</objective>

<execution_context>
@/home/sheldon/.claude/get-shit-done/workflows/execute-plan.md
@/home/sheldon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-e2e-testing/14-RESEARCH.md
@.planning/phases/14-e2e-testing/14-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Student lesson completion test spec</name>
  <files>
    e2e/tests/lesson.spec.ts
  </files>
  <action>
    1. Create `e2e/tests/lesson.spec.ts` (TEST-03):
       - Import `test, expect` from custom fixtures (`../fixtures/auth`)
       - Import `mockMuxPlayer, simulateVideoProgress` from `../helpers/mux-mock`
       - Import `mockGradingResponse` from `../helpers/webhook-mock`
       - Import `LessonPage` from `../pages/lesson.page`

       - Test: "student can navigate to a lesson from dashboard"
         - Navigate to `/dashboard`
         - Assert course list is visible
         - Click on first available course
         - Assert lessons list is visible
         - Click on first unlocked lesson
         - Assert lesson page loads with video player area

       - Test: "student can complete a lesson with video progress and interaction pass"
         - Mock Mux player (block streaming, provide placeholder)
         - Mock grading response (return passing score)
         - Navigate to a lesson page directly
         - Simulate video progress to 100% using `simulateVideoProgress(page, 100)`
         - Wait for interaction to appear (video should pause at interaction point)
         - Fill in the interaction input with an answer
         - Submit the answer
         - Assert feedback is shown (from mocked grading response)
         - Assert lesson completion indicator appears

       - Test: "lesson shows locked state for incomplete prerequisites"
         - Navigate to dashboard
         - Assert a later lesson shows lock icon or disabled state
         - Click on locked lesson
         - Assert user cannot access it (redirect or lock overlay)

       IMPORTANT: Before writing tests, READ the following files to understand the actual UI structure:
       - `src/app/(dashboard)/` -- dashboard layout and pages
       - Course/lesson page components
       - The video player component (how it renders mux-player)
       - The interaction overlay component
       - The progress API route (`src/app/api/progress/[lessonId]/route.ts`)

       Match test selectors to actual component structure. Add `data-testid` attributes to components where needed for reliable test selection.
  </action>
  <verify>
    - `ls e2e/tests/lesson.spec.ts` shows file exists
    - `npx playwright test --list --project=chromium` includes lesson test names
    - Test file imports helpers and page objects from Plan 01
  </verify>
  <done>Lesson completion test covers navigation from dashboard, video progress + interaction completion flow, and prerequisite lock enforcement.</done>
</task>

<task type="auto">
  <name>Task 2: Coach review workflow test spec</name>
  <files>
    e2e/tests/coach.spec.ts
  </files>
  <action>
    1. Create `e2e/tests/coach.spec.ts` (TEST-05):
       - Import `test, expect` from custom fixtures (`../fixtures/auth`)
       - Import `CoachPage` from `../pages/coach.page`
       - This test runs in the `coach-chromium` project (uses coach storageState)

       - Test: "coach can see submission queue"
         - Navigate to coach dashboard (`/coach` or wherever the submissions page is)
         - Assert the submissions queue/list is visible
         - Assert at least the page layout renders (queue may be empty in test env)

       - Test: "coach can review a submission and send feedback"
         - Navigate to coach submissions page
         - If submissions exist, click on first one
         - If no submissions exist, this test should be marked as conditional or create a test submission first via API
         - In submission detail view, type feedback text
         - Click send feedback button
         - Assert success indicator appears (toast, status change, etc.)
         - Mock the notification API (`/api/notify/coach-feedback`) to prevent actual email sending

       - Test: "coach can add notes to a submission"
         - Navigate to a submission detail page
         - Type a note in the notes input
         - Submit the note
         - Assert the note appears in the notes list

       IMPORTANT: Before writing tests, READ the following files to understand the coach UI:
       - `src/app/(dashboard)/coach/` -- coach pages
       - Submission components
       - Feedback and notes components
       - `src/app/api/submissions/` routes

       Match test selectors to actual component structure. Add `data-testid` attributes where needed. The coach tests use coach auth state (coach-chromium project), so they will automatically have coach role.
  </action>
  <verify>
    - `ls e2e/tests/coach.spec.ts` shows file exists
    - `npx playwright test --list --project=coach-chromium` includes coach test names
    - Test file uses CoachPage page object
  </verify>
  <done>Coach test covers submission queue visibility, feedback sending flow, and notes creation.</done>
</task>

</tasks>

<verification>
- Both test spec files exist in e2e/tests/
- `npx playwright test --list` shows all test names
- Lesson tests run under chromium/webkit/mobile projects (student auth)
- Coach tests run under coach-chromium project (coach auth)
- data-testid attributes added to dashboard, lesson, and coach components where missing
</verification>

<success_criteria>
1. lesson.spec.ts has 3 tests: navigation, completion flow, lock enforcement
2. coach.spec.ts has 3 tests: queue visibility, feedback sending, notes creation
3. Tests use Page Object Models from Plan 01
4. Tests mock external dependencies (Mux, grading webhooks, notification emails)
5. Coach tests use coach auth storageState
</success_criteria>

<output>
After completion, create `.planning/phases/14-e2e-testing/14-03-SUMMARY.md`
</output>
