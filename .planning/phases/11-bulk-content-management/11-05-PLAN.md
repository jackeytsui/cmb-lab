---
phase: 11-bulk-content-management
plan: 05
type: execute
wave: 3
depends_on: ["11-02", "11-03", "11-04"]
files_modified:
  - src/app/(dashboard)/admin/content/page.tsx
  - src/app/(dashboard)/admin/content/uploads/page.tsx
  - src/app/(dashboard)/admin/page.tsx
autonomous: true

must_haves:
  truths:
    - "Admin navigation includes link to Content Management"
    - "Content management page shows upload zone and video library"
    - "Coach can perform batch assign from video library"
    - "BatchEditModal is wired for batch metadata editing (not needed for video library - videos have no title/description to edit)"
    - "BULK-05 (lesson reordering) already satisfied by existing ContentList component from Phase 9"
  artifacts:
    - path: "src/app/(dashboard)/admin/content/page.tsx"
      provides: "Content management hub page"
      min_lines: 100
    - path: "src/app/(dashboard)/admin/content/uploads/page.tsx"
      provides: "Video uploads page"
      min_lines: 80
  key_links:
    - from: "src/app/(dashboard)/admin/content/page.tsx"
      to: "src/components/admin/VideoUploadZone.tsx"
      via: "VideoUploadZone component"
      pattern: "VideoUploadZone"
    - from: "src/app/(dashboard)/admin/content/page.tsx"
      to: "src/components/admin/VideoLibrary.tsx"
      via: "VideoLibrary component"
      pattern: "VideoLibrary"
---

<objective>
Create admin pages for bulk content management that integrate all upload, batch, and move components.

Purpose: Provide a unified UI for coaches to upload, organize, and manage video content efficiently.

Output: Content management pages with upload zone, video library, and batch action buttons.
</objective>

<execution_context>
@/home/sheldon/.claude/get-shit-done/workflows/execute-plan.md
@/home/sheldon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-bulk-content-management/11-02-SUMMARY.md
@.planning/phases/11-bulk-content-management/11-03-SUMMARY.md
@.planning/phases/11-bulk-content-management/11-04-SUMMARY.md
@src/app/(dashboard)/admin/page.tsx (admin dashboard)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create content management hub page</name>
  <files>src/app/(dashboard)/admin/content/page.tsx</files>
  <action>
    Create src/app/(dashboard)/admin/content/page.tsx:

    ```typescript
    import { redirect } from "next/navigation";
    import { hasMinimumRole } from "@/lib/auth";
    import { ContentManagementClient } from "./ContentManagementClient";

    export default async function ContentManagementPage() {
      const hasAccess = await hasMinimumRole("coach");
      if (!hasAccess) {
        redirect("/dashboard");
      }

      return (
        <div className="min-h-screen bg-zinc-950 p-6">
          <div className="mx-auto max-w-7xl space-y-8">
            {/* Header */}
            <div className="flex items-center justify-between">
              <div>
                <h1 className="text-3xl font-bold text-white">Content Management</h1>
                <p className="text-zinc-400 mt-1">
                  Upload, organize, and manage video content
                </p>
              </div>

              <nav className="flex items-center gap-2 text-sm">
                <a href="/admin" className="text-zinc-400 hover:text-white">
                  Admin
                </a>
                <span className="text-zinc-600">/</span>
                <span className="text-white">Content</span>
              </nav>
            </div>

            {/* Client component for interactive features */}
            <ContentManagementClient />
          </div>
        </div>
      );
    }
    ```

    Also create src/app/(dashboard)/admin/content/ContentManagementClient.tsx:

    ```typescript
    "use client";

    import { useState, useCallback } from "react";
    import { Button } from "@/components/ui/button";
    import { VideoUploadZone } from "@/components/admin/VideoUploadZone";
    import { VideoLibrary } from "@/components/admin/VideoLibrary";
    import { BatchAssignModal } from "@/components/admin/BatchAssignModal";
    import type { VideoUpload } from "@/db/schema/uploads";

    type Tab = "upload" | "library";

    export function ContentManagementClient() {
      const [activeTab, setActiveTab] = useState<Tab>("upload");
      const [selectedIds, setSelectedIds] = useState<string[]>([]);
      const [selectedVideos, setSelectedVideos] = useState<VideoUpload[]>([]);
      const [showAssignModal, setShowAssignModal] = useState(false);
      const [refreshKey, setRefreshKey] = useState(0);

      const handleUploadComplete = useCallback(() => {
        setRefreshKey((k) => k + 1);
        setActiveTab("library");
      }, []);

      const handleSelectionChange = useCallback((ids: string[]) => {
        setSelectedIds(ids);
      }, []);

      const handleBatchAssign = useCallback(() => {
        // We need to fetch full video objects for selected IDs
        // For now, we'll fetch them when modal opens
        setShowAssignModal(true);
      }, []);

      const handleAssignSuccess = useCallback(() => {
        setSelectedIds([]);
        setRefreshKey((k) => k + 1);
      }, []);

      return (
        <>
          {/* Tabs */}
          <div className="flex items-center gap-4 border-b border-zinc-800 pb-4">
            <button
              onClick={() => setActiveTab("upload")}
              className={`px-4 py-2 text-sm font-medium rounded-lg transition-colors ${
                activeTab === "upload"
                  ? "bg-zinc-800 text-white"
                  : "text-zinc-400 hover:text-white hover:bg-zinc-800/50"
              }`}
            >
              Upload Videos
            </button>
            <button
              onClick={() => setActiveTab("library")}
              className={`px-4 py-2 text-sm font-medium rounded-lg transition-colors ${
                activeTab === "library"
                  ? "bg-zinc-800 text-white"
                  : "text-zinc-400 hover:text-white hover:bg-zinc-800/50"
              }`}
            >
              Video Library
            </button>

            {/* Batch actions */}
            {activeTab === "library" && selectedIds.length > 0 && (
              <div className="ml-auto flex items-center gap-2">
                <span className="text-sm text-zinc-400">
                  {selectedIds.length} selected
                </span>
                <Button
                  variant="outline"
                  size="sm"
                  onClick={handleBatchAssign}
                  className="border-cyan-500/50 text-cyan-400 hover:bg-cyan-500/10"
                >
                  Assign to Lessons
                </Button>
              </div>
            )}
          </div>

          {/* Tab content */}
          {activeTab === "upload" && (
            <div className="space-y-6">
              <div className="rounded-lg border border-zinc-800 bg-zinc-900 p-6">
                <h2 className="text-xl font-semibold text-white mb-4">
                  Upload Videos
                </h2>
                <p className="text-sm text-zinc-400 mb-6">
                  Drag and drop video files or click to browse. Files are uploaded
                  directly to Mux for processing.
                </p>
                <VideoUploadZone onUploadComplete={handleUploadComplete} />
              </div>

              <div className="rounded-lg border border-zinc-800 bg-zinc-900 p-6">
                <h3 className="text-lg font-medium text-white mb-2">
                  Upload Guidelines
                </h3>
                <ul className="text-sm text-zinc-400 space-y-1">
                  <li>• Supported formats: MP4, MOV, WebM, and most video formats</li>
                  <li>• Maximum 5 files upload concurrently to avoid rate limits</li>
                  <li>• Videos are processed by Mux (may take a few minutes)</li>
                  <li>• After processing, assign videos to lessons from the library</li>
                </ul>
              </div>
            </div>
          )}

          {activeTab === "library" && (
            <div className="space-y-6">
              <div className="rounded-lg border border-zinc-800 bg-zinc-900 p-6">
                <div className="flex items-center justify-between mb-4">
                  <h2 className="text-xl font-semibold text-white">
                    Unassigned Videos
                  </h2>
                  <p className="text-sm text-zinc-400">
                    Click videos to select, then use batch actions
                  </p>
                </div>
                <VideoLibrary
                  key={refreshKey}
                  selectable
                  selectedIds={selectedIds}
                  onSelectionChange={handleSelectionChange}
                  unassignedOnly
                />
              </div>
            </div>
          )}

          {/* Batch assign modal */}
          <BatchAssignModalWrapper
            open={showAssignModal}
            onOpenChange={setShowAssignModal}
            selectedIds={selectedIds}
            onSuccess={handleAssignSuccess}
          />
        </>
      );
    }

    // Wrapper to fetch full video data for modal
    function BatchAssignModalWrapper({
      open,
      onOpenChange,
      selectedIds,
      onSuccess,
    }: {
      open: boolean;
      onOpenChange: (open: boolean) => void;
      selectedIds: string[];
      onSuccess: () => void;
    }) {
      const [videos, setVideos] = useState<VideoUpload[]>([]);
      const [loading, setLoading] = useState(false);

      // Fetch videos when modal opens
      if (open && videos.length === 0 && !loading && selectedIds.length > 0) {
        setLoading(true);
        fetch("/api/admin/uploads?status=ready&unassigned=true")
          .then((res) => res.json())
          .then((data) => {
            const selectedVideos = (data.uploads || []).filter(
              (v: VideoUpload) => selectedIds.includes(v.id)
            );
            setVideos(selectedVideos);
          })
          .finally(() => setLoading(false));
      }

      // Reset when closed
      if (!open && videos.length > 0) {
        setVideos([]);
      }

      if (!open) return null;

      return (
        <BatchAssignModal
          open={open}
          onOpenChange={onOpenChange}
          selectedVideos={videos}
          onSuccess={onSuccess}
        />
      );
    }
    ```
  </action>
  <verify>npm run build compiles without errors</verify>
  <done>Content management hub page created with upload zone and video library tabs</done>
</task>

<task type="auto">
  <name>Task 2: Create dedicated uploads page</name>
  <files>src/app/(dashboard)/admin/content/uploads/page.tsx</files>
  <action>
    Create src/app/(dashboard)/admin/content/uploads/page.tsx:

    ```typescript
    import { redirect } from "next/navigation";
    import { hasMinimumRole } from "@/lib/auth";
    import { db } from "@/db";
    import { videoUploads } from "@/db/schema";
    import { desc } from "drizzle-orm";

    export default async function UploadsPage() {
      const hasAccess = await hasMinimumRole("coach");
      if (!hasAccess) {
        redirect("/dashboard");
      }

      // Fetch all uploads for admin view
      const uploads = await db
        .select()
        .from(videoUploads)
        .orderBy(desc(videoUploads.createdAt))
        .limit(100);

      return (
        <div className="min-h-screen bg-zinc-950 p-6">
          <div className="mx-auto max-w-7xl space-y-8">
            {/* Header */}
            <div className="flex items-center justify-between">
              <div>
                <h1 className="text-3xl font-bold text-white">All Uploads</h1>
                <p className="text-zinc-400 mt-1">
                  View all uploaded videos and their status
                </p>
              </div>

              <nav className="flex items-center gap-2 text-sm">
                <a href="/admin" className="text-zinc-400 hover:text-white">
                  Admin
                </a>
                <span className="text-zinc-600">/</span>
                <a href="/admin/content" className="text-zinc-400 hover:text-white">
                  Content
                </a>
                <span className="text-zinc-600">/</span>
                <span className="text-white">Uploads</span>
              </nav>
            </div>

            {/* Uploads table */}
            <div className="rounded-lg border border-zinc-800 bg-zinc-900 overflow-hidden">
              <table className="w-full">
                <thead className="bg-zinc-800/50">
                  <tr>
                    <th className="px-4 py-3 text-left text-sm font-medium text-zinc-300">
                      Filename
                    </th>
                    <th className="px-4 py-3 text-left text-sm font-medium text-zinc-300">
                      Status
                    </th>
                    <th className="px-4 py-3 text-left text-sm font-medium text-zinc-300">
                      Duration
                    </th>
                    <th className="px-4 py-3 text-left text-sm font-medium text-zinc-300">
                      Assigned
                    </th>
                    <th className="px-4 py-3 text-left text-sm font-medium text-zinc-300">
                      Uploaded
                    </th>
                  </tr>
                </thead>
                <tbody className="divide-y divide-zinc-800">
                  {uploads.length === 0 ? (
                    <tr>
                      <td
                        colSpan={5}
                        className="px-4 py-8 text-center text-zinc-400"
                      >
                        No uploads yet
                      </td>
                    </tr>
                  ) : (
                    uploads.map((upload) => (
                      <tr key={upload.id} className="hover:bg-zinc-800/30">
                        <td className="px-4 py-3">
                          <span className="text-white truncate max-w-xs block">
                            {upload.filename}
                          </span>
                        </td>
                        <td className="px-4 py-3">
                          <StatusBadge status={upload.status} />
                        </td>
                        <td className="px-4 py-3 text-zinc-400 text-sm">
                          {upload.durationSeconds
                            ? `${Math.floor(upload.durationSeconds / 60)}:${String(
                                upload.durationSeconds % 60
                              ).padStart(2, "0")}`
                            : "—"}
                        </td>
                        <td className="px-4 py-3">
                          {upload.lessonId ? (
                            <span className="text-green-400 text-sm">Yes</span>
                          ) : (
                            <span className="text-zinc-500 text-sm">No</span>
                          )}
                        </td>
                        <td className="px-4 py-3 text-zinc-400 text-sm">
                          {new Date(upload.createdAt).toLocaleDateString()}
                        </td>
                      </tr>
                    ))
                  )}
                </tbody>
              </table>
            </div>
          </div>
        </div>
      );
    }

    function StatusBadge({ status }: { status: string }) {
      const colors: Record<string, string> = {
        pending: "bg-zinc-700 text-zinc-300",
        uploading: "bg-blue-500/20 text-blue-400",
        processing: "bg-yellow-500/20 text-yellow-400",
        ready: "bg-green-500/20 text-green-400",
        errored: "bg-red-500/20 text-red-400",
      };

      return (
        <span
          className={`inline-flex px-2 py-1 text-xs font-medium rounded ${
            colors[status] || "bg-zinc-700 text-zinc-300"
          }`}
        >
          {status}
        </span>
      );
    }
    ```
  </action>
  <verify>npm run build compiles without errors</verify>
  <done>Uploads page shows all uploads with status in table format</done>
</task>

<task type="auto">
  <name>Task 3: Update admin dashboard with content management link</name>
  <files>src/app/(dashboard)/admin/page.tsx</files>
  <action>
    Add a Content Management link after the AI Prompts card in the admin dashboard.

    In src/app/(dashboard)/admin/page.tsx, find the closing `</Link>` tag for AI Prompts (around line 195)
    and add this card immediately after it (before the closing `</div>` of the grid):

    ```typescript
            <Link
              href="/admin/content"
              className="group rounded-lg border border-zinc-700 bg-zinc-800 p-6 transition-colors hover:border-zinc-500 hover:bg-zinc-700"
            >
              <div className="mb-4 flex h-12 w-12 items-center justify-center rounded-lg bg-orange-500/10 text-orange-400">
                <svg
                  xmlns="http://www.w3.org/2000/svg"
                  width="24"
                  height="24"
                  viewBox="0 0 24 24"
                  fill="none"
                  stroke="currentColor"
                  strokeWidth="2"
                  strokeLinecap="round"
                  strokeLinejoin="round"
                >
                  <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4" />
                  <polyline points="17 8 12 3 7 8" />
                  <line x1="12" x2="12" y1="3" y2="15" />
                </svg>
              </div>
              <h3 className="text-lg font-semibold text-white group-hover:text-orange-400">
                Content Management
              </h3>
              <p className="mt-1 text-sm text-zinc-400">
                Upload videos, batch assign, move content
              </p>
            </Link>
    ```

    Insert after line 195 (after the AI Prompts `</Link>` closing tag) and before line 196 (`</div>`).
  </action>
  <verify>Visit /admin and see Content Management link; clicking it navigates to /admin/content</verify>
  <done>Admin dashboard includes Content Management navigation card</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without TypeScript errors
2. Pages exist:
   - src/app/(dashboard)/admin/content/page.tsx
   - src/app/(dashboard)/admin/content/uploads/page.tsx
   - src/app/(dashboard)/admin/content/ContentManagementClient.tsx
3. Admin dashboard has Content Management link
4. Content page shows tabs for Upload and Library
5. Video library supports multi-select with batch assign action
</verification>

<success_criteria>
- /admin/content page loads with upload zone and library tabs
- Upload tab shows drag-drop zone with progress tracking
- Library tab shows unassigned videos with selection
- Batch assign button appears when videos selected
- /admin/content/uploads shows all uploads with status
- Admin dashboard has Content Management navigation card
- Note: BULK-05 (lesson reordering) already satisfied by existing ContentList.tsx from Phase 9
- Note: BatchEditModal exists for courses/modules/lessons but not needed in video library (videos have no editable metadata)
</success_criteria>

<output>
After completion, create `.planning/phases/11-bulk-content-management/11-05-SUMMARY.md`
</output>
