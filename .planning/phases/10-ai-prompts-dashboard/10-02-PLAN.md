---
phase: 10-ai-prompts-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/lib/prompts.ts
  - src/app/api/admin/prompts/route.ts
  - src/app/api/admin/prompts/[promptId]/route.ts
  - src/app/api/admin/prompts/[promptId]/versions/route.ts
  - src/app/api/admin/prompts/[promptId]/versions/[versionId]/restore/route.ts
autonomous: true

must_haves:
  truths:
    - "getPrompt(slug, default) returns prompt content from database or default"
    - "GET /api/admin/prompts returns list of all prompts with type labels"
    - "PUT /api/admin/prompts/[promptId] updates content and creates new version"
    - "GET /api/admin/prompts/[promptId]/versions returns version history"
    - "POST /api/admin/prompts/[promptId]/versions/[versionId]/restore restores old version"
  artifacts:
    - path: "src/lib/prompts.ts"
      provides: "Prompt loading with caching"
      exports: ["getPrompt", "invalidatePromptCache"]
    - path: "src/app/api/admin/prompts/route.ts"
      provides: "Prompt list endpoint"
      exports: ["GET"]
    - path: "src/app/api/admin/prompts/[promptId]/route.ts"
      provides: "Single prompt CRUD"
      exports: ["GET", "PUT"]
    - path: "src/app/api/admin/prompts/[promptId]/versions/route.ts"
      provides: "Version history endpoint"
      exports: ["GET"]
    - path: "src/app/api/admin/prompts/[promptId]/versions/[versionId]/restore/route.ts"
      provides: "Version restore endpoint"
      exports: ["POST"]
  key_links:
    - from: "src/lib/prompts.ts"
      to: "src/db/schema/prompts.ts"
      via: "database query"
      pattern: "db.query.aiPrompts"
    - from: "PUT route"
      to: "src/lib/prompts.ts"
      via: "cache invalidation"
      pattern: "invalidatePromptCache"
---

<objective>
Create prompt loading utilities with caching and complete API routes for prompt management.

Purpose: These utilities and APIs enable the UI to list, edit, and restore prompts. The caching ensures AI features load prompts efficiently.
Output: lib/prompts.ts utility, 5 API route files in /api/admin/prompts hierarchy.
</objective>

<execution_context>
@/home/sheldon/.claude/get-shit-done/workflows/execute-plan.md
@/home/sheldon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-ai-prompts-dashboard/10-RESEARCH.md

# Schema from previous plan
@src/db/schema/prompts.ts

# Existing API patterns
@src/app/api/admin/courses/route.ts
@src/app/api/admin/courses/[courseId]/route.ts

# Auth utility
@src/lib/auth.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create prompt loading utility with caching</name>
  <files>src/lib/prompts.ts</files>
  <action>
Create `src/lib/prompts.ts` with:

1. **In-memory cache** with TTL:
   ```typescript
   const promptCache = new Map<string, { content: string; expires: number }>();
   const CACHE_TTL = 60 * 1000; // 1 minute
   ```

2. **getPrompt(slug: string, defaultContent: string): Promise<string>**
   - Check cache first; if valid (not expired), return cached content
   - Query db.query.aiPrompts.findFirst where slug matches
   - Select only currentContent column for efficiency
   - If found, cache it and return
   - If not found or error, return defaultContent
   - Wrap DB call in try/catch; log errors but don't throw (graceful degradation)

3. **invalidatePromptCache(slug: string): void**
   - Delete the slug from cache
   - Call this after any prompt update

4. **invalidateAllPromptCache(): void**
   - Clear entire cache (useful for debugging/testing)

Export: getPrompt, invalidatePromptCache, invalidateAllPromptCache

Use eq from drizzle-orm for the where clause.
  </action>
  <verify>
TypeScript compiles: `npx tsc --noEmit` passes.
File exists at src/lib/prompts.ts with all exports.
  </verify>
  <done>
prompts.ts utility created with caching logic.
getPrompt, invalidatePromptCache, invalidateAllPromptCache exported.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create API routes for prompt list and single prompt operations</name>
  <files>
    src/app/api/admin/prompts/route.ts
    src/app/api/admin/prompts/[promptId]/route.ts
  </files>
  <action>
**Create `src/app/api/admin/prompts/route.ts`:**

1. **GET handler** - List all prompts:
   - Check hasMinimumRole("coach") - return 403 if not
   - Query db.query.aiPrompts.findMany()
   - Select: id, slug, name, type, description, currentVersion, updatedAt
   - Order by name ascending
   - Return { prompts: [...] }

**Create `src/app/api/admin/prompts/[promptId]/route.ts`:**

1. **GET handler** - Get single prompt:
   - Check hasMinimumRole("coach")
   - Get promptId from params (await params pattern from Next.js 15)
   - Query db.query.aiPrompts.findFirst where id matches
   - Return full prompt object including currentContent
   - 404 if not found

2. **PUT handler** - Update prompt (creates new version):
   - Check hasMinimumRole("coach")
   - Parse body: { content: string, changeNote?: string }
   - Validate content is non-empty string
   - Get current prompt (need slug and currentVersion)
   - 404 if not found
   - Calculate newVersion = currentVersion + 1
   - Use db.transaction:
     a. Insert into aiPromptVersions: promptId, version: newVersion, content, changeNote, createdBy (from currentUser().publicMetadata.dbUserId)
     b. Update aiPrompts: set currentContent, currentVersion, updatedAt
   - Call invalidatePromptCache(slug)
   - Return { success: true, version: newVersion }

Use existing admin route patterns. Import currentUser from @clerk/nextjs/server for getting user ID.
  </action>
  <verify>
`npx tsc --noEmit` passes.
Both route files exist with GET handlers.
PUT handler exists in [promptId]/route.ts.
  </verify>
  <done>
/api/admin/prompts GET returns prompt list.
/api/admin/prompts/[promptId] GET returns single prompt.
/api/admin/prompts/[promptId] PUT updates prompt and creates version.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create API routes for version history and restore</name>
  <files>
    src/app/api/admin/prompts/[promptId]/versions/route.ts
    src/app/api/admin/prompts/[promptId]/versions/[versionId]/restore/route.ts
  </files>
  <action>
**Create `src/app/api/admin/prompts/[promptId]/versions/route.ts`:**

1. **GET handler** - Get version history:
   - Check hasMinimumRole("coach")
   - Get promptId from params
   - Query aiPromptVersions where promptId matches
   - Include createdByUser relation to get editor name/email
   - Order by version descending (newest first)
   - Return { versions: [...] } with id, version, content (truncate to 200 chars for preview), changeNote, createdAt, createdBy (user name/email or "System")

**Create `src/app/api/admin/prompts/[promptId]/versions/[versionId]/restore/route.ts`:**

1. **POST handler** - Restore a previous version:
   - Check hasMinimumRole("coach")
   - Get promptId and versionId from params
   - Find the version to restore (aiPromptVersions where id = versionId AND promptId matches)
   - 404 if not found
   - Get current prompt for slug and currentVersion
   - Calculate newVersion = currentVersion + 1
   - Use db.transaction:
     a. Insert new version with content from restored version, changeNote: "Restored from version X"
     b. Update aiPrompts: currentContent from restored, currentVersion = newVersion
   - Call invalidatePromptCache(slug)
   - Return { success: true, version: newVersion, restoredFrom: versionToRestore.version }

Use and() from drizzle-orm to combine conditions in where clause.
  </action>
  <verify>
`npx tsc --noEmit` passes.
Version routes exist at correct paths.
API structure: /api/admin/prompts/[promptId]/versions and /api/admin/prompts/[promptId]/versions/[versionId]/restore
  </verify>
  <done>
GET /versions returns version history with preview.
POST /versions/[versionId]/restore creates new version from old content.
Cache is invalidated on restore.
  </done>
</task>

</tasks>

<verification>
1. src/lib/prompts.ts exists with getPrompt and cache functions
2. All 5 API route files exist at correct paths
3. `npx tsc --noEmit` passes
4. Test API manually:
   - `curl http://localhost:3000/api/admin/prompts` (with auth) returns prompt list
   - PUT to update a prompt returns new version number
   - GET versions shows history
</verification>

<success_criteria>
- [ ] getPrompt returns cached content or queries database
- [ ] invalidatePromptCache clears cache for specific slug
- [ ] GET /api/admin/prompts returns all prompts with type labels
- [ ] GET /api/admin/prompts/[promptId] returns full prompt with content
- [ ] PUT /api/admin/prompts/[promptId] creates new version and updates prompt
- [ ] GET /api/admin/prompts/[promptId]/versions returns version history
- [ ] POST /api/admin/prompts/[promptId]/versions/[versionId]/restore restores old version as new
- [ ] All routes require coach role (403 for unauthorized)
- [ ] Cache invalidation called after PUT and restore
</success_criteria>

<output>
After completion, create `.planning/phases/10-ai-prompts-dashboard/10-02-SUMMARY.md`
</output>
