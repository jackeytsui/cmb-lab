---
phase: 10-ai-prompts-dashboard
plan: 03
type: execute
wave: 3
depends_on: ["10-02"]
files_modified:
  - src/app/(dashboard)/admin/prompts/page.tsx
  - src/components/admin/PromptList.tsx
  - src/lib/lesson-context.ts
autonomous: true

must_haves:
  truths:
    - "Coach sees /admin/prompts page with list of all AI prompts"
    - "Prompts display type badges (Voice AI, Text Grading, etc.)"
    - "Coach can filter prompts by type"
    - "Voice AI conversation loads prompts from database instead of hardcoded strings"
  artifacts:
    - path: "src/app/(dashboard)/admin/prompts/page.tsx"
      provides: "Admin prompts list page"
      min_lines: 50
    - path: "src/components/admin/PromptList.tsx"
      provides: "Client component for prompt list with filtering"
      min_lines: 60
    - path: "src/lib/lesson-context.ts"
      provides: "Lesson instructions using database prompts"
      contains: "getPrompt"
  key_links:
    - from: "src/app/(dashboard)/admin/prompts/page.tsx"
      to: "/api/admin/prompts"
      via: "server component database query"
      pattern: "db.query.aiPrompts"
    - from: "src/lib/lesson-context.ts"
      to: "src/lib/prompts.ts"
      via: "getPrompt import"
      pattern: "import.*getPrompt.*from.*prompts"
---

<objective>
Create the prompt list UI page and wire voice AI to use database prompts.

Purpose: Coaches can view all prompts in the system. Voice AI starts loading prompts from database, proving the system works end-to-end.
Output: Admin prompts list page, PromptList component, updated lesson-context.ts using getPrompt.
</objective>

<execution_context>
@/home/sheldon/.claude/get-shit-done/workflows/execute-plan.md
@/home/sheldon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-ai-prompts-dashboard/10-RESEARCH.md

# Prompt utilities from previous plan
@src/lib/prompts.ts

# Existing admin page patterns
@src/app/(dashboard)/admin/students/page.tsx
@src/components/admin/StudentList.tsx

# Current hardcoded prompts to replace
@src/lib/lesson-context.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create admin prompts list page</name>
  <files>src/app/(dashboard)/admin/prompts/page.tsx</files>
  <action>
Create `src/app/(dashboard)/admin/prompts/page.tsx` as a server component:

1. **Access control:** Check hasMinimumRole("coach") - redirect to /dashboard if not

2. **Data fetching:** Query db.query.aiPrompts.findMany() directly (server component pattern):
   - Select: id, slug, name, type, description, currentVersion, updatedAt
   - Order by name ascending

3. **Page structure** following admin/students/page.tsx pattern:
   - Breadcrumb: Admin > AI Prompts
   - Header with icon (Sparkles from lucide-react), title "AI Prompts"
   - Description showing prompt count
   - PromptList client component with initialPrompts prop

4. **Data formatting:** Convert dates to ISO strings for client component

5. **Styling:** Use existing admin page patterns (bg-zinc-900, text-white, container mx-auto px-4 py-8)

Import PromptList from "@/components/admin/PromptList".
  </action>
  <verify>
File exists at src/app/(dashboard)/admin/prompts/page.tsx.
`npx tsc --noEmit` passes.
  </verify>
  <done>
Admin prompts page renders at /admin/prompts.
Shows list of prompts with count.
Access restricted to coach+ role.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create PromptList client component with type filtering</name>
  <files>src/components/admin/PromptList.tsx</files>
  <action>
Create `src/components/admin/PromptList.tsx` as a client component:

1. **Props interface:**
   ```typescript
   interface Prompt {
     id: string;
     slug: string;
     name: string;
     type: string;
     description: string | null;
     currentVersion: number;
     updatedAt: string;
   }
   ```

2. **Type labels map:**
   ```typescript
   const typeLabels: Record<string, { label: string; color: string }> = {
     grading_text: { label: "Text Grading", color: "bg-cyan-500/10 text-cyan-400" },
     grading_audio: { label: "Audio Grading", color: "bg-purple-500/10 text-purple-400" },
     voice_ai: { label: "Voice AI", color: "bg-green-500/10 text-green-400" },
     chatbot: { label: "Chatbot", color: "bg-yellow-500/10 text-yellow-400" },
   };
   ```

3. **State:** filter (string, default "all")

4. **Filter tabs:** Buttons for "All", "Text Grading", "Audio Grading", "Voice AI", "Chatbot"
   - Active tab: bg-zinc-700 text-white
   - Inactive: text-zinc-400 hover:text-white

5. **Filtered list:** Apply filter to prompts array

6. **Prompt cards:** For each prompt, render Link to /admin/prompts/[id]:
   - Card styling: rounded-lg border border-zinc-700 bg-zinc-800 p-4 hover:border-zinc-600
   - Name as h3 (font-medium text-white)
   - Description as p (text-sm text-zinc-400)
   - Version and "Updated X ago" (text-xs text-zinc-500) - use formatDistanceToNow from date-fns
   - Type badge on right side with appropriate color

7. **Empty state:** If no prompts match filter, show message

Use Link from next/link for navigation.
  </action>
  <verify>
File exists at src/components/admin/PromptList.tsx.
`npx tsc --noEmit` passes.
  </verify>
  <done>
PromptList renders prompts with type badges.
Filter tabs work to show specific types.
Each prompt links to detail page.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire voice AI to load prompts from database</name>
  <files>src/lib/lesson-context.ts</files>
  <action>
Update `src/lib/lesson-context.ts` to use database prompts:

1. **Import getPrompt:**
   ```typescript
   import { getPrompt } from "@/lib/prompts";
   ```

2. **Define default prompts as constants** (fallback if DB unavailable):
   - DEFAULT_VOICE_TUTOR_SYSTEM: The current buildGenericInstructions() content
   - DEFAULT_LESSON_TEMPLATE: Template with {{lessonTitle}}, {{courseTitle}}, {{moduleTitle}}, {{vocabulary}}

3. **Update buildLessonInstructions:**
   - Load system prompt: `await getPrompt("voice-tutor-system", DEFAULT_VOICE_TUTOR_SYSTEM)`
   - Load lesson template: `await getPrompt("voice-tutor-lesson-template", DEFAULT_LESSON_TEMPLATE)`
   - If lesson found:
     - Replace {{lessonTitle}} with lesson.lessonTitle
     - Replace {{courseTitle}} with lesson.courseTitle
     - Replace {{moduleTitle}} with lesson.moduleTitle
     - Replace {{vocabulary}} with vocabularySection
     - Combine system prompt + lesson context
   - If lesson not found: return just system prompt (generic instructions)

4. **Remove buildGenericInstructions function** - now handled by getPrompt fallback

5. **Keep the DB queries for lesson/vocabulary** - those are data, not prompts

The key change: Replace hardcoded prompt strings with getPrompt() calls that have the same content as defaults.
  </action>
  <verify>
`npx tsc --noEmit` passes.
lesson-context.ts imports getPrompt.
Voice AI conversation still works (test manually if dev server running).
  </verify>
  <done>
buildLessonInstructions uses getPrompt for system prompt and lesson template.
Fallback defaults ensure voice AI works even if DB query fails.
Template variables {{lessonTitle}} etc. are replaced with actual values.
  </done>
</task>

</tasks>

<verification>
1. Admin prompts page accessible at /admin/prompts
2. Page shows list of seeded prompts with type badges
3. Filter tabs work to show Voice AI prompts only
4. Voice AI conversation still functions (loads prompts from DB)
5. Clicking a prompt links to /admin/prompts/[id] (404 expected until Plan 04)
</verification>

<success_criteria>
- [ ] /admin/prompts page exists and requires coach role
- [ ] Page shows all AI prompts from database
- [ ] Type badges display with correct colors (Voice AI = green)
- [ ] Filter tabs filter prompts by type
- [ ] Each prompt card shows name, description, version, last updated
- [ ] lesson-context.ts uses getPrompt instead of hardcoded strings
- [ ] Voice AI conversation works with database prompts
- [ ] Fallback to defaults if database unavailable
</success_criteria>

<output>
After completion, create `.planning/phases/10-ai-prompts-dashboard/10-03-SUMMARY.md`
</output>
