---
phase: 60-session-management-coach-review
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/video-thread/VideoThreadPlayer.tsx
  - src/types/video-thread-player.ts
  - src/app/(dashboard)/dashboard/threads/[threadId]/page.tsx
  - src/app/api/video-threads/[threadId]/route.ts
autonomous: true

must_haves:
  truths:
    - "Student can navigate back to previously completed steps using a back button and the step history is visually indicated"
    - "Student who left mid-thread can return and resume from the last completed step"
    - "Completion screen appears when no more steps remain and cannot be bypassed via back button"
  artifacts:
    - path: "src/components/video-thread/VideoThreadPlayer.tsx"
      provides: "Back button using history array, resume from lastStepId, GO_BACK action"
    - path: "src/types/video-thread-player.ts"
      provides: "GO_BACK action type"
    - path: "src/app/(dashboard)/dashboard/threads/[threadId]/page.tsx"
      provides: "Session resume query passing lastStepId + sessionId to player"
    - path: "src/app/api/video-threads/[threadId]/route.ts"
      provides: "GET endpoint returns existing in_progress session for authenticated user"
  key_links:
    - from: "src/app/(dashboard)/dashboard/threads/[threadId]/page.tsx"
      to: "videoThreadSessions table"
      via: "Drizzle query for existing in_progress session"
      pattern: "videoThreadSessions.*in_progress"
    - from: "src/components/video-thread/VideoThreadPlayer.tsx"
      to: "state.history"
      via: "GO_BACK action pops last step from history"
      pattern: "GO_BACK|history.*pop|history.*slice"
---

<objective>
Add student session resume and step history navigation to the thread player.

Purpose: Students who close the browser mid-thread can return and pick up where they left off. Students who want to review a previous step can use back navigation without losing progress.

Output: Updated VideoThreadPlayer with back button, session resume via server-side lastStepId lookup, and proper history state management.
</objective>

<execution_context>
@/Users/sheldonho/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sheldonho/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-student-player-foundation/58-01-SUMMARY.md
@.planning/phases/58-student-player-foundation/58-02-SUMMARY.md
@.planning/phases/59-media-responses-logic-backend/59-02-SUMMARY.md
@src/components/video-thread/VideoThreadPlayer.tsx
@src/types/video-thread-player.ts
@src/app/(dashboard)/dashboard/threads/[threadId]/page.tsx
@src/app/api/video-threads/[threadId]/route.ts
@src/db/schema/video-threads.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add session resume to thread player page and GET endpoint</name>
  <files>
    src/app/(dashboard)/dashboard/threads/[threadId]/page.tsx
    src/app/api/video-threads/[threadId]/route.ts
  </files>
  <action>
  **GET endpoint (`route.ts`):**
  Add an optional query to the existing GET handler that, for the authenticated user, looks up the most recent `in_progress` session for this thread+student. Return `existingSession: { id, lastStepId } | null` alongside the existing `{ thread, steps }` response. Use `getCurrentUser()` (already imported pattern) to get `user.id`, then query `videoThreadSessions` with `where: and(eq(threadId), eq(studentId, user.id), eq(status, 'in_progress'))` ordered by `startedAt desc`, limit 1. If found, return `existingSession: { id: session.id, lastStepId: session.lastStepId }`. If not found, return `existingSession: null`.

  **Page (`page.tsx`):**
  After fetching thread+steps, also query `videoThreadSessions` for the authenticated user's most recent `in_progress` session (same query pattern as above -- use the DB user id from `getCurrentUser()` since `auth()` only returns clerkId). Pass `resumeSessionId` and `resumeStepId` as props to `VideoThreadPlayer`. Update the `VideoThreadPlayerProps` interface in the component to accept these optional props.

  The page server component should do the session lookup directly via Drizzle (following the v3.1 pattern of server component DB queries, not self-fetching API routes).
  </action>
  <verify>TypeScript compiles (`npx tsc --noEmit` on the modified files). The page passes resumeSessionId and resumeStepId to VideoThreadPlayer.</verify>
  <done>Thread player page queries for existing in_progress session and passes resume data to player component. GET endpoint also returns existingSession for API consumers.</done>
</task>

<task type="auto">
  <name>Task 2: Add back navigation and session resume to VideoThreadPlayer</name>
  <files>
    src/components/video-thread/VideoThreadPlayer.tsx
    src/types/video-thread-player.ts
  </files>
  <action>
  **Types (`video-thread-player.ts`):**
  Add `GO_BACK` action to the `PlayerAction` union type (no payload needed -- it pops the last step from history). Also add `resumeSessionId?: string | null` and `resumeStepId?: string | null` to the props or a new initialization type. These are not needed in the action union since they are initial props.

  **Player component (`VideoThreadPlayer.tsx`):**

  1. **Props:** Add `resumeSessionId?: string | null` and `resumeStepId?: string | null` to `VideoThreadPlayerProps`.

  2. **Initial state:** In the `useReducer` initializer, if `resumeStepId` is provided AND the step exists in the `steps` array, use it as `currentStepId` instead of the first step. Also set `sessionId: resumeSessionId ?? null` so subsequent responses attach to the existing session.

  3. **GO_BACK reducer case:** When `GO_BACK` is dispatched:
     - If `state.history.length === 0`, do nothing (return state unchanged).
     - Otherwise, pop the last entry from `state.history` (use `slice(0, -1)`) and set `currentStepId` to the popped value. Set `status: 'playing'` and `recordingMode: null`.

  4. **Back button UI:** In the overlay (inside the gradient div, ABOVE the prompt text), render a back button when `state.history.length > 0`:
     ```
     <button onClick={() => dispatch({ type: 'GO_BACK' })} className="...">
       <ChevronLeft /> Back
     </button>
     ```
     Style: `text-white/70 hover:text-white text-sm flex items-center gap-1 mb-2 transition-colors`. Import `ChevronLeft` from lucide-react.

  5. **Step indicator:** Below the back button and above the prompt, show a subtle step counter: `Step {currentIndex + 1} of {steps.length}` in `text-white/50 text-xs`. Calculate `currentIndex` as `steps.findIndex(s => s.id === state.currentStepId)`.

  6. **Completion screen polish:** The existing completion screen is already functional. Add a "Restart" button below the "Back to Dashboard" link that dispatches `INIT_THREAD` to reset state and replay from step 0. Style: `text-zinc-400 hover:text-white text-sm underline`.
  </action>
  <verify>TypeScript compiles. The player renders a back button when history exists. Dispatching GO_BACK navigates to the previous step. Resume props correctly set initial step to lastStepId.</verify>
  <done>Student can navigate back through completed steps, resume incomplete sessions from lastStepId, and see step progress indicator. Completion screen has a restart option.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes without errors
2. The VideoThreadPlayer accepts resumeSessionId and resumeStepId props
3. The GO_BACK action type exists in PlayerAction union
4. The back button only appears when history has entries
5. The thread player page queries videoThreadSessions for resume data
6. The completion screen shows both "Back to Dashboard" and "Restart" options
</verification>

<success_criteria>
- Student with an existing in_progress session sees the thread resume from their last step
- Student can click back to revisit previous steps without losing session state
- Step counter shows current position (e.g., "Step 2 of 5")
- Completion screen offers restart option
- All TypeScript types compile cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/60-session-management-coach-review/60-01-SUMMARY.md`
</output>
