---
phase: 42-coach-practice-results
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [src/components/coach/PracticeResultsPanel.tsx]
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Coach can filter by student name and results narrow to matching students"
    - "Coach can filter by practice set and results narrow to that set"
    - "Coach can filter by date range and results narrow to that range"
    - "Coach can filter by score range and results narrow to that range"
    - "Filters are combinable — applying multiple filters narrows results additively"
  artifacts:
    - path: "src/components/coach/PracticeResultsPanel.tsx"
      provides: "buildQueryString sending correct param names to API"
      contains: "params.set(\"student\""
  key_links:
    - from: "src/components/coach/PracticeResultsPanel.tsx"
      to: "src/app/api/coach/practice-results/route.ts"
      via: "query param names in buildQueryString"
      pattern: "params\\.set\\(\"(student|setId|from|to|scoreMin|scoreMax)\""
---

<objective>
Fix query parameter name mismatches between PracticeResultsPanel client component and the practice results API route, restoring all 4 filter dimensions.

Purpose: The client sends 'studentName', 'practiceSetId', 'dateFrom', and 'dateTo' but the API expects 'student', 'setId', 'from', and 'to'. This causes 4 of 6 filter dimensions to silently fail — the API never receives the filter values so it returns unfiltered results. The scoreMin/scoreMax params already match and work correctly.

Output: Fixed PracticeResultsPanel.tsx with all 6 query param names matching API expectations.
</objective>

<execution_context>
@/home/sheldon/.claude/get-shit-done/workflows/execute-plan.md
@/home/sheldon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-coach-practice-results/42-VERIFICATION.md
@src/components/coach/PracticeResultsPanel.tsx
@src/app/api/coach/practice-results/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix query param name mismatches in buildQueryString</name>
  <files>src/components/coach/PracticeResultsPanel.tsx</files>
  <action>
    In `buildQueryString()` function, fix 4 query parameter names to match what the API route expects:

    1. Line 14: Change `params.set("studentName", filters.studentName)` to `params.set("student", filters.studentName)`
    2. Line 16: Change `params.set("practiceSetId", filters.practiceSetId)` to `params.set("setId", filters.practiceSetId)`
    3. Line 18: Change `params.set("dateFrom", filters.dateFrom)` to `params.set("from", filters.dateFrom)`
    4. Line 19: Change `params.set("dateTo", filters.dateTo)` to `params.set("to", filters.dateTo)`

    The API route (route.ts) reads these params as:
    - `searchParams.get("student")` (line 38)
    - `searchParams.get("setId")` (line 43)
    - `searchParams.get("from")` (line 48)
    - `searchParams.get("to")` (line 55)
    - `searchParams.get("scoreMin")` (line 64) — already correct
    - `searchParams.get("scoreMax")` (line 72) — already correct

    Do NOT change the Filters interface property names (studentName, practiceSetId, dateFrom, dateTo) — those are internal state names used throughout the filter components. Only change the string keys passed to `params.set()`.

    The VERIFICATION.md only identified 2 mismatches (studentName/practiceSetId), but upon code inspection, dateFrom/dateTo are also mismatched (API expects "from"/"to"). Fix all 4.
  </action>
  <verify>
    1. `npx tsc --noEmit` — zero type errors
    2. `npm run build` — passes
    3. Grep the fixed file to confirm all 6 param names match API expectations:
       - `grep -n 'params.set' src/components/coach/PracticeResultsPanel.tsx` should show: "student", "setId", "from", "to", "scoreMin", "scoreMax"
    4. Cross-reference with API route: `grep -n 'searchParams.get' src/app/api/coach/practice-results/route.ts` — param names must match exactly
  </verify>
  <done>
    All 6 query param names in buildQueryString() match the API route's searchParams.get() calls exactly: student, setId, from, to, scoreMin, scoreMax. TypeScript compiles cleanly. Build passes.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — zero errors
2. `npm run build` — passes
3. All 6 filter dimensions use matching param names between client and API:
   - student ↔ student ✓
   - setId ↔ setId ✓
   - from ↔ from ✓
   - to ↔ to ✓
   - scoreMin ↔ scoreMin ✓
   - scoreMax ↔ scoreMax ✓
</verification>

<success_criteria>
- PracticeResultsPanel.tsx buildQueryString() sends param names that exactly match API route's searchParams.get() calls
- All 4 previously-broken filter dimensions (student, set, dateFrom, dateTo) now reach the API correctly
- TypeScript compiles, build passes
- COACH-03 requirement (combinable filters) unblocked
</success_criteria>

<output>
After completion, create `.planning/phases/42-coach-practice-results/42-03-SUMMARY.md`
</output>
