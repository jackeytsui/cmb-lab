---
phase: 75-lto-student-access-mandarin-accelerator
plan: 04
type: execute
wave: 2
depends_on: [75-01]
files_modified:
  - src/app/api/admin/accelerator/reader/route.ts
  - src/app/(dashboard)/admin/accelerator/reader/page.tsx
  - src/app/(dashboard)/admin/accelerator/reader/AdminReaderClient.tsx
  - src/app/(dashboard)/dashboard/accelerator/reader/page.tsx
  - src/app/(dashboard)/dashboard/accelerator/reader/[passageId]/page.tsx
  - src/app/api/accelerator/reader/progress/route.ts
autonomous: true
requirements: [LTO-13, LTO-14, LTO-15]

must_haves:
  truths:
    - "Coach can create, edit, and delete curated passages via admin panel"
    - "Coach can bulk upload passages via JSON"
    - "Student sees a list of 5 passages with read/unread badges"
    - "Student opens a passage in the existing Reader with full features"
    - "Student cannot create/import their own passages from the curated reader page"
    - "Read status is tracked when student opens a passage"
  artifacts:
    - path: "src/app/api/admin/accelerator/reader/route.ts"
      provides: "CRUD + bulk upload API for curated passages"
      exports: ["GET", "POST", "PUT", "DELETE"]
    - path: "src/app/(dashboard)/dashboard/accelerator/reader/[passageId]/page.tsx"
      provides: "Curated passage opened in ReaderClient with initialText and hidden import UI"
      contains: "ReaderClient"
    - path: "src/app/api/accelerator/reader/progress/route.ts"
      provides: "Read status tracking API"
      exports: ["GET", "POST"]
  key_links:
    - from: "[passageId]/page.tsx"
      to: "src/app/(dashboard)/dashboard/reader/ReaderClient.tsx"
      via: "ReaderClient with initialText prop"
      pattern: "ReaderClient.*initialText"
    - from: "[passageId]/page.tsx"
      to: "/api/accelerator/reader/progress"
      via: "mark as read on open"
      pattern: "accelerator/reader/progress"
---

<objective>
Build the Comprehensive AI Reader Passages feature: admin content management (CRUD + JSON bulk upload) and student-facing curated passages list with read/unread badges that open in the existing ReaderClient with full features but no import/create capability.

Purpose: Third of three Mandarin Accelerator features. Students read preloaded Mandarin passages with full Reader features.
Output: Working admin panel for passage management, working student passages list with Reader integration and read status tracking.
</objective>

<execution_context>
@/Users/jackeytsui/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jackeytsui/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/75-lto-student-access-mandarin-accelerator/75-CONTEXT.md
@.planning/phases/75-lto-student-access-mandarin-accelerator/75-RESEARCH.md
@.planning/phases/75-lto-student-access-mandarin-accelerator/75-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 schema (src/db/schema/accelerator.ts) -->
```typescript
export const curatedPassages = pgTable("curated_passages", {
  id: uuid("id").defaultRandom().primaryKey(),
  title: text("title").notNull(),
  body: text("body").notNull(),
  sortOrder: integer("sort_order").notNull().default(0),
  createdBy: uuid("created_by").references(() => users.id),
  createdAt: timestamp("created_at").notNull().defaultNow(),
  updatedAt: timestamp("updated_at").notNull().defaultNow().$onUpdate(() => new Date()),
});

export const passageReadStatus = pgTable("passage_read_status", {
  id: uuid("id").defaultRandom().primaryKey(),
  userId: uuid("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
  passageId: uuid("passage_id").notNull().references(() => curatedPassages.id, { onDelete: "cascade" }),
  readAt: timestamp("read_at").notNull().defaultNow(),
});
```

<!-- From src/app/(dashboard)/dashboard/reader/ReaderClient.tsx -->
```typescript
export function ReaderClient({ initialText }: { initialText?: string }) {
  // ...
  const [rawText, setRawText] = useState(initialText ?? "");
  // Renders ImportDialog, ReaderToolbar, ReaderTextArea, CharacterPopup
}
```

<!-- ImportDialog renders a paste/import UI that must be hidden for curated mode -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Admin API and panel for curated passages</name>
  <files>
    src/app/api/admin/accelerator/reader/route.ts,
    src/app/(dashboard)/admin/accelerator/reader/page.tsx,
    src/app/(dashboard)/admin/accelerator/reader/AdminReaderClient.tsx
  </files>
  <read_first>
    src/db/schema/accelerator.ts,
    src/app/(dashboard)/admin/grammar/page.tsx,
    src/app/(dashboard)/admin/grammar/AdminGrammarClient.tsx,
    src/lib/auth.ts
  </read_first>
  <action>
1. **Create `src/app/api/admin/accelerator/reader/route.ts`:**
   - Guard all handlers with `await requireMinimumRole("coach")`.
   - **GET**: Fetch all curated passages ordered by sortOrder. Return JSON array.
   - **POST**: Accept single or bulk format.
     - Single schema: `z.object({ title: z.string().min(1), body: z.string().min(1), sortOrder: z.number().int().optional() })`
     - Bulk schema: `z.object({ passages: z.array(singleSchema) })`
     - Use `z.union([singleSchema, bulkSchema])` to accept both.
     - Set `createdBy` to current user's DB ID.
     - Return 201.
   - **PUT**: Accept `{ id, title?, body?, sortOrder? }`. Update by ID. Return updated record.
   - **DELETE**: Accept `{ id }`. Delete by ID. Return 204.

2. **Create `src/app/(dashboard)/admin/accelerator/reader/page.tsx`:**
   - Server component. Guard with `await requireMinimumRole("coach")`.
   - Render heading "Manage Curated Passages" and `<AdminReaderClient />`.

3. **Create `src/app/(dashboard)/admin/accelerator/reader/AdminReaderClient.tsx`:**
   - "use client" component.
   - Fetch passages from `/api/admin/accelerator/reader` on mount.
   - Display a table: Title, Body (truncated to 100 chars), Sort Order, Actions (Edit/Delete).
   - "Add Passage" button: dialog with title (text input) and body (large textarea for Chinese text) and sortOrder fields.
   - "Bulk Upload JSON" button: accept JSON file with `{ passages: [{ title, body, sortOrder? }] }` format. POST to API.
   - Edit: dialog with prepopulated fields, PUT to API.
   - Delete: confirmation, DELETE to API.
   - Use shadcn/ui: Button, Input, Textarea, Table, Dialog, Label.
  </action>
  <verify>
    <automated>cd /Users/jackeytsui/Downloads/New-LMS-main && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - reader/route.ts exports GET, POST, PUT, DELETE handlers
    - reader/route.ts contains `requireMinimumRole("coach")` in every handler
    - reader/route.ts contains `z.union` for single/bulk upload
    - reader/route.ts contains `db.insert(curatedPassages)` for creation
    - AdminReaderClient.tsx is a "use client" component
    - AdminReaderClient.tsx contains a file input or upload button for JSON bulk upload
    - admin page.tsx contains `requireMinimumRole("coach")`
    - TypeScript compiles cleanly
  </acceptance_criteria>
  <done>Coach can view, create, bulk upload, edit, and delete curated passages. All operations are coach-gated.</done>
</task>

<task type="auto">
  <name>Task 2: Student curated passages list and reader integration with read status</name>
  <files>
    src/app/(dashboard)/dashboard/accelerator/reader/page.tsx,
    src/app/(dashboard)/dashboard/accelerator/reader/[passageId]/page.tsx,
    src/app/api/accelerator/reader/progress/route.ts
  </files>
  <read_first>
    src/db/schema/accelerator.ts,
    src/app/(dashboard)/dashboard/reader/ReaderClient.tsx,
    src/components/auth/FeatureGate.tsx,
    src/components/reader/ImportDialog.tsx,
    .planning/phases/75-lto-student-access-mandarin-accelerator/75-CONTEXT.md
  </read_first>
  <action>
1. **Create `src/app/api/accelerator/reader/progress/route.ts`:**
   - Auth: get current user via Clerk auth + DB lookup. Return 401 if not authenticated.
   - **GET**: Fetch all `passageReadStatus` records for the current user. Return `{ readPassageIds: string[] }`.
   - **POST**: Accept `{ passageId: string }`. Upsert into `passageReadStatus` (use `onConflictDoNothing` since re-reading is a no-op). Return 200.

2. **Create `src/app/(dashboard)/dashboard/accelerator/reader/page.tsx`:**
   - Server component. Wrap in `<FeatureGate feature="mandarin_accelerator">`.
   - Fetch all curated passages from DB (ordered by sortOrder).
   - Fetch current user's read status (read passage IDs).
   - Page title: "AI Reader (Curated Passages)".
   - **Passages List (D-21):** Render a simple list/grid of passage cards. Each card shows:
     - Passage title (large text)
     - "Read" badge (green, shown if passageId is in readPassageIds) or "Unread" badge (gray/muted) per D-24
     - Click/tap navigates to `/dashboard/accelerator/reader/{passageId}`
   - Simple, clean design. 1-column list on mobile, 2 columns on desktop.

3. **Create `src/app/(dashboard)/dashboard/accelerator/reader/[passageId]/page.tsx`:**
   - Server component. Wrap in `<FeatureGate feature="mandarin_accelerator">`.
   - Fetch the passage by ID from DB. If not found, return `notFound()`.
   - **Mark as read:** Get current user, then call server-side DB insert (upsert) into `passageReadStatus` for this user + passage. This marks the passage as read immediately when the page loads.
   - **Render Reader (D-22):** Import `ReaderClient` from `@/app/(dashboard)/dashboard/reader/ReaderClient`. Render `<ReaderClient initialText={passage.body} />`.
   - **Hide Import UI (D-22):** The `ReaderClient` renders an `ImportDialog` that allows users to paste/import text. To prevent LTO students from creating their own content:
     - Option A (preferred -- minimal change): Add a CSS wrapper that hides the ImportDialog. Wrap the ReaderClient in a div with className that uses Tailwind to hide the import dialog:
       ```tsx
       <div className="[&_.import-dialog-trigger]:hidden">
         <ReaderClient initialText={passage.body} />
       </div>
       ```
       Check if ImportDialog has a detectable class or data attribute. If it uses a shadcn Dialog trigger with a specific class, target that.
     - Option B (if Option A is unreliable): Add a `hideImport?: boolean` prop to `ReaderClient`. When `hideImport` is true, skip rendering `<ImportDialog>`. This is a one-line conditional change in ReaderClient.tsx:
       ```tsx
       {!hideImport && <ImportDialog ... />}
       ```
       Use Option B if the ImportDialog trigger doesn't have a targetable CSS selector.
   - Add a "Back to Passages" link at the top of the page.

   **Important per D-22:** All Reader features MUST work -- dictionary popup, TTS, annotations, vocab save. The ONLY restriction is hiding the import/create-own-content UI.
  </action>
  <verify>
    <automated>cd /Users/jackeytsui/Downloads/New-LMS-main && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - reader/page.tsx wraps content in FeatureGate with feature="mandarin_accelerator"
    - reader/page.tsx renders passage cards with "Read" or "Unread" badge text
    - [passageId]/page.tsx wraps content in FeatureGate with feature="mandarin_accelerator"
    - [passageId]/page.tsx imports and renders ReaderClient with initialText prop set to passage.body
    - [passageId]/page.tsx contains logic to hide ImportDialog (either CSS wrapper or hideImport prop)
    - [passageId]/page.tsx contains upsert into passageReadStatus (server-side mark as read)
    - progress/route.ts exports GET and POST handlers
    - progress/route.ts contains `onConflictDoNothing` for upsert pattern
    - TypeScript compiles cleanly
  </acceptance_criteria>
  <done>Student sees a list of 5 curated passages with read/unread badges, can open any passage in the full Reader with dictionary popup, TTS, annotations, and vocab save, but cannot import/create their own content. Read status is tracked automatically on page open.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. Admin: Passage CRUD with bulk upload works
3. Student: Passage list shows read/unread badges
4. Student: Opening passage renders ReaderClient with initialText
5. ImportDialog is hidden in curated reader view
6. Read status is recorded on page load
</verification>

<success_criteria>
- Coach can manage curated passages (CRUD + bulk JSON upload) via admin panel (D-23)
- 5 passages shown in a list with titles and read/unread badges (D-21, D-24)
- Passage opens in existing Reader with full features (D-22)
- Import/create UI is hidden -- LTO students cannot create own content (D-22)
- Read status tracked automatically when passage is opened (D-24)
</success_criteria>

<output>
After completion, create `.planning/phases/75-lto-student-access-mandarin-accelerator/75-04-SUMMARY.md`
</output>
