---
phase: 26-error-handling-resilience
plan: 02
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - src/app/(dashboard)/admin/analytics/AnalyticsDashboard.tsx
  - src/components/admin/StudentList.tsx
  - src/components/search/SearchBar.tsx
  - src/app/(dashboard)/admin/knowledge/search/SearchPageClient.tsx
autonomous: true

must_haves:
  truths:
    - "Admin can tell when analytics data failed to load (visible error message) and can retry without refreshing the page"
    - "Admin can tell when the student list failed to load (visible error message) and can retry without refreshing the page"
    - "User can distinguish between 'search found nothing' and 'search is broken' in the global search bar"
    - "Admin can distinguish between 'no KB results' and 'KB search failed' on the knowledge base search page"
  artifacts:
    - path: "src/app/(dashboard)/admin/analytics/AnalyticsDashboard.tsx"
      provides: "Analytics dashboard with visible error states per API call"
      contains: "ErrorAlert"
    - path: "src/components/admin/StudentList.tsx"
      provides: "Student list with error state and retry"
      contains: "ErrorAlert"
    - path: "src/components/search/SearchBar.tsx"
      provides: "Search bar that distinguishes error from empty results"
      contains: "error"
    - path: "src/app/(dashboard)/admin/knowledge/search/SearchPageClient.tsx"
      provides: "KB search that distinguishes error from empty results"
      contains: "error"
  key_links:
    - from: "src/app/(dashboard)/admin/analytics/AnalyticsDashboard.tsx"
      to: "src/components/ui/error-alert.tsx"
      via: "import { ErrorAlert }"
      pattern: "import.*ErrorAlert.*from.*error-alert"
    - from: "src/components/admin/StudentList.tsx"
      to: "src/components/ui/error-alert.tsx"
      via: "import { ErrorAlert }"
      pattern: "import.*ErrorAlert.*from.*error-alert"
    - from: "src/components/search/SearchBar.tsx"
      to: "search results dropdown"
      via: "searchError state rendered inline in dropdown before empty-state check"
      pattern: "searchError"
    - from: "src/app/(dashboard)/admin/knowledge/search/SearchPageClient.tsx"
      to: "search results area"
      via: "searchError state rendered inline before no-results check"
      pattern: "searchError"
---

<objective>
Fix the HIGH and critical MEDIUM severity components that silently swallow errors or confuse errors with empty states. These are the most impactful fixes: components where users currently have no idea something is wrong.

Purpose: The research audit identified 4 components with the worst error handling patterns: AnalyticsDashboard (silently ignores failed API calls), StudentList (console-only errors), SearchBar (error looks like "no results"), and SearchPageClient (error looks like "no results"). These are the most confusing failures because the user has no indication anything went wrong.

Output: 4 modified files, each with visible error states using the ErrorAlert component from Plan 01.
</objective>

<execution_context>
@/home/sheldon/.claude/get-shit-done/workflows/execute-plan.md
@/home/sheldon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-error-handling-resilience/26-RESEARCH.md
@.planning/phases/26-error-handling-resilience/26-01-SUMMARY.md
@src/app/(dashboard)/admin/analytics/AnalyticsDashboard.tsx
@src/components/admin/StudentList.tsx
@src/components/search/SearchBar.tsx
@src/app/(dashboard)/admin/knowledge/search/SearchPageClient.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix AnalyticsDashboard and StudentList (HIGH severity — silent failures)</name>
  <files>src/app/(dashboard)/admin/analytics/AnalyticsDashboard.tsx, src/components/admin/StudentList.tsx</files>
  <action>
**AnalyticsDashboard** (`src/app/(dashboard)/admin/analytics/AnalyticsDashboard.tsx`):

Read the file first. The current pattern is:
```
const [overviewRes, completionRes, ...] = await Promise.all([...]);
if (overviewRes.ok) setOverview(await overviewRes.json());
// No else — silent failure
```

Fix by:
1. Add `import { ErrorAlert } from '@/components/ui/error-alert'` at the top.
2. Add a new state variable: `const [error, setError] = useState<string | null>(null)`.
3. In the fetch function (likely `fetchData` or `useEffect` with fetches), wrap the existing logic:
   - At the start of the fetch: `setError(null)`.
   - Track which endpoints fail: create a `failedEndpoints: string[]` array.
   - For each response, if `!res.ok`, push a label to `failedEndpoints` (e.g., 'overview stats', 'completion data', 'drop-off data', etc.).
   - After all fetches, if `failedEndpoints.length > 0`: `setError(\`Failed to load: ${failedEndpoints.join(', ')}. Some data may be incomplete.\`)`.
   - In the catch block: `setError('Failed to load analytics data. Please try again.')`.
4. In the JSX, render the error banner near the top of the dashboard content (before the data sections, after any date range selector):
   ```tsx
   {error && <ErrorAlert message={error} onRetry={() => fetchData(dateRange)} />}
   ```
   Adjust `fetchData(dateRange)` to match the actual function name and params in the file.

**StudentList** (`src/components/admin/StudentList.tsx`):

Read the file first. The current pattern is:
```
catch (error) { console.error('Error fetching students:', error); }
```

Fix by:
1. Add `import { ErrorAlert } from '@/components/ui/error-alert'`.
2. Add a state variable: `const [error, setError] = useState<string | null>(null)`.
3. In the fetch function:
   - At the start: `setError(null)`.
   - In the catch block: replace `console.error(...)` with `setError('Failed to load student list. Please try again.')`. Keep the console.error too for debugging.
4. In the JSX, render the error state. Find where the student list/table is rendered. BEFORE the table (but after any search/filter controls), add:
   ```tsx
   {error && <ErrorAlert message={error} onRetry={() => fetchStudents()} variant="block" />}
   ```
   When error is shown and students array is empty, don't render the table. If students array has stale data from a previous successful fetch, still show the error banner above the table so the user knows the refresh failed.
   Adjust `fetchStudents()` to match the actual function name in the file.
  </action>
  <verify>
Run: `grep "ErrorAlert" src/app/\(dashboard\)/admin/analytics/AnalyticsDashboard.tsx` — should match (imported and used).
Run: `grep "setError" src/app/\(dashboard\)/admin/analytics/AnalyticsDashboard.tsx` — should match.
Run: `grep "ErrorAlert" src/components/admin/StudentList.tsx` — should match.
Run: `grep "setError" src/components/admin/StudentList.tsx` — should match.
Run: `grep "onRetry" src/app/\(dashboard\)/admin/analytics/AnalyticsDashboard.tsx` — should match (retry wired).
Run: `grep "onRetry" src/components/admin/StudentList.tsx` — should match (retry wired).
  </verify>
  <done>AnalyticsDashboard shows a visible error banner listing which data sections failed to load, with a retry button. StudentList admin shows a visible error with retry button instead of silently swallowing fetch failures.</done>
</task>

<task type="auto">
  <name>Task 2: Fix SearchBar and SearchPageClient (MEDIUM severity — error masquerades as empty results)</name>
  <files>src/components/search/SearchBar.tsx, src/app/(dashboard)/admin/knowledge/search/SearchPageClient.tsx</files>
  <action>
**SearchBar** (`src/components/search/SearchBar.tsx`):

Read the file first. The current anti-pattern is: catch block sets `results = []` and `isOpen = false`, making search failure look like "no results found".

Fix by:
1. Add a state variable: `const [searchError, setSearchError] = useState<string | null>(null)`.
2. In the fetch/search function:
   - At the start: `setSearchError(null)`.
   - In the catch block: instead of setting results to [] and closing, set `setSearchError('Search failed. Please try again.')` and keep `isOpen` true so the user sees the error.
   - Do NOT set results to [] on error — leave previous results or set to null to distinguish from empty.
3. In the search results dropdown JSX, add an error state check BEFORE the empty results check:
   ```tsx
   {searchError ? (
     <div className="p-3 text-sm text-red-400">{searchError}</div>
   ) : results.length === 0 ? (
     // existing "no results" UI
   ) : (
     // existing results rendering
   )}
   ```

**SearchPageClient** (`src/app/(dashboard)/admin/knowledge/search/SearchPageClient.tsx`):

Read the file first. The current anti-pattern is: catch block sets `results = []`, making search failure look like "No results for X".

Fix by:
1. Add a state variable: `const [searchError, setSearchError] = useState<string | null>(null)`.
2. In the fetch/search function:
   - At the start: `setSearchError(null)`.
   - In the catch block: `setSearchError('Failed to search knowledge base. Please try again.')`. Keep console.error for debugging. Do NOT set results to [].
3. In the JSX, add an error state check BEFORE the "no results" check:
   ```tsx
   {searchError ? (
     <div className="rounded-md bg-red-500/10 border border-red-500/30 p-4">
       <p className="text-sm text-red-400">{searchError}</p>
       <button onClick={handleSearch} className="mt-2 text-xs text-blue-400 hover:text-blue-300">
         Try again
       </button>
     </div>
   ) : results.length === 0 && query ? (
     // existing "no results for X" UI
   ) : (
     // existing results rendering
   )}
   ```
   Adjust `handleSearch` to match the actual search function name.

For both: The key distinction is that errors are NOT the same as "no results". An error means the search system is broken; no results means the search worked but nothing matched.
  </action>
  <verify>
Run: `grep "searchError" src/components/search/SearchBar.tsx` — should match.
Run: `grep "searchError" src/app/\(dashboard\)/admin/knowledge/search/SearchPageClient.tsx` — should match.
Run: `grep "Search failed" src/components/search/SearchBar.tsx` — should match (specific error message).
Run: `grep "Failed to search" src/app/\(dashboard\)/admin/knowledge/search/SearchPageClient.tsx` — should match (specific error message).
  </verify>
  <done>SearchBar shows "Search failed" in the dropdown when fetch errors occur instead of silently closing. SearchPageClient shows "Failed to search knowledge base" with retry instead of "No results for X" when the API fails.</done>
</task>

</tasks>

<verification>
1. `grep -l "ErrorAlert" src/app/\(dashboard\)/admin/analytics/AnalyticsDashboard.tsx src/components/admin/StudentList.tsx` — both files import ErrorAlert
2. `grep -l "searchError" src/components/search/SearchBar.tsx src/app/\(dashboard\)/admin/knowledge/search/SearchPageClient.tsx` — both files track error state separately from results
3. `grep "onRetry\|Try again\|handleSearch" src/app/\(dashboard\)/admin/analytics/AnalyticsDashboard.tsx src/components/admin/StudentList.tsx src/components/search/SearchBar.tsx src/app/\(dashboard\)/admin/knowledge/search/SearchPageClient.tsx` — all 4 files have retry mechanisms
4. `npm run build 2>&1 | tail -5` — build succeeds (no TypeScript errors from modifications)
</verification>

<success_criteria>
- AnalyticsDashboard: Failed API calls produce a visible error banner listing which sections failed, with retry
- StudentList (admin): Fetch failure shows error UI with retry button instead of silent console.error
- SearchBar: Search failure shows "Search failed" error in dropdown instead of closing the dropdown
- SearchPageClient: Search failure shows error message with retry instead of "No results for X"
- No TypeScript compilation errors in modified files
</success_criteria>

<output>
After completion, create `.planning/phases/26-error-handling-resilience/26-02-SUMMARY.md`
</output>
