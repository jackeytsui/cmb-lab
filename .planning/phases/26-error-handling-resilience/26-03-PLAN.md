---
phase: 26-error-handling-resilience
plan: 03
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - src/components/tags/TagManager.tsx
  - src/app/(dashboard)/coach/students/StudentListWithTags.tsx
  - src/components/admin/VideoLibrary.tsx
  - src/components/admin/AILogList.tsx
  - src/components/admin/ContentList.tsx
  - src/components/coach/SubmissionQueue.tsx
autonomous: true

must_haves:
  truths:
    - "Admin can tell when tag operations fail and sees a specific message explaining what went wrong"
    - "Coach can tell when the student list failed to load and can retry without refreshing the page"
    - "Admin can distinguish between 'no data yet' and 'failed to load' in video library, AI logs, and content list"
    - "Coach can retry a failed submission queue load without refreshing the page"
  artifacts:
    - path: "src/components/tags/TagManager.tsx"
      provides: "Tag manager with visible error states on CRUD operations"
      contains: "error"
    - path: "src/app/(dashboard)/coach/students/StudentListWithTags.tsx"
      provides: "Coach student list with error state and retry"
      contains: "error"
    - path: "src/components/admin/VideoLibrary.tsx"
      provides: "Video library with error state and retry"
      contains: "error"
    - path: "src/components/admin/AILogList.tsx"
      provides: "AI log list with error state and retry"
      contains: "error"
    - path: "src/components/admin/ContentList.tsx"
      provides: "Content list with error state and retry"
      contains: "error"
    - path: "src/components/coach/SubmissionQueue.tsx"
      provides: "Submission queue with retry button added to existing error state"
      contains: "retry\|Try again\|onRetry\|refetch\|fetchSubmissions"
  key_links:
    - from: "src/components/tags/TagManager.tsx"
      to: "src/components/ui/error-alert.tsx"
      via: "import { ErrorAlert }"
      pattern: "import.*ErrorAlert.*from.*error-alert"
    - from: "src/app/(dashboard)/coach/students/StudentListWithTags.tsx"
      to: "src/components/ui/error-alert.tsx"
      via: "import { ErrorAlert }"
      pattern: "import.*ErrorAlert.*from.*error-alert"
    - from: "src/components/admin/VideoLibrary.tsx"
      to: "src/components/ui/error-alert.tsx"
      via: "import { ErrorAlert }"
      pattern: "import.*ErrorAlert.*from.*error-alert"
    - from: "src/components/admin/AILogList.tsx"
      to: "src/components/ui/error-alert.tsx"
      via: "import { ErrorAlert }"
      pattern: "import.*ErrorAlert.*from.*error-alert"
    - from: "src/components/admin/ContentList.tsx"
      to: "src/components/ui/error-alert.tsx"
      via: "import { ErrorAlert }"
      pattern: "import.*ErrorAlert.*from.*error-alert"
    - from: "src/components/coach/SubmissionQueue.tsx"
      to: "fetch function"
      via: "retry button onClick calls fetch/refetch function"
      pattern: "Try again|onRetry|retry"
---

<objective>
Fix the remaining MEDIUM severity components and add a retry button to SubmissionQueue. These are admin and coach components that either silently swallow fetch errors or need audit to confirm their error handling state.

Purpose: After Plan 02 fixes the worst offenders (HIGH + critical MEDIUM), this plan handles the next tier: admin listing components and coach components that are likely missing error states based on research audit. This completes the Phase 26 requirement that every API-consuming component has a visible error state.

Output: 6 modified files, each with visible error states using the ErrorAlert component from Plan 01.
</objective>

<execution_context>
@/home/sheldon/.claude/get-shit-done/workflows/execute-plan.md
@/home/sheldon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-error-handling-resilience/26-RESEARCH.md
@.planning/phases/26-error-handling-resilience/26-01-SUMMARY.md
@src/components/tags/TagManager.tsx
@src/app/(dashboard)/coach/students/StudentListWithTags.tsx
@src/components/admin/VideoLibrary.tsx
@src/components/admin/AILogList.tsx
@src/components/admin/ContentList.tsx
@src/components/coach/SubmissionQueue.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix TagManager, StudentListWithTags, and SubmissionQueue (coach/tag components)</name>
  <files>src/components/tags/TagManager.tsx, src/app/(dashboard)/coach/students/StudentListWithTags.tsx, src/components/coach/SubmissionQueue.tsx</files>
  <action>
These three components need error handling fixes. Research confirmed: TagManager has CRUD operations with console.error-only catches, StudentListWithTags has fetch with console.error-only catch, and SubmissionQueue has error display but no retry button.

**TagManager** (`src/components/tags/TagManager.tsx`):
1. Add `import { ErrorAlert } from '@/components/ui/error-alert'` at the top.
2. Add error state: `const [fetchError, setFetchError] = useState<string | null>(null)`.
3. In the list fetch function: add `setFetchError(null)` at start. In the catch block: add `setFetchError('Failed to load tags. Please try again.')` (keep any existing console.error).
4. For CRUD operation catch blocks that only have console.error: add a visible error state. For CRUD catch blocks that already show inline errors or alerts, leave them as-is.
5. In JSX, render `{fetchError && <ErrorAlert message={fetchError} onRetry={() => fetchTags()} />}` before the tag list. Adjust function name to match actual code.

**StudentListWithTags** (`src/app/(dashboard)/coach/students/StudentListWithTags.tsx`):
1. Add `import { ErrorAlert } from '@/components/ui/error-alert'`.
2. Add error state: `const [error, setError] = useState<string | null>(null)`.
3. In fetch function: add `setError(null)` at start. In catch block: add `setError('Failed to load students. Please try again.')` (keep any existing console.error).
4. In JSX, render `{error && <ErrorAlert message={error} onRetry={() => fetchStudents()} variant="block" />}` before the student list. When error is present and list is empty, do not render the list. Adjust function name to match actual code.

**SubmissionQueue** (`src/components/coach/SubmissionQueue.tsx`):
This component already has error state display but no retry button. Add one:
1. Find the existing error rendering block (the `{error && <div>...error text...</div>}` pattern).
2. Replace the existing error div with `<ErrorAlert message={error} onRetry={() => fetchSubmissions()} />` for consistency with the rest of Phase 26. Adjust the function name to match the actual fetch/refetch function in the component.
3. Add `import { ErrorAlert } from '@/components/ui/error-alert'` at the top.
  </action>
  <verify>
Run: `grep "ErrorAlert\|error" src/components/tags/TagManager.tsx | head -5` — should show ErrorAlert import or error state.
Run: `grep "ErrorAlert\|error" src/app/\(dashboard\)/coach/students/StudentListWithTags.tsx | head -5` — should show error handling.
Run: `grep "Try again\|onRetry\|retry" src/components/coach/SubmissionQueue.tsx` — should match (retry button added).
  </verify>
  <done>TagManager shows error messages when tag operations fail. StudentListWithTags shows error with retry when student fetch fails. SubmissionQueue has a retry button on its existing error state.</done>
</task>

<task type="auto">
  <name>Task 2: Fix VideoLibrary, AILogList, and ContentList (admin listing components)</name>
  <files>src/components/admin/VideoLibrary.tsx, src/components/admin/AILogList.tsx, src/components/admin/ContentList.tsx</files>
  <action>
These three admin listing components all need ErrorAlert import, error state, and visible error rendering. Research confirmed they have catch blocks with console.error only (or no catch at all) and no visible error UI.

**VideoLibrary** (`src/components/admin/VideoLibrary.tsx`):
1. Add `import { ErrorAlert } from '@/components/ui/error-alert'` at the top.
2. Add error state: `const [error, setError] = useState<string | null>(null)`.
3. In the data fetch function: add `setError(null)` at start. In the catch block: add `setError('Failed to load video library. Please try again.')` (keep any existing console.error).
4. If there is an `if (!response.ok)` check that only logs or does nothing, add `throw new Error('Failed to fetch')` to flow into the catch block.
5. In JSX, render `{error && <ErrorAlert message={error} onRetry={() => fetchVideos()} />}` BEFORE the video list/table. Adjust function name to match actual code. When error is present and data array is empty, do not render the empty table.

**AILogList** (`src/components/admin/AILogList.tsx`):
1. Add `import { ErrorAlert } from '@/components/ui/error-alert'` at the top.
2. Add error state: `const [error, setError] = useState<string | null>(null)`.
3. In the data fetch function: add `setError(null)` at start. In the catch block: add `setError('Failed to load AI logs. Please try again.')` (keep any existing console.error).
4. If there is an `if (!response.ok)` check that only logs or does nothing, add `throw new Error('Failed to fetch')` to flow into the catch block.
5. In JSX, render `{error && <ErrorAlert message={error} onRetry={() => fetchLogs()} />}` BEFORE the log list/table. Adjust function name to match actual code. When error is present and data array is empty, do not render the empty table.

**ContentList** (`src/components/admin/ContentList.tsx`):
1. Add `import { ErrorAlert } from '@/components/ui/error-alert'` at the top.
2. Add error state: `const [error, setError] = useState<string | null>(null)`.
3. In the data fetch function: add `setError(null)` at start. In the catch block: add `setError('Failed to load content. Please try again.')` (keep any existing console.error).
4. If there is an `if (!response.ok)` check that only logs or does nothing, add `throw new Error('Failed to fetch')` to flow into the catch block.
5. In JSX, render `{error && <ErrorAlert message={error} onRetry={() => fetchContent()} />}` BEFORE the content list/table. Adjust function name to match actual code. When error is present and data array is empty, do not render the empty table.
  </action>
  <verify>
Run: `grep "ErrorAlert" src/components/admin/VideoLibrary.tsx` — should match.
Run: `grep "ErrorAlert" src/components/admin/AILogList.tsx` — should match.
Run: `grep "ErrorAlert" src/components/admin/ContentList.tsx` — should match.
Run: `grep "setError" src/components/admin/VideoLibrary.tsx src/components/admin/AILogList.tsx src/components/admin/ContentList.tsx` — all three should match.
Run: `grep "onRetry" src/components/admin/VideoLibrary.tsx src/components/admin/AILogList.tsx src/components/admin/ContentList.tsx` — all three should have retry.
  </verify>
  <done>VideoLibrary, AILogList, and ContentList all show visible error states with retry buttons when their data fetches fail, instead of silently swallowing errors or showing nothing.</done>
</task>

</tasks>

<verification>
1. `grep -l "ErrorAlert\|error" src/components/tags/TagManager.tsx src/app/\(dashboard\)/coach/students/StudentListWithTags.tsx src/components/coach/SubmissionQueue.tsx src/components/admin/VideoLibrary.tsx src/components/admin/AILogList.tsx src/components/admin/ContentList.tsx` — all 6 files have error handling
2. `grep -c "onRetry\|Try again\|retry" src/components/coach/SubmissionQueue.tsx` — at least 1 (retry button exists)
3. `grep -c "ErrorAlert" src/components/admin/VideoLibrary.tsx src/components/admin/AILogList.tsx src/components/admin/ContentList.tsx` — each has at least 1 reference
4. `npm run build 2>&1 | tail -5` — build succeeds (no TypeScript errors from modifications)
</verification>

<success_criteria>
- TagManager: Tag CRUD failures produce visible error messages
- StudentListWithTags: Fetch failure shows error with retry
- SubmissionQueue: Existing error state now has a retry button
- VideoLibrary: Fetch failure shows error with retry
- AILogList: Fetch failure shows error with retry
- ContentList: Fetch failure shows error with retry
- No TypeScript compilation errors in modified files
</success_criteria>

<output>
After completion, create `.planning/phases/26-error-handling-resilience/26-03-SUMMARY.md`
</output>
