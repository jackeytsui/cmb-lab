---
phase: 68-analytics-student-facing-display
plan: 02
type: execute
wave: 2
depends_on: ["68-01"]
files_modified:
  - src/lib/role-analytics.ts
  - src/components/admin/StudentAccessAttribution.tsx
  - src/app/(dashboard)/admin/students/[studentId]/page.tsx
  - src/components/settings/MyRolesSection.tsx
  - src/app/(dashboard)/settings/page.tsx
autonomous: true

must_haves:
  truths:
    - "Coach can see which specific role grants access to each course or feature for any student"
    - "Student's settings page shows all assigned roles with expiration dates and a summary of what each role grants"
  artifacts:
    - path: "src/lib/role-analytics.ts"
      provides: "getAccessAttribution and getStudentRoleView functions added"
      exports: ["getAccessAttribution", "getStudentRoleView"]
    - path: "src/components/admin/StudentAccessAttribution.tsx"
      provides: "Client component showing per-role access breakdown for a student"
    - path: "src/components/settings/MyRolesSection.tsx"
      provides: "Read-only component showing the current student their roles and granted access"
  key_links:
    - from: "src/app/(dashboard)/admin/students/[studentId]/page.tsx"
      to: "src/components/admin/StudentAccessAttribution.tsx"
      via: "import and render below Role Assignments section"
      pattern: "StudentAccessAttribution"
    - from: "src/app/(dashboard)/settings/page.tsx"
      to: "src/lib/role-analytics.ts"
      via: "server-side call to getStudentRoleView(user.id)"
      pattern: "getStudentRoleView"
    - from: "src/app/(dashboard)/settings/page.tsx"
      to: "src/components/settings/MyRolesSection.tsx"
      via: "passes role data as props"
      pattern: "MyRolesSection"
---

<objective>
Add per-student access attribution view for coaches on the student detail page, and a student-facing "My Roles & Access" section on the settings page.

Purpose: Coaches need to debug which role grants what (attribution), and students need transparency about what roles they have and what each role enables.
Output: StudentAccessAttribution component on admin student detail page + MyRolesSection on settings page
</objective>

<execution_context>
@/Users/sheldonho/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sheldonho/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-analytics-student-facing-display/68-RESEARCH.md
@.planning/phases/68-analytics-student-facing-display/68-01-SUMMARY.md

Key source files to reference:
@src/lib/role-analytics.ts (created in 68-01 -- add attribution functions here)
@src/lib/permissions.ts (FEATURE_KEYS for display labels)
@src/db/schema/roles.ts (schema for roles, roleCourses, roleFeatures, userRoles)
@src/app/(dashboard)/admin/students/[studentId]/page.tsx (student detail page -- add attribution section)
@src/components/admin/StudentRoleAssignment.tsx (existing component pattern on student detail page)
@src/app/(dashboard)/settings/page.tsx (settings page -- add MyRolesSection)
@src/components/settings/SettingsForm.tsx (existing settings component pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add attribution queries and build StudentAccessAttribution component</name>
  <files>src/lib/role-analytics.ts, src/components/admin/StudentAccessAttribution.tsx, src/app/(dashboard)/admin/students/[studentId]/page.tsx</files>
  <action>
**Add to `src/lib/role-analytics.ts`:**

1. `getAccessAttribution(userId: string)` -- Per Research Pattern 4:
   - Query active role assignments for the user: userRoles JOIN roles WHERE eq(userId), isNull(roles.deletedAt), active (non-expired).
   - Get roleIds, then batch-fetch roleCourses JOIN courses (for courseTitle) and roleFeatures in Promise.all using inArray(roleId, roleIds).
   - Return per-role breakdown: `[{ roleId, roleName, roleColor, allCourses, expiresAt, courses: [{ courseId, courseTitle, moduleId, lessonId, accessTier }], features: string[] }]`.
   - If `allCourses` is true for a role, include a flag/indicator rather than enumerating courses (per Research Q4 recommendation).

2. `FEATURE_LABELS` -- A Record mapping feature keys to human-readable labels:
   `{ ai_conversation: "AI Conversation", practice_sets: "Practice Sets", dictionary_reader: "Dictionary Reader", listening_lab: "Listening Lab", video_threads: "Video Threads", certificates: "Certificates", ai_chat: "AI Chat" }`. Export it for use in both admin and student-facing components.

**Create `src/components/admin/StudentAccessAttribution.tsx`:**

A `"use client"` component that receives `studentId` prop. On mount, fetches from a new API endpoint or directly calls a route. Since this is admin-side and needs to call server function, create a lightweight API approach: fetch `/api/admin/students/[studentId]/roles?detail=attribution`.

Actually, to keep it simpler and avoid modifying the existing roles route, add a query parameter approach: the existing GET endpoint at `/api/admin/students/[studentId]/roles` already returns assigned and available roles. Extend it to accept `?include=attribution` and return attribution data alongside. OR, for cleaner separation, have the component fetch `/api/admin/roles/analytics?studentId={studentId}` which returns attribution for that specific student.

**Recommended approach:** Fetch `/api/admin/roles/analytics?studentId={studentId}`. Modify the API route from 68-01 to handle an optional `studentId` query param -- when present, call `getAccessAttribution(studentId)` and return `{ attribution: [...] }` instead of the dashboard analytics. This keeps all analytics queries behind one endpoint.

**Component layout:**
- Section header: "Access Attribution" with Eye icon
- If no roles assigned, show empty state: "No roles assigned to this student"
- For each role, a collapsible card showing:
  - Role name with colored badge, expiration date if set (formatted with date-fns)
  - If `allCourses`: "All Courses" indicator with globe icon
  - Course grants: list of course titles with access tier badges (full, module, lesson)
  - Feature grants: row of feature labels using FEATURE_LABELS mapping
- Skeleton loading state while fetching

**Modify `src/app/(dashboard)/admin/students/[studentId]/page.tsx`:**
- Import `StudentAccessAttribution` from `@/components/admin/StudentAccessAttribution`
- Add new section between "Role Assignments" and "Activity Timeline" sections:
  ```
  <section className="mb-8">
    <h2 className="text-xl font-semibold mb-4 flex items-center gap-2">
      <Eye className="w-5 h-5 text-cyan-400" />
      Access Attribution
    </h2>
    <div className="bg-zinc-900/50 border border-zinc-800 rounded-lg p-6">
      <StudentAccessAttribution studentId={studentId} />
    </div>
  </section>
  ```
- Eye icon is already imported in the file.
  </action>
  <verify>
Run `npx tsc --noEmit` -- zero type errors. Navigate to /admin/students/[studentId] and verify the Access Attribution section appears between Role Assignments and Activity Timeline. Data loads showing per-role breakdown.
  </verify>
  <done>
Coach can view access attribution for any student on the student detail page, seeing exactly which role grants access to which courses and features. Each role shows its course grants and feature flags with human-readable labels.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create student-facing MyRolesSection on settings page</name>
  <files>src/lib/role-analytics.ts, src/components/settings/MyRolesSection.tsx, src/app/(dashboard)/settings/page.tsx</files>
  <action>
**Add to `src/lib/role-analytics.ts`:**

`getStudentRoleView(userId: string)` -- Per Research Pattern 5:
- Query active role assignments: userRoles JOIN roles WHERE eq(userId), isNull(roles.deletedAt), active (non-expired).
- Batch-fetch roleCourses JOIN courses (for courseTitle only -- no admin-sensitive data) and roleFeatures using inArray.
- Return safe data shape (no assignedBy, no student counts, no other students' data -- Research Pitfall 3):
  `[{ name, color, description, expiresAt, courses: [{ courseTitle, accessTier }], features: string[] }]`
- This is nearly identical to getAccessAttribution but strips admin fields (roleId, courseId, moduleId, lessonId).

**Create `src/components/settings/MyRolesSection.tsx`:**

A client component (not `"use client"` -- receive data as props from server component) that displays the student's assigned roles.

Props: `roles: Array<{ name, color, description, expiresAt, courses: Array<{ courseTitle, accessTier }>, features: string[] }>`

Layout:
- Section header: "My Roles & Access" with ShieldCheck icon
- If no roles: show empty state card: "You don't have any roles assigned yet. Contact your coach for access."
- For each role, a card showing:
  - Role name with colored dot/badge (using inline style `backgroundColor: role.color`)
  - Description text if present (text-zinc-400)
  - Expiration: if expiresAt is set, show "Expires {formatted date}" or "Expires in {X days}" using formatDistanceToNow. If expiring within 7 days, show amber warning color. If no expiration, show "No expiration" in green.
  - "Courses" subsection: list of course titles. If role grants allCourses (would be indicated by empty courses array + some flag -- check what getStudentRoleView returns), show "All Courses". Otherwise list each course title.
  - "Features" subsection: row of feature labels using FEATURE_LABELS, styled as small badges.

**Modify `src/app/(dashboard)/settings/page.tsx`:**

This is a Server Component. Add the server-side data fetch:
```typescript
import { getStudentRoleView } from "@/lib/role-analytics";
import { MyRolesSection } from "@/components/settings/MyRolesSection";

// Inside the SettingsPage function, after getCurrentUser():
const userRoleData = await getStudentRoleView(user.id);
```

Add `<MyRolesSection roles={userRoleData} />` ABOVE the SettingsForm in the JSX, within the same container div. This puts the roles section before language/daily goal settings, giving it prominence.

Note: `role-analytics.ts` has `import "server-only"` so it can only be called from Server Components -- this is correct since settings/page.tsx is a Server Component.
  </action>
  <verify>
Run `npx tsc --noEmit` -- zero type errors. Run `npm run build` -- build passes. Navigate to /settings as a student user and verify the "My Roles & Access" section appears above the settings form showing assigned roles with descriptions, expiration dates, course access, and feature labels.
  </verify>
  <done>
Student's settings page shows all assigned roles with expiration dates and a summary of what each role grants (courses and features). No admin-sensitive data is exposed. Empty state shown for students without roles.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `npm run build` succeeds
3. /admin/students/[id] page shows "Access Attribution" section with per-role course/feature breakdown
4. /settings page shows "My Roles & Access" section with the student's own roles, expirations, courses, and features
5. Student-facing view does NOT expose assignedBy, student counts, or other students' data
6. All feature keys display human-readable labels (not raw keys like "ai_conversation")
</verification>

<success_criteria>
- ANALYTICS-05: Coach can see access attribution (which role granted specific course/feature access to student) on the student detail page
- ANALYTICS-06: Student profile (settings page) shows all assigned roles with expiration dates and granted permissions
</success_criteria>

<output>
After completion, create `.planning/phases/68-analytics-student-facing-display/68-02-SUMMARY.md`
</output>
