---
phase: 39-xp-streak-engine
plan: 05
type: execute
wave: 3
depends_on: ["39-03", "39-04"]
files_modified:
  - src/app/(dashboard)/dashboard/page.tsx
  - src/components/xp/XPOverview.tsx
autonomous: false

must_haves:
  truths:
    - "Student dashboard page fetches XP data from /api/xp and displays activity rings, level, streak, and daily goal progress"
    - "XP overview section renders all 4 XP components (ActivityRings, LevelBadge, StreakDisplay, DailyGoalProgress) in a cohesive layout"
    - "XP section degrades gracefully if XP data fails to load (shows error state, not broken page)"
    - "Activity rings animate on mount showing Learn, Practice, Speak dimensions"
  artifacts:
    - path: "src/components/xp/XPOverview.tsx"
      provides: "Composite XP section that fetches data and renders all 4 XP components"
      exports: ["XPOverview"]
    - path: "src/app/(dashboard)/dashboard/page.tsx"
      provides: "Dashboard page with XP overview section"
      contains: "XPOverview"
  key_links:
    - from: "src/components/xp/XPOverview.tsx"
      to: "/api/xp"
      via: "fetch in useEffect"
      pattern: "fetch.*api/xp"
    - from: "src/app/(dashboard)/dashboard/page.tsx"
      to: "src/components/xp/XPOverview.tsx"
      via: "import and render"
      pattern: "XPOverview"
---

<objective>
Create the XPOverview composite component and integrate it into the student dashboard page.

Purpose: This plan wires all XP components together into a single section that fetches data from GET /api/xp and renders the activity rings, level badge, streak display, and daily goal progress in a polished layout on the student dashboard. This is the moment the XP system becomes visible to students.

Output: XP overview section visible on the student dashboard with live data, graceful error handling, and animated components.
</objective>

<execution_context>
@/home/sheldon/.claude/get-shit-done/workflows/execute-plan.md
@/home/sheldon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-xp-streak-engine/39-RESEARCH.md
@.planning/phases/39-xp-streak-engine/39-03-SUMMARY.md
@.planning/phases/39-xp-streak-engine/39-04-SUMMARY.md
@src/app/(dashboard)/dashboard/page.tsx
@src/components/xp/ActivityRings.tsx
@src/components/xp/LevelBadge.tsx
@src/components/xp/StreakDisplay.tsx
@src/components/xp/DailyGoalProgress.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create XPOverview composite component and integrate into dashboard</name>
  <files>src/components/xp/XPOverview.tsx, src/app/(dashboard)/dashboard/page.tsx</files>
  <action>
**XPOverview** (`src/components/xp/XPOverview.tsx`):
- "use client" directive
- Uses `useState` and `useEffect` to fetch from `/api/xp` on mount
- Manages loading/error/data states
- Layout (responsive grid):
  - On desktop (lg+): 2-column grid
    - Left column: ActivityRings (large, centered) with DailyGoalProgress below
    - Right column: LevelBadge on top, StreakDisplay below
  - On mobile: single column stack (ActivityRings -> DailyGoalProgress -> LevelBadge -> StreakDisplay)
- Wrapped in a card-like container with zinc-800/900 bg, rounded-xl, p-6, subtle border
- Section title: "Your Progress" or "XP & Activity" in text-lg font-semibold
- Loading state: Skeleton placeholders matching the component dimensions (use project's Skeleton component from @/components/ui/skeleton with bg-zinc-800)
- Error state: Use ErrorAlert component (from @/components/ui or wherever it's defined) with retry button that re-fetches
- Pass data to child components:
  - ActivityRings: rings array built from daily activity counts + RING_GOALS constants
  - LevelBadge: level info from calculateLevel response
  - StreakDisplay: streak data
  - DailyGoalProgress: daily totalXp, goalXp, goalMet

**Dashboard Integration** (`src/app/(dashboard)/dashboard/page.tsx`):
- Read the existing dashboard page to understand its current structure
- Import XPOverview
- Add XPOverview above or below the existing course grid section
- Do NOT break existing dashboard content — XPOverview is an additive section
- Ensure the XPOverview renders only for students (check user role if the dashboard distinguishes by role)

Styling should match the project's dark theme (zinc-800/900 bg, zinc-100 text, amber/emerald/blue accents).
  </action>
  <verify>Run `npm run build` to verify no type errors. Run `npm run dev` and navigate to the dashboard page to visually check the XP overview renders correctly.</verify>
  <done>XPOverview renders on the student dashboard fetching data from /api/xp. All 4 XP components display with animated rings, level badge, streak counter, and daily goal progress. Error and loading states work correctly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete XP & Streak Engine integrated into the student dashboard. Activity rings animate, level badge shows current level with progress, streak display shows consecutive days and freezes, and daily goal progress shows today's XP vs target.</what-built>
  <how-to-verify>
    1. Open the app at http://localhost:3000/dashboard
    2. Verify the XP overview section appears on the dashboard with all 4 components
    3. Verify activity rings show 3 concentric circles (Learn=emerald, Practice=blue, Speak=amber) with smooth fill animation on page load
    4. Verify level badge shows "Level 1" (or appropriate level) with progress bar
    5. Verify streak display shows current streak count (likely 0 for new data) with freeze indicators
    6. Verify daily goal progress shows "0 / 100 XP" (or current state)
    7. Navigate to Settings page and confirm daily goal tier selector still works
    8. If possible: complete a lesson and reload dashboard to verify XP updates
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `src/components/xp/XPOverview.tsx` exists and exports XPOverview component
2. Dashboard page imports and renders XPOverview
3. XPOverview fetches from `/api/xp` and handles loading/error/success states
4. Activity rings animate with framer-motion on mount
5. Layout is responsive (2-column on desktop, stacked on mobile)
6. Existing dashboard functionality (course grid) is not broken
7. `npm run build` passes cleanly
</verification>

<success_criteria>
The XP & Streak Engine is fully integrated and visible on the student dashboard. Students see animated activity rings, their current level with progress, streak count with freeze indicators, and daily goal progress. The XP system is live and awarding XP for lessons, practice, and voice conversations.
</success_criteria>

<output>
After completion, create `.planning/phases/39-xp-streak-engine/39-05-SUMMARY.md`
</output>
