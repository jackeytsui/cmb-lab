---
phase: 30-foundation-and-fonts
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/interactions/TextInteraction.tsx
  - src/components/audio/AudioInteraction.tsx
  - src/components/chat/ChatMessage.tsx
  - src/components/interactions/FeedbackDisplay.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Chinese text in interaction prompts renders with the PhoneticText font wrapper"
    - "Chinese text in chatbot plain-text segments renders with the PhoneticText font wrapper"
    - "Chinese text in grading feedback and corrections renders with the PhoneticText font wrapper"
    - "Switching language preference changes the font class applied by PhoneticText across all integrated components"
  artifacts:
    - path: "src/components/interactions/TextInteraction.tsx"
      provides: "Interaction prompt wrapped in PhoneticText"
      contains: "PhoneticText"
    - path: "src/components/audio/AudioInteraction.tsx"
      provides: "Audio interaction prompt wrapped in PhoneticText"
      contains: "PhoneticText"
    - path: "src/components/chat/ChatMessage.tsx"
      provides: "Chat plain-text segments wrapped in PhoneticText"
      contains: "PhoneticText"
    - path: "src/components/interactions/FeedbackDisplay.tsx"
      provides: "Feedback text and corrections wrapped in PhoneticText"
      contains: "PhoneticText"
  key_links:
    - from: "src/components/interactions/TextInteraction.tsx"
      to: "src/components/phonetic/PhoneticText.tsx"
      via: "import and usage wrapping prompt text"
      pattern: "import.*PhoneticText"
    - from: "src/components/audio/AudioInteraction.tsx"
      to: "src/components/phonetic/PhoneticText.tsx"
      via: "import and usage wrapping prompt text"
      pattern: "import.*PhoneticText"
    - from: "src/components/chat/ChatMessage.tsx"
      to: "src/components/phonetic/PhoneticText.tsx"
      via: "import and usage wrapping plain text spans"
      pattern: "import.*PhoneticText"
    - from: "src/components/interactions/FeedbackDisplay.tsx"
      to: "src/components/phonetic/PhoneticText.tsx"
      via: "import and usage wrapping feedback text"
      pattern: "import.*PhoneticText"
---

<objective>
Integrate PhoneticText wrapper into all components that display Chinese text, closing the gap between font infrastructure (built in 30-01) and visible font application.

Purpose: Phase 30 verification found that PhoneticText has zero usage across the codebase. Truths 2, 3, and 4 failed because no Chinese text is actually wrapped with the component. This plan integrates PhoneticText into the 4 key Chinese text display points: interaction prompts, chatbot messages, and grading feedback.

Output: PhoneticText component imported and used in TextInteraction, AudioInteraction, ChatMessage, and FeedbackDisplay -- making the font class (font-hanzi-pinyin or font-cantonese-visual) applied to Chinese text based on user preference.
</objective>

<execution_context>
@/home/sheldon/.claude/get-shit-done/workflows/execute-plan.md
@/home/sheldon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-foundation-and-fonts/30-01-SUMMARY.md
@.planning/phases/30-foundation-and-fonts/30-VERIFICATION.md

Key existing files:
@src/components/phonetic/PhoneticText.tsx
@src/components/interactions/TextInteraction.tsx
@src/components/audio/AudioInteraction.tsx
@src/components/chat/ChatMessage.tsx
@src/components/interactions/FeedbackDisplay.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wrap interaction prompts with PhoneticText</name>
  <files>
    src/components/interactions/TextInteraction.tsx
    src/components/audio/AudioInteraction.tsx
  </files>
  <action>
    Import PhoneticText from "@/components/phonetic/PhoneticText" in both files.

    In TextInteraction.tsx (line 112), change the prompt display from:
    ```tsx
    <div className="text-xl text-white font-medium">{prompt}</div>
    ```
    to:
    ```tsx
    <div className="text-xl text-white font-medium">
      <PhoneticText>{prompt}</PhoneticText>
    </div>
    ```

    In AudioInteraction.tsx (line 166), apply the same change to the prompt div:
    ```tsx
    <div className="text-xl text-white font-medium">
      <PhoneticText>{prompt}</PhoneticText>
    </div>
    ```

    Both components are already "use client" so PhoneticText (which uses hooks) is compatible.

    DO NOT wrap the entire component or container -- only wrap the Chinese text content ({prompt}). The outer div retains its styling classes.

    DO NOT modify SubtitleOverlay -- it has its own ruby annotation system with per-character pinyin/jyutping data. PhoneticText would conflict with that approach.
  </action>
  <verify>
    Run `npx tsc --noEmit` to confirm no type errors.
    Grep for "PhoneticText" in both files to confirm import and usage.
  </verify>
  <done>
    TextInteraction and AudioInteraction prompt text is wrapped in PhoneticText, applying the correct phonetic font class based on user language preference.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wrap chatbot plain-text and feedback text with PhoneticText</name>
  <files>
    src/components/chat/ChatMessage.tsx
    src/components/interactions/FeedbackDisplay.tsx
  </files>
  <action>
    **ChatMessage.tsx:**

    Import PhoneticText from "@/components/phonetic/PhoneticText".

    In the `renderAnnotatedText` function (line 57-66), wrap the plain text spans with PhoneticText. Currently:
    ```tsx
    if (segment.type === 'annotation') {
      return <ChineseAnnotation key={i} text={segment.content} />;
    }
    return <span key={i}>{segment.content}</span>;
    ```
    Change the plain text case to:
    ```tsx
    return <PhoneticText key={i}>{segment.content}</PhoneticText>;
    ```

    This wraps ALL plain text (including English) with PhoneticText. The phonetic font only affects CJK Unicode ranges, so English text renders normally with the font's Latin fallback. The annotation segments (ChineseAnnotation) already have their own ruby markup and should NOT be wrapped.

    **FeedbackDisplay.tsx:**

    Import PhoneticText from "@/components/phonetic/PhoneticText".

    Wrap the main feedback message (line 37):
    ```tsx
    <p className="text-white font-medium"><PhoneticText>{feedback.feedback}</PhoneticText></p>
    ```

    Wrap each correction list item (line 58):
    ```tsx
    <li key={index}><PhoneticText>{correction}</PhoneticText></li>
    ```

    Wrap the hint text (line 71):
    ```tsx
    <p><PhoneticText>{feedback.hints[0]}</PhoneticText></p>
    ```

    FeedbackDisplay is already "use client" so hook usage within PhoneticText is compatible.

    DO NOT wrap score badges, labels, or structural text ("Corrections:", "Hint:") -- only wrap content that may contain Chinese characters.
  </action>
  <verify>
    Run `npx tsc --noEmit` to confirm no type errors.
    Grep for "PhoneticText" across the entire src/ directory to confirm at least 5 files import it (PhoneticText.tsx itself + 4 consumers).
  </verify>
  <done>
    ChatMessage plain-text segments and FeedbackDisplay content are wrapped in PhoneticText. Combined with Task 1, all major Chinese text display points now use PhoneticText, satisfying verification truths 2, 3, and 4 (pending actual font files for visible annotations).
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. Grep `PhoneticText` across src/ shows imports in at least 5 files:
   - src/components/phonetic/PhoneticText.tsx (definition)
   - src/components/interactions/TextInteraction.tsx (consumer)
   - src/components/audio/AudioInteraction.tsx (consumer)
   - src/components/chat/ChatMessage.tsx (consumer)
   - src/components/interactions/FeedbackDisplay.tsx (consumer)
3. No PhoneticText usage on SubtitleOverlay (it has its own annotation system)
4. All consumer files are "use client" components (compatible with PhoneticText's hook usage)
</verification>

<success_criteria>
- PhoneticText imported and used in 4 consumer components
- Interaction prompts (text and audio) display Chinese text with phonetic font class
- Chatbot plain-text segments display with phonetic font class
- Grading feedback, corrections, and hints display with phonetic font class
- TypeScript compilation passes
- No runtime errors from hook usage in client components
</success_criteria>

<output>
After completion, create `.planning/phases/30-foundation-and-fonts/30-03-SUMMARY.md`
</output>
