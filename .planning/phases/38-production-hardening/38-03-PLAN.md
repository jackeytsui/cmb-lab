---
phase: 38-production-hardening
plan: 03
type: execute
wave: 2
depends_on: ["38-01"]
files_modified:
  - src/app/(dashboard)/practice/[setId]/page.tsx
  - src/lib/assignments.ts
  - src/app/api/coach/submissions/route.ts
  - src/app/(dashboard)/dashboard/page.tsx
  - src/app/(dashboard)/coach/pronunciation/page.tsx
  - src/app/(dashboard)/admin/exercises/page.tsx
  - src/app/(dashboard)/admin/exercises/[exerciseId]/page.tsx
  - src/app/(dashboard)/admin/exercises/new/page.tsx
  - src/components/layout/AppSidebar.tsx
  - src/components/practice/builder/useBuilderState.ts
  - src/components/video/VideoPreviewPlayer.tsx
  - src/app/(dashboard)/settings/page.tsx
  - src/components/notifications/useNotifications.ts
  - src/components/pwa/usePWAInstall.ts
  - src/components/video/useSubtitlePreference.ts
  - src/components/chat/ChatWidget.tsx
  - src/components/search/SearchBar.tsx
autonomous: true

must_haves:
  truths:
    - "Practice results page loads practice set with exercises in a single query using Drizzle `with` clause"
    - "getStudentAssignments parallelizes independent queries with Promise.all"
    - "`npx eslint src/` reports zero errors (warnings acceptable)"
  artifacts:
    - path: "src/app/(dashboard)/practice/[setId]/page.tsx"
      provides: "Optimized single-query practice set loading"
      contains: "with:"
    - path: "src/lib/assignments.ts"
      provides: "Parallelized assignment queries"
      contains: "Promise.all"
  key_links:
    - from: "src/app/(dashboard)/practice/[setId]/page.tsx"
      to: "db.query.practiceSets"
      via: "Drizzle with clause"
      pattern: "with.*exercises"
---

<objective>
Eliminate N+1 queries on key pages and resolve all ESLint errors.

Purpose: Sequential queries slow page loads as data grows. The practice results page makes 3 sequential queries and the getStudentAssignments function makes 4-5 sequential queries. These can be parallelized or consolidated. Additionally, 28 ESLint errors need to be fixed or suppressed to keep the codebase clean.

Output: Optimized queries on practice results and assignments pages, zero ESLint errors.
</objective>

<execution_context>
@/home/sheldon/.claude/get-shit-done/workflows/execute-plan.md
@/home/sheldon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-production-hardening/38-RESEARCH.md
@src/app/(dashboard)/practice/[setId]/page.tsx
@src/lib/assignments.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Optimize N+1 queries on practice results and assignments</name>
  <files>src/app/(dashboard)/practice/[setId]/page.tsx, src/lib/assignments.ts</files>
  <action>
  **Part A — Practice results page (`src/app/(dashboard)/practice/[setId]/page.tsx`):**

  The page currently makes sequential calls to `getPracticeSet`, `listExercises`, and `getCurrentUser` (or similar). Consolidate the first two into a single Drizzle query:

  ```typescript
  const practiceSet = await db.query.practiceSets.findFirst({
    where: eq(practiceSets.id, setId),
    with: {
      exercises: {
        orderBy: (exercises, { asc }) => [asc(exercises.orderIndex)],
      },
    },
  });
  ```

  Then use `Promise.all` for any remaining independent queries (e.g., user fetch and practice set fetch if they're separate):
  ```typescript
  const [practiceSet, user] = await Promise.all([
    db.query.practiceSets.findFirst({ where: ..., with: { exercises: true } }),
    getCurrentUser(),
  ]);
  ```

  Read the file first to understand the exact current query pattern, then refactor accordingly.

  **Part B — getStudentAssignments (`src/lib/assignments.ts`):**

  This function currently makes 4-5 sequential queries (course-level assignments, module-level, lesson-level, free assignments, then possibly attempts). Wrap independent queries in `Promise.all`:

  ```typescript
  const [courseAssignments, moduleAssignments, lessonAssignments, freeAssignments] = await Promise.all([
    // course-level query
    // module-level query
    // lesson-level query
    // free assignments query
  ]);
  ```

  Read the file first to understand the exact query structure. Only parallelize queries that don't depend on each other's results.

  **Part C — Coach submission API audit:**

  Read `src/app/api/coach/submissions/route.ts` and check for N+1 patterns (e.g., looping over submissions and fetching user data per submission). If found, refactor to use a single query with joins or `with` clause. If already optimized, skip.
  </action>
  <verify>Read the modified files to confirm `with:` clause is used for practice set + exercises, and `Promise.all` wraps independent queries in assignments.ts. Run `npm run build` to confirm no TypeScript errors were introduced.</verify>
  <done>Practice results page loads set + exercises in one query. Assignment resolution uses Promise.all for parallel queries. No N+1 patterns remain in audited pages.</done>
</task>

<task type="auto">
  <name>Task 2: Fix all ESLint errors (28 errors across the codebase)</name>
  <files>Multiple files across src/</files>
  <action>
  Run `npx eslint src/` to get the current error list. Then fix each category:

  **Category 1: `react-hooks/error-boundaries` (8 errors) — SUPPRESS**
  These are false positives on server components that use try/catch around data fetching. Add `// eslint-disable-next-line react-hooks/error-boundaries` above each flagged line. These are in server component pages (admin, coach, student pages) where try/catch wraps async data fetching, not React rendering.

  **Category 2: `react-hooks/refs` (6 errors) — FIX**
  These are in `useBuilderState.ts`, `VideoPreviewPlayer.tsx`, and `test-interactive/page.tsx`. The issue is accessing `.current` on a ref during render. Move ref access into `useEffect`, `useCallback`, or event handlers instead of the render path.

  **Category 3: `react-hooks/set-state-in-effect` (4 errors) — SUPPRESS**
  These are in `useNotifications.ts`, `usePWAInstall.ts`, `useSubtitlePreference.ts`, `ChatWidget.tsx`. They are loading-from-localStorage / initial-fetch patterns that set state synchronously inside useEffect. This is intentional for hydration safety. Add `// eslint-disable-next-line react-hooks/set-state-in-effect` above each flagged line.

  **Category 4: `@typescript-eslint/no-explicit-any` (7 errors) — FIX**
  Replace `any` with proper types in exercise form components and pronunciation page. Common fixes:
  - `Record<string, unknown>` for generic objects
  - Specific types from the schema for known data shapes
  - `unknown` with type narrowing for truly dynamic data

  **Category 5: `react/no-unescaped-entities` (1 error) — FIX**
  Replace `'` with `&apos;` in the dashboard page.

  **Category 6: `@typescript-eslint/no-unused-vars` (17 warnings) — FIX**
  Run `npx eslint src/ --fix` first to auto-fix what it can (unused eslint-disable directives, some unused vars). Then manually remove or prefix with `_` any remaining unused variables.

  **Category 7: `react-hooks/exhaustive-deps` (2 warnings) — FIX or SUPPRESS**
  Add missing dependencies to the dependency array, or suppress with a comment explaining why the dependency is intentionally omitted.

  **Category 8: `@next/next/no-img-element` (1 warning) — SUPPRESS**
  Add `// eslint-disable-next-line @next/next/no-img-element` above the `<img>` tag in CourseCard.

  **Category 9: Unused eslint-disable directives (6 warnings) — FIX**
  Remove stale `eslint-disable` comments that no longer suppress anything. `npx eslint src/ --fix` handles these automatically.

  **Order of operations:**
  1. Run `npx eslint src/ --fix` first for auto-fixable issues
  2. Fix Category 4 (any types) and Category 2 (refs) manually
  3. Add suppression comments for Categories 1, 3, 8
  4. Fix Category 5 (unescaped entities)
  5. Clean up remaining unused vars
  6. Run `npx eslint src/` to verify zero errors
  </action>
  <verify>Run `npx eslint src/` — output should show 0 errors. Warnings are acceptable but should be minimal. Run `npm run build` to confirm no TypeScript issues introduced by the fixes.</verify>
  <done>`npx eslint src/` reports 0 errors. Genuine bugs fixed (ref access, any types, unescaped entities). False positives suppressed with explanatory comments. Build still passes.</done>
</task>

</tasks>

<verification>
1. `npx eslint src/ 2>&1 | grep "error" | tail -1` shows "0 errors"
2. Practice results page uses `with:` clause for single-query loading
3. `getStudentAssignments` uses `Promise.all` for parallel queries
4. `npm run build` passes after all changes
</verification>

<success_criteria>
- Zero ESLint errors (0 errors, minimal warnings)
- Practice set + exercises loaded in single query
- Student assignment resolution uses parallel queries
- Build continues to pass
</success_criteria>

<output>
After completion, create `.planning/phases/38-production-hardening/38-03-SUMMARY.md`
</output>
