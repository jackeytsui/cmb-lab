---
phase: 38-production-hardening
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - e2e/fixtures/auth.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "`npx eslint .` reports zero errors across the entire codebase including e2e/"
  artifacts:
    - path: "e2e/fixtures/auth.ts"
      provides: "Playwright auth fixtures with suppressed false-positive lint errors"
      contains: "eslint-disable-next-line"
  key_links: []
---

<objective>
Suppress 2 false-positive ESLint errors in Playwright test fixtures.

Purpose: VERIFICATION.md found 2 remaining ESLint errors in `e2e/fixtures/auth.ts` (lines 17 and 26). ESLint's `react-hooks/rules-of-hooks` rule falsely flags Playwright's `use()` fixture API as a React Hook violation. These are Playwright test helper functions, not React components. Adding `eslint-disable-next-line` comments with explanatory context closes the final PROD-07 gap.

Output: Zero ESLint errors across the entire codebase.
</objective>

<execution_context>
@/home/sheldon/.claude/get-shit-done/workflows/execute-plan.md
@/home/sheldon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-production-hardening/38-VERIFICATION.md
@e2e/fixtures/auth.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add eslint-disable comments to Playwright fixture use() calls</name>
  <files>e2e/fixtures/auth.ts</files>
  <action>
  Add `// eslint-disable-next-line react-hooks/rules-of-hooks` above lines 17 and 26 in `e2e/fixtures/auth.ts`, each preceded by a brief explanatory comment.

  Before line 17 (`await use(page);` inside `studentPage`), add:
  ```typescript
  // Playwright fixture API, not a React hook
  // eslint-disable-next-line react-hooks/rules-of-hooks
  ```

  Before line 26 (`await use(page);` inside `coachPage`), add the same two lines:
  ```typescript
  // Playwright fixture API, not a React hook
  // eslint-disable-next-line react-hooks/rules-of-hooks
  ```

  This follows the existing codebase pattern established in plan 38-03, where `eslint-disable-next-line` with explanatory comments is used for known false-positive lint violations (e.g., zodResolver `as any`).

  After editing, run `npx eslint e2e/fixtures/auth.ts` to confirm zero errors on this file, then run `npx eslint .` (or `npx eslint src/ e2e/`) to confirm zero errors across the full codebase.
  </action>
  <verify>Run `npx eslint e2e/fixtures/auth.ts` — expect 0 errors, 0 warnings. Run `npx eslint . 2>&1 | tail -5` — expect "0 errors" in the summary line.</verify>
  <done>Both `use(page)` calls have `eslint-disable-next-line` comments with explanatory context. `npx eslint` reports zero errors across the entire codebase. PROD-07 fully satisfied.</done>
</task>

</tasks>

<verification>
1. `npx eslint e2e/fixtures/auth.ts` reports 0 errors
2. `npx eslint . 2>&1 | grep -E "\d+ error"` shows "0 errors"
3. `npm run build` still passes (no regressions)
</verification>

<success_criteria>
- Zero ESLint errors across the entire codebase (src/ and e2e/)
- Explanatory comments accompany each suppression
- Build continues to pass
</success_criteria>

<output>
After completion, create `.planning/phases/38-production-hardening/38-05-SUMMARY.md`
</output>
