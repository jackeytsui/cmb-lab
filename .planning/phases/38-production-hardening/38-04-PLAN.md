---
phase: 38-production-hardening
plan: 04
type: execute
wave: 2
depends_on: ["38-01"]
files_modified:
  - src/app/(dashboard)/settings/loading.tsx
  - src/app/(dashboard)/my-conversations/loading.tsx
  - src/app/(dashboard)/dashboard/practice/loading.tsx
  - src/app/(dashboard)/practice/[setId]/loading.tsx
  - src/app/(dashboard)/coach/students/loading.tsx
  - src/app/(dashboard)/coach/pronunciation/loading.tsx
  - src/app/(dashboard)/coach/conversations/[conversationId]/loading.tsx
  - src/app/(dashboard)/admin/exercises/loading.tsx
  - src/app/(dashboard)/admin/analytics/loading.tsx
  - src/app/(dashboard)/admin/content/loading.tsx
  - src/app/(dashboard)/admin/courses/loading.tsx
  - src/app/(dashboard)/admin/prompts/loading.tsx
  - src/app/(dashboard)/admin/ghl/loading.tsx
  - src/app/(dashboard)/admin/students/[studentId]/loading.tsx
  - src/app/(dashboard)/admin/practice-sets/[setId]/builder/loading.tsx
autonomous: true

must_haves:
  truths:
    - "All high-traffic pages show a loading skeleton while data fetches"
    - "Loading states render inside the sidebar layout main area (no sidebar/header duplication)"
    - "Loading skeletons visually match the page structure they replace"
  artifacts:
    - path: "src/app/(dashboard)/settings/loading.tsx"
      provides: "Settings page loading skeleton"
      contains: "Skeleton"
    - path: "src/app/(dashboard)/practice/[setId]/loading.tsx"
      provides: "Practice player loading skeleton"
      contains: "Skeleton"
    - path: "src/app/(dashboard)/coach/students/loading.tsx"
      provides: "Coach students page loading skeleton"
      contains: "Skeleton"
    - path: "src/app/(dashboard)/admin/exercises/loading.tsx"
      provides: "Admin exercises loading skeleton"
      contains: "Skeleton"
  key_links:
    - from: "src/app/(dashboard)/**/loading.tsx"
      to: "Next.js Suspense boundary"
      via: "File convention"
      pattern: "export default function Loading"
---

<objective>
Create loading.tsx files for all uncovered high-traffic pages.

Purpose: 30 page directories are missing loading.tsx files. When data fetches take time, users see a blank white page instead of a skeleton. Creating loading states at key parent levels and high-traffic pages provides immediate visual feedback during navigation. Loading states render inside the sidebar layout's `<main>` area -- they must NOT include sidebar or header elements.

Output: ~15 loading.tsx files covering all high-traffic student, coach, and admin pages.
</objective>

<execution_context>
@/home/sheldon/.claude/get-shit-done/workflows/execute-plan.md
@/home/sheldon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-production-hardening/38-RESEARCH.md
@src/app/(dashboard)/dashboard/loading.tsx (reference pattern)
@src/app/(dashboard)/admin/loading.tsx (reference pattern)
@src/app/(dashboard)/coach/loading.tsx (reference pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create loading.tsx for student and coach pages</name>
  <files>
    src/app/(dashboard)/settings/loading.tsx,
    src/app/(dashboard)/my-conversations/loading.tsx,
    src/app/(dashboard)/dashboard/practice/loading.tsx,
    src/app/(dashboard)/practice/[setId]/loading.tsx,
    src/app/(dashboard)/coach/students/loading.tsx,
    src/app/(dashboard)/coach/pronunciation/loading.tsx,
    src/app/(dashboard)/coach/conversations/[conversationId]/loading.tsx
  </files>
  <action>
  First, read existing loading.tsx files to understand the established pattern:
  - `src/app/(dashboard)/dashboard/loading.tsx`
  - `src/app/(dashboard)/coach/loading.tsx`

  Then create loading.tsx files for 7 student/coach pages. Each loading.tsx:
  - Exports a default `Loading` function
  - Uses the `Skeleton` component from `@/components/ui/skeleton`
  - Renders inside the main content area (NO sidebar, NO header)
  - Uses the same container pattern as other loading files (`container mx-auto px-4 py-8` or similar)
  - Uses `bg-zinc-800` for skeleton background (dark theme)

  **Per-page skeletons (match each page's layout):**

  1. **settings/loading.tsx** — Page title skeleton + card with 4 form field skeletons (label + input pairs) + save button skeleton. The settings page has sections for language, daily goal, timezone, and notifications.

  2. **my-conversations/loading.tsx** — Page title skeleton + list of 5 conversation row skeletons (avatar circle + two text lines).

  3. **dashboard/practice/loading.tsx** — Page title skeleton + filter bar skeleton + grid of 4 assignment card skeletons (title + status badge + due date).

  4. **practice/[setId]/loading.tsx** — Page title skeleton + progress bar skeleton + large content area skeleton (exercise area) + navigation button skeletons.

  5. **coach/students/loading.tsx** — Page title skeleton + search bar skeleton + table skeleton with 5 rows (avatar + name + email + progress bar + tags).

  6. **coach/pronunciation/loading.tsx** — Page title skeleton + filter bar skeleton + list of 4 result card skeletons (student name + score + date).

  7. **coach/conversations/[conversationId]/loading.tsx** — Back link skeleton + conversation header skeleton + 6 message bubble skeletons (alternating left/right).
  </action>
  <verify>Verify each file exists: `ls src/app/(dashboard)/settings/loading.tsx src/app/(dashboard)/my-conversations/loading.tsx src/app/(dashboard)/dashboard/practice/loading.tsx src/app/(dashboard)/practice/\[setId\]/loading.tsx src/app/(dashboard)/coach/students/loading.tsx src/app/(dashboard)/coach/pronunciation/loading.tsx src/app/(dashboard)/coach/conversations/\[conversationId\]/loading.tsx`. Grep each for "Skeleton" to confirm they import and use the skeleton component.</verify>
  <done>7 loading.tsx files created for student and coach pages. Each provides page-specific skeleton structure matching the actual page layout.</done>
</task>

<task type="auto">
  <name>Task 2: Create loading.tsx for admin pages</name>
  <files>
    src/app/(dashboard)/admin/exercises/loading.tsx,
    src/app/(dashboard)/admin/analytics/loading.tsx,
    src/app/(dashboard)/admin/content/loading.tsx,
    src/app/(dashboard)/admin/courses/loading.tsx,
    src/app/(dashboard)/admin/prompts/loading.tsx,
    src/app/(dashboard)/admin/ghl/loading.tsx,
    src/app/(dashboard)/admin/students/[studentId]/loading.tsx,
    src/app/(dashboard)/admin/practice-sets/[setId]/builder/loading.tsx
  </files>
  <action>
  Create loading.tsx files for 8 admin pages. Same pattern as Task 1:
  - Default export `Loading` function
  - Uses `Skeleton` from `@/components/ui/skeleton`
  - Dark theme skeletons (`bg-zinc-800`)
  - Content area only (no sidebar/header)

  **Per-page skeletons:**

  1. **admin/exercises/loading.tsx** — Page title skeleton + "Create" button skeleton + table skeleton with 5 rows (type icon + title + language tag + actions).

  2. **admin/analytics/loading.tsx** — Page title skeleton + date filter skeleton + grid of 4 stat card skeletons + large chart area skeleton.

  3. **admin/content/loading.tsx** — Page title skeleton + tabs skeleton + grid of 4 video card skeletons (thumbnail + title + status).

  4. **admin/courses/loading.tsx** — Page title skeleton + "Create" button skeleton + list of 3 course card skeletons (title + description + module count).

  5. **admin/prompts/loading.tsx** — Page title skeleton + list of 5 prompt row skeletons (name + type badge + last edited date).

  6. **admin/ghl/loading.tsx** — Page title skeleton + connection status card skeleton + field mapping table skeleton with 4 rows.

  7. **admin/students/[studentId]/loading.tsx** — Back link skeleton + student header skeleton (avatar + name + email) + tab bar skeleton + content area skeleton.

  8. **admin/practice-sets/[setId]/builder/loading.tsx** — Toolbar skeleton + two-column layout (palette column with 6 block skeletons + canvas column with large empty area skeleton).

  **Pages intentionally SKIPPED** (inherit parent loading.tsx or are low-traffic):
  - `admin/exercises/new`, `admin/exercises/[exerciseId]` — inherit from `admin/exercises/loading.tsx`
  - `admin/knowledge/[entryId]`, `admin/knowledge/new`, `admin/knowledge/search` — inherit from `admin/knowledge/loading.tsx`
  - `admin/content/uploads` — inherit from `admin/content/loading.tsx`
  - `admin/courses/[courseId]/*` — deeply nested CRUD forms, low traffic
  - `admin/prompts/[promptId]` — inherit from `admin/prompts/loading.tsx`
  - `test-interactive`, `test-video` — test pages, not user-facing

  After creating all files, run `npm run build` to confirm no import or TypeScript errors.
  </action>
  <verify>Verify each file exists and contains the Skeleton import. Run `npm run build` to confirm no errors. Count total loading.tsx files: `find src/app -name "loading.tsx" | wc -l` should show ~26 (11 existing + 15 new).</verify>
  <done>8 loading.tsx files created for admin pages. Total loading.tsx count is ~26 across the app. All high-traffic pages now show skeleton UI during data fetching. Build passes.</done>
</task>

</tasks>

<verification>
1. `find src/app -name "loading.tsx" | wc -l` shows ~26 files
2. All new loading.tsx files import `Skeleton` from `@/components/ui/skeleton`
3. No loading.tsx file contains `AppSidebar`, `SidebarProvider`, or header elements
4. `npm run build` passes with all new loading files
</verification>

<success_criteria>
- 15 new loading.tsx files created covering student, coach, and admin pages
- Each loading skeleton matches the structural layout of its corresponding page
- Loading states render only in the main content area (no sidebar/header duplication)
- Build passes with all new files
</success_criteria>

<output>
After completion, create `.planning/phases/38-production-hardening/38-04-SUMMARY.md`
</output>
