---
phase: 55-coach-assignments
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema/video.ts
  - src/lib/video-assignments.ts
  - src/app/api/admin/video-assignments/route.ts
  - src/app/api/admin/video-assignments/[assignmentId]/route.ts
  - src/app/(dashboard)/coach/video-assignments/page.tsx
  - src/components/coach/VideoAssignmentDialog.tsx
autonomous: true

must_haves:
  truths:
    - "Coach can create a video assignment by entering a YouTube URL, selecting a target (student/tag/course/module/lesson), and optionally adding notes and a due date"
    - "Coach can view all video assignments they have created"
    - "Coach can delete a video assignment"
    - "Duplicate assignment (same video + same target) is rejected with a user-friendly error"
  artifacts:
    - path: "src/db/schema/video.ts"
      provides: "videoAssignments table definition with relations"
      contains: "videoAssignments"
    - path: "src/lib/video-assignments.ts"
      provides: "CRUD functions for video assignments"
      exports: ["createVideoAssignment", "deleteVideoAssignment", "listCoachVideoAssignments"]
    - path: "src/app/api/admin/video-assignments/route.ts"
      provides: "POST + GET endpoints for video assignment CRUD"
      exports: ["POST", "GET"]
    - path: "src/app/api/admin/video-assignments/[assignmentId]/route.ts"
      provides: "DELETE endpoint for removing a video assignment"
      exports: ["DELETE"]
    - path: "src/app/(dashboard)/coach/video-assignments/page.tsx"
      provides: "Coach video assignments management page"
    - path: "src/components/coach/VideoAssignmentDialog.tsx"
      provides: "Dialog component for creating video assignments"
  key_links:
    - from: "src/components/coach/VideoAssignmentDialog.tsx"
      to: "/api/admin/video-assignments"
      via: "fetch POST"
      pattern: "fetch.*video-assignments"
    - from: "src/app/api/admin/video-assignments/route.ts"
      to: "src/lib/video-assignments.ts"
      via: "createVideoAssignment function call"
      pattern: "createVideoAssignment"
    - from: "src/lib/video-assignments.ts"
      to: "src/db/schema/video.ts"
      via: "Drizzle insert into videoAssignments"
      pattern: "db\\.insert\\(videoAssignments\\)"
---

<objective>
Create the video assignments schema, server-side CRUD library, API routes, and coach management UI for assigning YouTube videos to students/groups.

Purpose: Enables coaches to assign YouTube videos as homework by entering a URL, picking a target, and optionally setting notes and a due date. This is the foundation that Plan 02 builds on for student visibility and progress monitoring.

Output: videoAssignments table in schema, CRUD lib, API routes, coach page with create/delete/list functionality.
</objective>

<execution_context>
@/home/sheldon/.claude/get-shit-done/workflows/execute-plan.md
@/home/sheldon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-coach-assignments/55-RESEARCH.md
@src/db/schema/video.ts
@src/db/schema/practice.ts
@src/db/schema/index.ts
@src/lib/assignments.ts
@src/lib/youtube.ts
@src/app/api/admin/assignments/route.ts
@src/app/(dashboard)/coach/page.tsx
@src/components/practice/assignments/AssignmentDialog.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Video assignments schema + CRUD library</name>
  <files>
    src/db/schema/video.ts
    src/lib/video-assignments.ts
  </files>
  <action>
**Schema (src/db/schema/video.ts):**

Add a `videoAssignments` table to the EXISTING video.ts file (after `videoVocabEncounters`). IMPORT `assignmentTargetTypeEnum` from `./practice` -- do NOT redeclare it (Pitfall #1 from research). Import `users` from `./users` (already imported).

Table definition:
```
videoAssignments = pgTable("video_assignments", {
  id: uuid("id").defaultRandom().primaryKey(),
  youtubeUrl: text("youtube_url").notNull(),
  youtubeVideoId: varchar("youtube_video_id", { length: 11 }).notNull(),
  title: text("title"),                    // Optional coach-provided title
  notes: text("notes"),                    // Optional coach notes/instructions
  targetType: assignmentTargetTypeEnum("target_type").notNull(),
  targetId: uuid("target_id").notNull(),
  assignedBy: uuid("assigned_by").notNull().references(() => users.id),
  dueDate: timestamp("due_date"),
  createdAt: timestamp("created_at").notNull().defaultNow(),
}, (table) => [
  index("video_assignments_assigned_by_idx").on(table.assignedBy),
  index("video_assignments_youtube_video_id_idx").on(table.youtubeVideoId),
  unique("video_assignments_video_target_unique").on(
    table.youtubeVideoId, table.targetType, table.targetId
  ),
])
```

Add relations: `videoAssignmentsRelations` with `one(users, { fields: [videoAssignments.assignedBy], references: [users.id] })`.

Add type exports: `VideoAssignment` and `NewVideoAssignment`.

**CRUD Library (src/lib/video-assignments.ts):**

Create this file following the exact patterns in `src/lib/assignments.ts`. Import `extractVideoId` from `@/lib/youtube`, import the `videoAssignments` table, import `validateTargetExists` logic (copy the function from assignments.ts since it's not exported -- it validates course/module/lesson/student/tag existence).

Functions to create:

1. `createVideoAssignment(data: { youtubeUrl, title?, notes?, targetType, targetId, assignedBy, dueDate? })`:
   - Call `extractVideoId(data.youtubeUrl)` -- throw "Invalid YouTube URL" if null
   - Validate targetType is in ["course", "module", "lesson", "student", "tag"]
   - Call validateTargetExists (internal copy)
   - Insert into videoAssignments with all fields
   - Catch error code 23505 and throw "This video is already assigned to this target" (Pitfall #5)
   - Return the created assignment

2. `deleteVideoAssignment(id: string)`: Hard delete, return deleted row or null

3. `listCoachVideoAssignments(coachId: string)`: Select all videoAssignments where assignedBy = coachId, ordered by createdAt desc. Return array.

Use the `VALID_TARGET_TYPES` constant like assignments.ts does.
  </action>
  <verify>
Run `npx tsc --noEmit` from project root. No type errors in video.ts or video-assignments.ts. Confirm videoAssignments is exported from schema/index.ts (it's covered by `export * from "./video"`).
  </verify>
  <done>
videoAssignments table defined in schema with correct indexes and unique constraint. CRUD library has createVideoAssignment (with URL validation, target validation, 23505 catch), deleteVideoAssignment, and listCoachVideoAssignments. All types compile cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: API routes + coach management page with create dialog</name>
  <files>
    src/app/api/admin/video-assignments/route.ts
    src/app/api/admin/video-assignments/[assignmentId]/route.ts
    src/app/(dashboard)/coach/video-assignments/page.tsx
    src/components/coach/VideoAssignmentDialog.tsx
  </files>
  <action>
**API Route: POST + GET (src/app/api/admin/video-assignments/route.ts):**

Follow the exact pattern from `src/app/api/admin/assignments/route.ts`:

POST: Require `hasMinimumRole("coach")`. Get current user via `getCurrentUser()`. Parse body: `{ youtubeUrl, title?, notes?, targetType, targetId, dueDate? }`. Validate youtubeUrl is a string, targetType in valid list, targetId is a string. Parse optional dueDate. Call `createVideoAssignment()`. Return 201 with assignment. Handle "already exists" -> 409, "not found" -> 400, other -> 500.

GET: Require coach role. Call `listCoachVideoAssignments(user.id)`. Return 200 with `{ assignments }`.

**API Route: DELETE (src/app/api/admin/video-assignments/[assignmentId]/route.ts):**

Require coach role. Extract assignmentId from params. Call `deleteVideoAssignment(assignmentId)`. Return 200 with `{ success: true }` or 404 if not found.

**Coach Page (src/app/(dashboard)/coach/video-assignments/page.tsx):**

Server component following `src/app/(dashboard)/coach/page.tsx` pattern. Check `hasMinimumRole("coach")`, redirect to /dashboard if not. Get current user via `getCurrentUser()`. Direct DB query via `listCoachVideoAssignments(user.id)` (v7-14 pattern -- no self-fetch). Pass assignments to a client wrapper that renders:
- Page title "Video Assignments" with a "New Assignment" button
- List of existing assignments showing: title (or "Untitled" fallback), YouTube video ID, target type, due date (formatted with date-fns if present), created date
- Each assignment has a delete button
- Empty state when no assignments exist

**VideoAssignmentDialog (src/components/coach/VideoAssignmentDialog.tsx):**

Client component following `src/components/practice/assignments/AssignmentDialog.tsx` pattern. Uses Radix Dialog, Select components (already installed).

Props: `{ open, onOpenChange, onCreated: () => void }`.

Form fields:
- YouTube URL input (text input, required)
- Title input (text input, optional, placeholder "Optional custom title")
- Notes textarea (optional, placeholder "Instructions for students...")
- Target Type select (Course/Module/Lesson/Student/Tag) -- same cascading logic as AssignmentDialog (selecting Course populates course list, selecting Module requires course then module, etc.)
- Target entity select (populated based on targetType via `/api/admin/assignments?action=targets&targetType=...` -- reuse existing target options endpoints from AssignmentDialog, OR fetch from the same endpoints the AssignmentDialog uses: `/api/admin/courses`, `/api/admin/students`, `/api/admin/tags`)
- Due Date input (date input, optional)

On submit: POST to `/api/admin/video-assignments` with all fields. Show loading state. On success: close dialog, call onCreated(). On error: show error message in dialog (especially "already assigned" for 409).

For the target selection cascading: Replicate the SAME target fetching pattern used in `AssignmentDialog.tsx` which fetches courses from `/api/admin/courses`, modules filtered by courseId, lessons filtered by moduleId, students, and tags. All these API endpoints already exist.

Add a link card to the coach dashboard (`src/app/(dashboard)/coach/page.tsx`) in the navigation cards grid -- use Video icon from lucide-react, link to `/coach/video-assignments`, with description "Assign YouTube videos to students as homework".
  </action>
  <verify>
Run `npx tsc --noEmit` from project root. No type errors. Verify the coach page is accessible at `/coach/video-assignments`. Verify the API routes exist at `/api/admin/video-assignments`.
  </verify>
  <done>
Coach can navigate to /coach/video-assignments, click "New Assignment", fill in a YouTube URL + target + optional notes/due date, and create an assignment. Assignments are listed on the page. Coach can delete assignments. Duplicate assignment returns user-friendly 409 error.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. videoAssignments table has correct unique constraint on (youtubeVideoId, targetType, targetId)
3. assignmentTargetTypeEnum is imported from practice.ts, NOT redeclared
4. API POST /api/admin/video-assignments returns 201 on success, 409 on duplicate, 400 on invalid input
5. API DELETE /api/admin/video-assignments/[id] returns 200 on success, 404 on not found
6. Coach page at /coach/video-assignments renders assignment list from direct DB query (no self-fetch)
7. VideoAssignmentDialog has all form fields: URL, title, notes, target type, target entity, due date
</verification>

<success_criteria>
- Coach can create a YouTube video assignment targeting a student, tag, course, module, or lesson
- Coach can view all their created video assignments on /coach/video-assignments
- Coach can delete a video assignment
- Duplicate video+target assignment is rejected with a clear error message
- All code compiles without type errors
</success_criteria>

<output>
After completion, create `.planning/phases/55-coach-assignments/55-01-SUMMARY.md`
</output>
