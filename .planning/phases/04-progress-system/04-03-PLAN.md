---
phase: 04-progress-system
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "lesson_progress table exists in Neon database"
    - "Schema matches src/db/schema/progress.ts definition"
    - "Drizzle Studio can browse lesson_progress table"
  artifacts:
    - path: "src/db/migrations/*.sql"
      provides: "lesson_progress CREATE TABLE statement"
      contains: "lesson_progress"
  key_links:
    - from: "src/db/schema/progress.ts"
      to: "Neon database"
      via: "Drizzle migration"
      pattern: "CREATE TABLE.*lesson_progress"
---

<objective>
Deploy lesson_progress database table to Neon.

Purpose: Gap closure - the lesson_progress schema exists in code but the table was never migrated to the database. All progress API calls will fail at runtime without this table.

Output: Schema pushed directly to Neon database via db:push.
</objective>

<execution_context>
@/home/sheldon/.claude/get-shit-done/workflows/execute-plan.md
@/home/sheldon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/db/schema/progress.ts
@src/db/schema/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Push lesson_progress schema to Neon database</name>
  <files>Neon database (no local file changes)</files>
  <action>
Run database push command to deploy the lesson_progress table:

1. Run `npm run db:push` to sync schema directly to Neon database
   - This pushes the schema changes to the actual database
   - Detects lesson_progress table in schema but not in database
   - Creates the table with all columns and constraints
   - No migration files needed - db:push is the direct sync approach

2. Verify the table exists by running Drizzle Studio briefly:
   - Run `npx drizzle-kit studio` (can be killed after verification)
   - OR use the Neon MCP tool to query the table directly

If db:push fails with connection error, ensure DATABASE_URL is set in .env.local.
If table already exists (from prior run), db:push will be a no-op.

Note: Do NOT use db:generate - this project uses db:push for direct schema synchronization without migration files.
  </action>
  <verify>
Run `npx drizzle-kit studio` and confirm lesson_progress table is visible.
OR query database directly: SELECT table_name FROM information_schema.tables WHERE table_name = 'lesson_progress';
  </verify>
  <done>
lesson_progress table exists in Neon database with all columns matching schema:
- id (uuid, primary key)
- user_id (uuid, FK to users)
- lesson_id (uuid, FK to lessons)
- video_watched_percent (integer)
- video_completed_at (timestamp)
- interactions_completed (integer)
- interactions_total (integer)
- completed_at (timestamp)
- started_at (timestamp)
- last_accessed_at (timestamp)
- Unique constraint on (user_id, lesson_id)
  </done>
</task>

</tasks>

<verification>
1. `npm run db:push` completes without errors
2. Table lesson_progress visible in Drizzle Studio or via SQL query
3. All foreign key constraints to users and lessons tables exist
4. Composite unique constraint on (user_id, lesson_id) exists
</verification>

<success_criteria>
- lesson_progress table deployed to Neon database
- Schema matches src/db/schema/progress.ts exactly
- Progress API routes (/api/progress/*) can now execute without "table not found" errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-progress-system/04-03-SUMMARY.md`
</output>
