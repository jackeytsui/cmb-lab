---
phase: 32-practice-set-builder
plan: 06
type: execute
wave: 4
depends_on: ["32-05"]
files_modified:
  - src/app/(dashboard)/admin/exercises/ExerciseListClient.tsx
  - src/app/(dashboard)/admin/exercises/page.tsx
autonomous: false

must_haves:
  truths:
    - "Each practice set in the exercises list has an 'Open in Builder' link that navigates to /admin/practice-sets/[setId]/builder"
    - "Coach can navigate from exercises list to builder and back"
    - "Builder page renders correctly and all core interactions work"
  artifacts:
    - path: "src/app/(dashboard)/admin/exercises/ExerciseListClient.tsx"
      provides: "Builder link on each practice set header"
  key_links:
    - from: "src/app/(dashboard)/admin/exercises/ExerciseListClient.tsx"
      to: "/admin/practice-sets/[setId]/builder"
      via: "Link or router.push"
      pattern: "practice-sets.*builder"
---

<objective>
Add navigation from the exercises list to the builder page, and verify the complete builder experience.

Purpose: Connects the existing exercise management page (Phase 31) to the new builder page (Phase 32) so coaches can discover and access the visual builder.
Output: Updated ExerciseListClient with builder links, visual verification of the complete builder.
</objective>

<execution_context>
@/home/sheldon/.claude/get-shit-done/workflows/execute-plan.md
@/home/sheldon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/32-practice-set-builder/32-05-SUMMARY.md
@src/app/(dashboard)/admin/exercises/ExerciseListClient.tsx
@src/app/(dashboard)/admin/exercises/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add builder navigation links to ExerciseListClient</name>
  <files>
    src/app/(dashboard)/admin/exercises/ExerciseListClient.tsx
    src/app/(dashboard)/admin/exercises/page.tsx
  </files>
  <action>
    **ExerciseListClient.tsx changes:**

    1. Import Blocks icon from lucide-react (represents the visual builder).

    2. In each practice set header row (inside the button that toggles expansion), add a "Builder" link next to the set title/status/count. This should be a separate clickable element (not inside the toggle button) — use e.stopPropagation() to prevent toggling when clicking the builder link.

    Modify the set header section to include:
    ```tsx
    <div className="flex shrink-0 items-center gap-2">
      <Button
        variant="outline"
        size="sm"
        onClick={(e) => {
          e.stopPropagation();
          router.push(`/admin/practice-sets/${set.id}/builder`);
        }}
        className="border-zinc-600 bg-zinc-700/50 text-zinc-300 hover:bg-zinc-700 hover:text-white"
      >
        <Blocks className="mr-1.5 h-3.5 w-3.5" />
        Builder
      </Button>
    </div>
    ```

    Place this button to the right of the set info (before the chevron or after the exercise count) within the set header. The exact placement: between the set info (title + status + count) and the right edge. Make the set header a flex row with the toggle area on the left and the builder button on the right.

    This means restructuring the set header slightly:
    - The outer container is a flex row
    - Left side: the toggle button (chevron + title + status + count) — this remains clickable for expand/collapse
    - Right side: the "Builder" button — separate from the toggle

    3. Also update the page header in `page.tsx` to mention the builder:
    Update the description text to: "Create and manage practice exercises grouped by practice sets. Use the Builder for visual drag-and-drop editing."
  </action>
  <verify>
    - Each practice set in the list has a "Builder" button
    - Clicking "Builder" navigates to /admin/practice-sets/[setId]/builder
    - Clicking "Builder" does NOT toggle the set expansion
    - `npx tsc --noEmit` compiles
  </verify>
  <done>
    Each practice set in the exercises list has a "Builder" link. Coaches can access the visual builder from the existing exercise management page.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Practice Set Builder (Phase 32):
    1. Visual drag-and-drop builder page at /admin/practice-sets/[setId]/builder
    2. Palette with 6 exercise types (drag to canvas)
    3. Sortable canvas (reorder by dragging)
    4. Inline editing (click exercise block to edit)
    5. Live student preview panel
    6. Undo/Redo (Ctrl+Z / Ctrl+Shift+Z)
    7. Save Draft / Publish buttons with batch save
    8. Duplicate practice set
    9. Unsaved changes warning
    10. Builder link on exercises list page
  </what-built>
  <how-to-verify>
    1. Start dev server: `npm run dev`
    2. Navigate to /admin/exercises
    3. Create a practice set if none exist (use "New Practice Set" button)
    4. Click "Builder" on a practice set to open the builder page
    5. Verify the 3-panel layout: palette (left), canvas (center), preview (right)
    6. Drag a "Multiple Choice" block from the palette onto the canvas
    7. Verify the block appears on the canvas with an "Needs editing" indicator
    8. Click the block to open inline editor
    9. Fill in the question and options, click Save
    10. Verify the preview panel updates to show the student view
    11. Drag another exercise type onto the canvas
    12. Reorder exercises by dragging within the canvas
    13. Press Ctrl+Z to undo the reorder
    14. Press Ctrl+Shift+Z to redo
    15. Click "Save Draft" — verify no errors
    16. Click "Duplicate" — verify navigation to new builder with copied exercises
    17. Make a change, then try to close the browser tab — verify beforeunload prompt
    18. Click "Publish" — verify status changes
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues</resume-signal>
</task>

</tasks>

<verification>
1. Builder link appears on each practice set in the exercises list
2. Builder page loads at /admin/practice-sets/[setId]/builder
3. All 7 BUILD requirements are visually verifiable:
   - BUILD-01: Drag from palette to canvas
   - BUILD-02: Reorder by dragging on canvas
   - BUILD-03: Click to edit inline
   - BUILD-04: Live preview panel
   - BUILD-05: Undo/redo with keyboard shortcuts
   - BUILD-06: Save draft / publish
   - BUILD-07: Duplicate practice set
</verification>

<success_criteria>
- Coach can access builder from exercises list via "Builder" button
- All 7 BUILD requirements are demonstrably working
- User has verified the builder experience visually
</success_criteria>

<output>
After completion, create `.planning/phases/32-practice-set-builder/32-06-SUMMARY.md`
</output>
