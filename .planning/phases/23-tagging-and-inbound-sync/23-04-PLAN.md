---
phase: 23-tagging-and-inbound-sync
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/coach/students/StudentListWithTags.tsx
  - src/app/api/admin/students/route.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Coach selects tag filters and the student list updates to show only students with matching tags, fetched from the server API"
    - "When no tags are selected, the initial server-rendered student list is shown (no extra fetch)"
    - "Loading state is visible while fetching filtered results"
  artifacts:
    - path: "src/app/(dashboard)/coach/students/StudentListWithTags.tsx"
      provides: "Client-side fetch with ?tagIds= when tag filters change"
      contains: "fetch.*api/admin/students.*tagIds"
  key_links:
    - from: "src/app/(dashboard)/coach/students/StudentListWithTags.tsx"
      to: "/api/admin/students?tagIds="
      via: "fetch in useEffect or useCallback triggered by selectedTagIds"
      pattern: "fetch.*api/admin/students.*tagIds"
---

<objective>
Wire TagFilter selectedTagIds to the student list API so tag filtering fetches server-side filtered results instead of only filtering the initially loaded students client-side.

Purpose: Closes the TAG-03 verification gap — TagFilter exists, the API supports ?tagIds=, but the client component never calls the API when filters change. This is critical for correct results with pagination in Phase 24.

Output: Updated StudentListWithTags.tsx that fetches from /api/admin/students?tagIds=id1,id2 when tag filters are active, falling back to the initial server-rendered data when no filters are selected.
</objective>

<execution_context>
@/home/sheldon/.claude/get-shit-done/workflows/execute-plan.md
@/home/sheldon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/23-tagging-and-inbound-sync/23-VERIFICATION.md
@src/app/(dashboard)/coach/students/StudentListWithTags.tsx
@src/app/api/admin/students/route.ts
@src/app/(dashboard)/coach/students/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire tag filter to server-side API fetch</name>
  <files>src/app/(dashboard)/coach/students/StudentListWithTags.tsx</files>
  <action>
Modify StudentListWithTags.tsx to fetch from the API when tag filters are active:

1. Add a `loading` state (boolean, default false) and a `fetchedStudents` state (Student[] | null, default null).

2. Add a `useEffect` that triggers when `selectedTagIds` changes:
   - If `selectedTagIds` is empty, set `fetchedStudents` to null (use initial data).
   - If `selectedTagIds` has values, set `loading = true`, fetch from `/api/admin/students?tagIds=${selectedTagIds.join(",")}`, parse the JSON response (shape: `{ students: [...], total: number }`), map the response students to the Student interface (the API returns the same shape but includes extra fields like clerkId/createdAt — just pick id, name, email, tags, and compute accessCount as 0 since it's not in the API response OR add a note that accessCount isn't available from this endpoint). Set `fetchedStudents` to the mapped result, set `loading = false`.
   - Use an AbortController to cancel in-flight requests when selectedTagIds changes again.
   - Wrap in try/catch. On error (not abort), set loading to false and log the error.

3. Replace the existing `filteredStudents` useMemo:
   - If `fetchedStudents` is not null, use `fetchedStudents` as the display list.
   - If `fetchedStudents` is null, use the `students` state (initial server data).
   - Remove the old client-side `.filter()` logic entirely.

4. Add a loading indicator in the JSX: When `loading` is true, show a subtle "Filtering..." text or a spinner above the student list (below the TagFilter, near the existing count text).

5. Update the `handleTagsChange` callback: Instead of `window.location.reload()`, refetch from the API to update the student list without a full page reload. If no tag filters are active, fetch all students from `/api/admin/students` (no tagIds param). Update both the `students` state (base data) and clear `fetchedStudents` to null so the fresh base data is shown.

Important notes:
- The API is at `/api/admin/students` and requires admin role. The coach students page currently uses direct DB queries. However, the coach has admin-level access in this app (hasMinimumRole("coach") is used in the page, but the API checks for "admin"). Check if the coach role satisfies `hasMinimumRole("admin")` — if not, the fetch will 403. If so, use `/api/admin/students` directly. If not, create the fetch to use the same endpoint but document that the role check may need adjustment.
- Actually, looking at the API route, it checks `hasMinimumRole("admin")`. The coach role may not satisfy this. To avoid scope creep, use a simpler approach: keep the initial server-rendered data as the unfiltered baseline, and for tag filtering, do client-side filtering on the initial data as a fallback. BUT ALSO attempt the API call. If the API returns 403, fall back to client-side filtering. This way tag filtering works immediately (client-side on loaded data) and will work even better once the role is aligned.
- ACTUALLY, the simplest correct fix: Change the approach. The server component already loads ALL students. For tag filtering, do client-side filtering on the full dataset (which is what it does now). The problem the verification identified is that client-side filtering won't work correctly with pagination (Phase 24). For NOW, the client-side filtering IS correct because all students are loaded. The gap is about FUTURE correctness with pagination.
- REVISED approach: Wire to the API for correctness, but also fix the role check. Update the API route `/api/admin/students/route.ts` to use `hasMinimumRole("coach")` instead of `hasMinimumRole("admin")` since the verification context says "All tag routes require coach+ role (not admin-only)". This is a one-line change in the API route.

Final implementation plan:
1. In `/api/admin/students/route.ts`: Change `hasMinimumRole("admin")` to `hasMinimumRole("coach")` (line 29).
2. In `StudentListWithTags.tsx`:
   - Add `loading` state and `fetchedStudents` state.
   - Add useEffect on selectedTagIds that fetches from API with ?tagIds= param.
   - Use AbortController for request cancellation.
   - Display `fetchedStudents ?? students` as the rendered list.
   - Show loading indicator when fetching.
   - Update handleTagsChange to refetch instead of window.location.reload().
  </action>
  <verify>
1. `npx tsc --noEmit` passes without type errors.
2. `npm run build` completes successfully.
3. Grep for `window.location.reload` in StudentListWithTags.tsx returns no results.
4. Grep for `fetch.*api/admin/students` in StudentListWithTags.tsx returns a match.
5. Grep for `hasMinimumRole.*coach` in `/api/admin/students/route.ts` returns a match.
  </verify>
  <done>
- When coach selects tags in TagFilter, StudentListWithTags fetches from /api/admin/students?tagIds=... and displays server-filtered results.
- When no tags are selected, initial server-rendered data is displayed (no extra fetch).
- Loading state shown during API fetch.
- No more window.location.reload() — tag changes refresh data via API.
- API route accepts coach+ role (not admin-only).
  </done>
</task>

</tasks>

<verification>
1. TypeScript compilation: `npx tsc --noEmit` passes
2. Build check: `npm run build` succeeds
3. Key wiring confirmed:
   - `StudentListWithTags.tsx` contains `fetch` call to `/api/admin/students` with `tagIds` parameter
   - `StudentListWithTags.tsx` does NOT contain `window.location.reload`
   - `/api/admin/students/route.ts` uses `hasMinimumRole("coach")` not `hasMinimumRole("admin")`
4. AbortController present for request cancellation
5. Loading state rendered in JSX
</verification>

<success_criteria>
- TAG-03 (filter students by tag) gap is closed: TagFilter selection triggers API fetch with ?tagIds= param
- Student list updates with server-filtered results when tags are selected
- No full page reload needed for tag operations
- Coach role can access the students API endpoint
</success_criteria>

<output>
After completion, create `.planning/phases/23-tagging-and-inbound-sync/23-04-SUMMARY.md`
</output>
