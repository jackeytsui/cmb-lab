---
phase: 53-playback-practice-controls
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useVideoSync.ts
  - src/lib/captions.ts
  - src/app/api/video/extract-captions/route.ts
  - src/components/video/DualSubtitleOverlay.tsx
  - src/components/video/TranscriptToolbar.tsx
  - src/components/video/YouTubePlayer.tsx
  - src/app/(dashboard)/dashboard/listening/ListeningClient.tsx
autonomous: true

must_haves:
  truths:
    - "Student can change playback speed (0.5x, 0.75x, 1x, 1.25x, 1.5x, 2x) and video plays at the selected speed"
    - "Dual subtitles (Chinese + English) can be toggled on the video overlay independently"
    - "English captions are fetched alongside Chinese captions when available"
  artifacts:
    - path: "src/hooks/useVideoSync.ts"
      provides: "playbackRate, setPlaybackRate, currentTimeMs exposed from hook"
      contains: "setPlaybackRate"
    - path: "src/components/video/DualSubtitleOverlay.tsx"
      provides: "Chinese + English subtitle overlay component"
      exports: ["DualSubtitleOverlay"]
    - path: "src/lib/captions.ts"
      provides: "extractEnglishCaptions function"
      exports: ["extractEnglishCaptions"]
    - path: "src/components/video/TranscriptToolbar.tsx"
      provides: "Speed selector and subtitle toggle controls"
      contains: "playbackRate"
  key_links:
    - from: "src/hooks/useVideoSync.ts"
      to: "playerRef.current.setPlaybackRate"
      via: "YouTube IFrame API call"
      pattern: "setPlaybackRate"
    - from: "src/components/video/DualSubtitleOverlay.tsx"
      to: "src/hooks/useVideoSync.ts"
      via: "currentTimeMs prop from hook"
      pattern: "findActiveCaptionIndex"
    - from: "src/app/api/video/extract-captions/route.ts"
      to: "src/lib/captions.ts"
      via: "extractEnglishCaptions import"
      pattern: "extractEnglishCaptions"
---

<objective>
Add playback speed control and dual subtitle overlay (Chinese + English) to the Video Listening Lab.

Purpose: VID-04 (speed control) and VID-05 (dual subtitles) are core playback features that give students control over their listening difficulty and comprehension support.

Output:
- Extended `useVideoSync` hook with `playbackRate`, `setPlaybackRate`, `currentTimeMs`
- `extractEnglishCaptions` function in captions lib
- English caption extraction in extract-captions API route
- New `DualSubtitleOverlay` component
- Speed selector + subtitle toggles in `TranscriptToolbar`
- Full wiring in `ListeningClient` and `YouTubePlayer`
</objective>

<execution_context>
@/home/sheldon/.claude/get-shit-done/workflows/execute-plan.md
@/home/sheldon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-playback-practice-controls/53-RESEARCH.md
@.planning/phases/51-interactive-transcript/51-02-SUMMARY.md
@.planning/phases/52-dictionary-vocabulary-integration/52-02-SUMMARY.md
@src/hooks/useVideoSync.ts
@src/lib/captions.ts
@src/app/api/video/extract-captions/route.ts
@src/components/video/YouTubePlayer.tsx
@src/components/video/TranscriptToolbar.tsx
@src/components/video/DualSubtitleOverlay.tsx
@src/app/(dashboard)/dashboard/listening/ListeningClient.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend useVideoSync with speed control + currentTimeMs, add extractEnglishCaptions, update extract-captions API</name>
  <files>
    src/hooks/useVideoSync.ts
    src/lib/captions.ts
    src/app/api/video/extract-captions/route.ts
  </files>
  <action>
    **useVideoSync.ts** -- Add three new capabilities to the existing hook:

    1. **Playback rate control:**
       - Add state: `const [playbackRate, setPlaybackRateState] = useState(1);`
       - Add `setPlaybackRate` callback that calls `playerRef.current.setPlaybackRate(rate)` and updates state. Guard with `if (!player) return;`.
       - In `handlePlayerReady`, query `player.getAvailablePlaybackRates()` (cast as `unknown as number[]` per the v7-3 pattern) and store in state `availableRates`. Default to `[0.5, 0.75, 1, 1.25, 1.5, 2]` if call fails.

    2. **Expose currentTimeMs as React state:**
       - Add state: `const [currentTimeMs, setCurrentTimeMs] = useState(0);`
       - In the polling setInterval callback, after computing `timeMs`, update: `setCurrentTimeMs((prev) => (Math.abs(prev - timeMs) > 200 ? timeMs : prev));` -- only update state when time moves more than 200ms to avoid re-render storms while still providing smooth subtitle sync.

    3. **Export the playerRef for external pause/play:**
       - Add `pauseVideo` and `playVideo` callbacks that call `playerRef.current?.pauseVideo()` / `playerRef.current?.playVideo()`.

    Return type extends to include: `playbackRate`, `setPlaybackRate`, `availableRates`, `currentTimeMs`, `pauseVideo`, `playVideo`.

    **captions.ts** -- Add `extractEnglishCaptions` function:
    - Try language codes `["en", "en-US", "en-GB"]` in order (same try/catch pattern as `extractChineseCaptions`).
    - Return `NormalizedCaption[] | null` (null when no English captions available).
    - On language-related errors, `continue` to next code. On any other error, return `null` (don't throw -- English is optional).
    - Export the function.

    **extract-captions/route.ts** -- Extract English captions in parallel with Chinese:
    - Import `extractEnglishCaptions` from `@/lib/captions`.
    - After the existing Chinese extraction succeeds (step 5), call `extractEnglishCaptions(videoId)` in parallel (or sequentially -- it's a fast operation).
    - Include `englishCaptions` in the JSON response alongside existing `captions` field. When English extraction returns null, send `englishCaptions: null`.
    - For the cached-captions early return path (step 4), also include `englishCaptions: null` (English captions are not persisted in DB per research decision).
    - For the no-Chinese-captions path, also include `englishCaptions: null`.
  </action>
  <verify>
    Run `npx tsc --noEmit` -- zero type errors. Verify `extractEnglishCaptions` is exported from captions.ts. Verify useVideoSync return type includes `playbackRate`, `setPlaybackRate`, `availableRates`, `currentTimeMs`, `pauseVideo`, `playVideo`.
  </verify>
  <done>
    useVideoSync exposes playback rate control and currentTimeMs. extractEnglishCaptions function exists. API route returns englishCaptions field.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create DualSubtitleOverlay, add speed/subtitle controls to toolbar, wire everything in ListeningClient</name>
  <files>
    src/components/video/DualSubtitleOverlay.tsx
    src/components/video/TranscriptToolbar.tsx
    src/components/video/YouTubePlayer.tsx
    src/app/(dashboard)/dashboard/listening/ListeningClient.tsx
  </files>
  <action>
    **DualSubtitleOverlay.tsx** -- New component (follow pattern from existing SubtitleOverlay.tsx):
    - Props: `currentTimeMs: number`, `chineseCaptions: CaptionLine[]`, `englishCaptions: CaptionLine[] | null`, `showChinese: boolean`, `showEnglish: boolean`.
    - Import `findActiveCaptionIndex` -- export this function from `useVideoSync.ts` (it's currently module-private; add `export` to the existing function declaration).
    - Find active Chinese caption index when `showChinese` is true, find active English caption index when `showEnglish && englishCaptions` is truthy. Use independent binary search calls (timestamps may differ between languages).
    - If both indices are -1, return null (no subtitle to show).
    - Render: `position: absolute`, `bottom-16`, `left-0 right-0`, `flex flex-col items-center gap-1`, `pointer-events-none`, `z-10`.
    - Chinese text: `bg-black/70 backdrop-blur-sm px-4 py-1.5 rounded-lg max-w-[90%]`, `text-xl text-white text-center`.
    - English text: `bg-black/60 backdrop-blur-sm px-4 py-1 rounded-lg max-w-[90%]`, `text-sm text-zinc-200 text-center`.

    **YouTubePlayer.tsx** -- Wrap the `<YouTube>` component and overlay in a `relative` container:
    - Change the outer `<div className="aspect-video">` to also include `relative overflow-hidden`.
    - Add optional `children?: React.ReactNode` prop so the overlay can be placed as a child.
    - Render `{children}` inside the aspect-video container, after the `<YouTube>` element.

    **TranscriptToolbar.tsx** -- Add speed selector and subtitle toggles:
    - Add props: `playbackRate: number`, `availableRates: number[]`, `onPlaybackRateChange: (rate: number) => void`, `showChineseSubs: boolean`, `showEnglishSubs: boolean`, `onToggleChineseSubs: () => void`, `onToggleEnglishSubs: () => void`, `hasEnglishCaptions: boolean`.
    - Speed selector: A row of small buttons showing rates like "0.5x", "1x", "1.5x" etc. Active rate gets `bg-zinc-700 text-white`. Filter `availableRates` to only show the required rates: `[0.5, 0.75, 1, 1.25, 1.5, 2]`. Use `Gauge` icon from lucide-react.
    - Subtitle toggles: Two small toggle buttons -- "CN" for Chinese subtitles, "EN" for English subtitles. Active toggle gets `bg-cyan-700/50 text-cyan-300`. EN button is disabled/dimmed when `!hasEnglishCaptions`. Use `Subtitles` icon from lucide-react.
    - Place these new controls in a second row below the existing annotation/script/vocab row, OR extend the existing row if space allows. Use a wrapping flex layout. The toolbar may need to grow slightly.

    **ListeningClient.tsx** -- Wire everything together:
    - Destructure new fields from `useVideoSync`: `playbackRate`, `setPlaybackRate`, `availableRates`, `currentTimeMs`, `pauseVideo`, `playVideo`.
    - Add state: `englishCaptions: CaptionLine[] | null` (default null), `showChineseSubs: boolean` (default false), `showEnglishSubs: boolean` (default false).
    - In `handleUrlSubmit`: Parse `data.englishCaptions` from the API response and set into state.
    - Render `<DualSubtitleOverlay>` as a child of `<YouTubePlayer>`: pass `currentTimeMs`, `chineseCaptions={captions}`, `englishCaptions`, `showChinese={showChineseSubs}`, `showEnglish={showEnglishSubs}`.
    - Pass speed/subtitle props to `<TranscriptToolbar>`: `playbackRate`, `availableRates`, `onPlaybackRateChange={setPlaybackRate}`, `showChineseSubs`, `showEnglishSubs`, `onToggleChineseSubs`, `onToggleEnglishSubs`, `hasEnglishCaptions={!!englishCaptions}`.
  </action>
  <verify>
    Run `npx tsc --noEmit` -- zero type errors. Verify DualSubtitleOverlay.tsx exists. Verify TranscriptToolbar renders speed selector. Verify ListeningClient passes all new props.
  </verify>
  <done>
    Speed selector buttons work in toolbar. Dual subtitle overlay renders Chinese and English text independently togglable. YouTubePlayer wraps overlay as children. All wired in ListeningClient.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `DualSubtitleOverlay.tsx` exists and exports `DualSubtitleOverlay`
3. `useVideoSync` returns `playbackRate`, `setPlaybackRate`, `availableRates`, `currentTimeMs`, `pauseVideo`, `playVideo`
4. `extractEnglishCaptions` is exported from `captions.ts`
5. `extract-captions` API route includes `englishCaptions` in response
6. `TranscriptToolbar` renders speed selector buttons and subtitle toggle buttons
7. `ListeningClient` wires speed control, subtitle state, and overlay
</verification>

<success_criteria>
- Student can click speed buttons (0.5x, 0.75x, 1x, 1.25x, 1.5x, 2x) in the toolbar and the video plays at the selected speed
- Chinese subtitle toggle shows/hides Chinese text overlay on the video
- English subtitle toggle shows/hides English text overlay on the video (disabled when no English captions available)
- Both subtitles can be shown simultaneously, independently togglable
- English captions are fetched from YouTube API alongside Chinese captions
</success_criteria>

<output>
After completion, create `.planning/phases/53-playback-practice-controls/53-01-SUMMARY.md`
</output>
