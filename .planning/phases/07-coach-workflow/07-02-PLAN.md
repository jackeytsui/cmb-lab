---
phase: 07-coach-workflow
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/app/(dashboard)/coach/page.tsx
  - src/app/(dashboard)/coach/submissions/[submissionId]/page.tsx
  - src/components/coach/SubmissionQueue.tsx
  - src/components/coach/SubmissionCard.tsx
autonomous: true

must_haves:
  truths:
    - "Coach can view list of pending submissions"
    - "Coach can see student name, lesson title, and submission type in queue"
    - "Coach can click submission to view full details"
  artifacts:
    - path: "src/app/(dashboard)/coach/page.tsx"
      provides: "Coach dashboard page with submission queue"
      min_lines: 40
    - path: "src/app/(dashboard)/coach/submissions/[submissionId]/page.tsx"
      provides: "Submission detail page for coach review"
      min_lines: 60
    - path: "src/components/coach/SubmissionQueue.tsx"
      provides: "Submission list component with filtering"
      min_lines: 50
    - path: "src/components/coach/SubmissionCard.tsx"
      provides: "Individual submission card in queue"
      min_lines: 30
  key_links:
    - from: "src/app/(dashboard)/coach/page.tsx"
      to: "/api/submissions"
      via: "fetch for submission list"
      pattern: "fetch.*api/submissions"
    - from: "src/components/coach/SubmissionQueue.tsx"
      to: "SubmissionCard"
      via: "renders submission cards"
      pattern: "SubmissionCard"
---

<objective>
Build the coach dashboard with submission queue and detail view.

Purpose: Coaches need a dedicated area to see student submissions awaiting review. The queue shows pending items with key info; clicking opens a detail view with full context.

Output: Coach dashboard page with SubmissionQueue component and individual SubmissionCard components. Submission detail page showing full submission with student info.
</objective>

<execution_context>
@/home/sheldon/.claude/get-shit-done/workflows/execute-plan.md
@/home/sheldon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# API from Plan 01
@.planning/phases/07-coach-workflow/07-01-PLAN.md

# UI patterns
@src/app/(dashboard)/dashboard/page.tsx
@src/app/(dashboard)/courses/[courseId]/page.tsx
@src/lib/auth.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create coach dashboard page with submission queue</name>
  <files>
    src/app/(dashboard)/coach/page.tsx
    src/components/coach/SubmissionQueue.tsx
    src/components/coach/SubmissionCard.tsx
  </files>
  <action>
Create `src/app/(dashboard)/coach/page.tsx`:
- Server component that checks hasMinimumRole("coach")
- Redirect to dashboard if not coach/admin
- Page title "Coach Dashboard"
- Render SubmissionQueue client component
- Dark mode styling consistent with existing dashboard

Create `src/components/coach/SubmissionQueue.tsx`:
- Client component ("use client")
- Fetch submissions from /api/submissions on mount
- State: submissions array, loading boolean, status filter
- Filter tabs: "Pending" (default), "Reviewed", "All"
- Loading state with skeleton cards
- Empty state: "No submissions to review"
- Map submissions to SubmissionCard components
- Style: dark theme, card grid layout

Create `src/components/coach/SubmissionCard.tsx`:
- Props: submission object (with student, lesson, interaction info)
- Display:
  - Student name
  - Lesson title
  - Submission type badge (text/audio)
  - AI score with color coding (red <70, yellow 70-85, green >85)
  - Time since submission (e.g., "2 hours ago")
  - Status badge if reviewed
- Click handler: navigate to /coach/submissions/[submissionId]
- Style: dark card with hover effect, consistent with course cards

Use existing UI components:
- Badge from shadcn/ui for status/type indicators
- Card styling consistent with dashboard
- lucide-react icons (MessageSquare for text, Mic for audio)
  </action>
  <verify>
TypeScript compiles:
```bash
npx tsc --noEmit
```

Files exist:
```bash
ls -la src/app/\(dashboard\)/coach/
ls -la src/components/coach/
```
  </verify>
  <done>
- Coach dashboard page renders SubmissionQueue
- SubmissionQueue fetches and displays submissions
- SubmissionCard shows key info with type/status badges
- Filter tabs for pending/reviewed/all
- Click navigates to detail page
  </done>
</task>

<task type="auto">
  <name>Task 2: Create submission detail page</name>
  <files>
    src/app/(dashboard)/coach/submissions/[submissionId]/page.tsx
  </files>
  <action>
Create `src/app/(dashboard)/coach/submissions/[submissionId]/page.tsx`:
- Server component
- Check hasMinimumRole("coach"), redirect if not authorized
- Fetch submission details from /api/submissions/[submissionId]
- Display 404 page if submission not found

Layout:
- Back button to /coach
- Page title: "Review Submission"
- Two-column layout (desktop), stacked on mobile

Left column - Submission Details:
- Student info card: name, email
- Lesson info: title, module name
- Interaction prompt shown
- Submission content:
  - For text: show the response in a styled block
  - For audio: audio player element with playback controls
- AI grading section:
  - Score with visual indicator
  - AI feedback text

Right column - Coach Actions (placeholder for Plan 03):
- Empty section with "Coach feedback coming in next plan" comment
- Will be replaced with notes and Loom feedback form

Style:
- Dark theme consistent with rest of app
- Card-based layout
- Proper spacing and typography
  </action>
  <verify>
TypeScript compiles:
```bash
npx tsc --noEmit
```

File exists:
```bash
ls -la "src/app/(dashboard)/coach/submissions/[submissionId]/"
```
  </verify>
  <done>
- Submission detail page displays full submission info
- Shows student info, lesson context, submission content
- Audio submissions have playback controls
- AI grading score and feedback displayed
- Right column placeholder for coach actions
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `npx tsc --noEmit`
2. Coach dashboard accessible at /coach
3. Submission detail page accessible at /coach/submissions/[id]
4. Components render properly with dark theme
</verification>

<success_criteria>
- [ ] Coach sees queue of pending submissions at /coach
- [ ] Queue shows student name, lesson, type, AI score for each
- [ ] Filter tabs work for pending/reviewed/all
- [ ] Clicking submission navigates to detail page
- [ ] Detail page shows full submission with audio playback if applicable
- [ ] Non-coach users redirected from /coach pages
</success_criteria>

<output>
After completion, create `.planning/phases/07-coach-workflow/07-02-SUMMARY.md`
</output>
