---
phase: 37-app-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema/users.ts
  - src/app/api/user/preferences/route.ts
  - middleware.ts
  - src/components/ui/sidebar.tsx
  - src/components/ui/tooltip.tsx
  - src/components/ui/separator.tsx
  - src/components/ui/collapsible.tsx
  - src/components/ui/dropdown-menu.tsx
  - src/components/ui/avatar.tsx
autonomous: true

must_haves:
  truths:
    - "Users table has dailyGoalXp and timezone columns with sensible defaults"
    - "Preferences API accepts and returns dailyGoalXp and timezone fields"
    - "The /settings route is protected by middleware (redirects unauthenticated users)"
    - "shadcn/ui sidebar component files exist in src/components/ui/"
  artifacts:
    - path: "src/db/schema/users.ts"
      provides: "dailyGoalXp (integer, default 100) and timezone (text, default UTC) columns"
      contains: "dailyGoalXp"
    - path: "src/app/api/user/preferences/route.ts"
      provides: "GET/PATCH for languagePreference, dailyGoalXp, timezone"
      exports: ["GET", "PATCH"]
    - path: "src/components/ui/sidebar.tsx"
      provides: "shadcn/ui sidebar primitives (SidebarProvider, Sidebar, SidebarContent, etc.)"
      contains: "SidebarProvider"
    - path: "middleware.ts"
      provides: "Protected route matcher including /settings"
      contains: "settings"
  key_links:
    - from: "src/app/api/user/preferences/route.ts"
      to: "src/db/schema/users.ts"
      via: "Drizzle query using dailyGoalXp and timezone columns"
      pattern: "dailyGoalXp|timezone"
---

<objective>
Install the shadcn/ui sidebar component, extend the users schema with settings columns, expand the preferences API to handle new fields, and update middleware to protect the /settings route.

Purpose: Establishes the foundation that Plans 02-04 build on -- sidebar primitives for the layout, schema columns for the settings page, and route protection for the settings URL.
Output: sidebar.tsx + supporting UI primitives installed, users table extended, preferences API expanded, middleware updated.
</objective>

<execution_context>
@/home/sheldon/.claude/get-shit-done/workflows/execute-plan.md
@/home/sheldon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-app-shell/37-RESEARCH.md

Key files:
@src/db/schema/users.ts
@src/app/api/user/preferences/route.ts
@middleware.ts
@components.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn/ui sidebar and extend users schema</name>
  <files>
    src/components/ui/sidebar.tsx
    src/components/ui/tooltip.tsx
    src/components/ui/separator.tsx
    src/components/ui/collapsible.tsx
    src/components/ui/dropdown-menu.tsx
    src/components/ui/avatar.tsx
    src/db/schema/users.ts
  </files>
  <action>
    1. Install the shadcn/ui sidebar component via CLI:
       ```bash
       npx shadcn@latest add sidebar --yes
       ```
       This installs `sidebar.tsx` and any missing dependencies (tooltip, separator, collapsible, dropdown-menu, avatar). If any sub-component already exists (e.g., sheet.tsx), the CLI will skip it.

    2. Verify the sidebar.tsx file was created at `src/components/ui/sidebar.tsx` and contains exports for `SidebarProvider`, `Sidebar`, `SidebarContent`, `SidebarHeader`, `SidebarFooter`, `SidebarGroup`, `SidebarGroupLabel`, `SidebarGroupContent`, `SidebarMenu`, `SidebarMenuItem`, `SidebarMenuButton`, `SidebarInset`, `SidebarTrigger`, `SidebarRail`.

    3. Add two new columns to the `users` table in `src/db/schema/users.ts`:
       - `dailyGoalXp: integer("daily_goal_xp").notNull().default(100)` — Default 100 = "Regular" tier. Phase 39 will define exact tier values (50/100/150/250).
       - `timezone: text("timezone").notNull().default("UTC")` — IANA timezone string.

       Import `integer` from `drizzle-orm/pg-core` (text and others are already imported).

    4. Generate the Drizzle migration:
       ```bash
       npm run db:generate
       ```

    5. Push the migration to the database:
       ```bash
       npm run db:push
       ```
  </action>
  <verify>
    - `ls src/components/ui/sidebar.tsx` confirms sidebar component exists
    - `grep "SidebarProvider" src/components/ui/sidebar.tsx` confirms key export
    - `grep "dailyGoalXp" src/db/schema/users.ts` confirms new column
    - `grep "timezone" src/db/schema/users.ts` confirms new column
    - `npm run db:generate` completes without errors
  </verify>
  <done>
    sidebar.tsx and all supporting UI primitives installed; users table has dailyGoalXp (integer, default 100) and timezone (text, default "UTC") columns; Drizzle migration generated.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend preferences API and update middleware</name>
  <files>
    src/app/api/user/preferences/route.ts
    middleware.ts
  </files>
  <action>
    1. Extend `src/app/api/user/preferences/route.ts`:

       **GET handler** — Add `dailyGoalXp` and `timezone` to the query columns and response:
       ```typescript
       columns: {
         id: true,
         languagePreference: true,
         dailyGoalXp: true,
         timezone: true,
       },
       ```
       Return all three fields in the JSON response.

       **PATCH handler** — Accept optional `dailyGoalXp` (integer) and `timezone` (string) in the request body alongside existing `languagePreference`:
       - Validate `dailyGoalXp`: if provided, must be a positive integer (> 0). Return 400 if invalid.
       - Validate `timezone`: if provided, must be a non-empty string. (Full IANA validation is optional; just ensure it's a string.)
       - Validate `languagePreference`: keep existing validation.
       - Update only the fields that are provided in the request body (use spread with conditional inclusion, matching existing pattern).
       - Return all three fields in the response.

    2. Update `middleware.ts`:
       Add `/settings(.*)` to the `isProtectedRoute` matcher array. Also add `/my-conversations(.*)` and `/my-feedback(.*)` if not already present (they appear to be missing from the current matcher but exist as pages).

       The updated `isProtectedRoute`:
       ```typescript
       const isProtectedRoute = createRouteMatcher([
         "/dashboard(.*)",
         "/courses(.*)",
         "/lessons(.*)",
         "/practice(.*)",
         "/settings(.*)",
         "/my-conversations(.*)",
         "/my-feedback(.*)",
       ]);
       ```
  </action>
  <verify>
    - `grep "dailyGoalXp" src/app/api/user/preferences/route.ts` confirms new field in API
    - `grep "timezone" src/app/api/user/preferences/route.ts` confirms new field in API
    - `grep "settings" middleware.ts` confirms /settings route is protected
    - `npx tsc --noEmit` passes (or at least no new errors from these files)
  </verify>
  <done>
    Preferences API returns and accepts dailyGoalXp + timezone alongside languagePreference. Middleware protects /settings, /my-conversations, and /my-feedback routes.
  </done>
</task>

</tasks>

<verification>
1. `ls src/components/ui/sidebar.tsx` — sidebar primitives exist
2. `grep "SidebarProvider" src/components/ui/sidebar.tsx` — key export present
3. `grep "dailyGoalXp" src/db/schema/users.ts` — schema column exists
4. `grep "timezone" src/db/schema/users.ts` — schema column exists
5. `grep "dailyGoalXp" src/app/api/user/preferences/route.ts` — API handles new field
6. `grep "settings" middleware.ts` — route protected
</verification>

<success_criteria>
- shadcn/ui sidebar component and all sub-components installed in src/components/ui/
- users table has dailyGoalXp (integer, default 100) and timezone (text, default "UTC") columns
- Drizzle migration generated for new columns
- Preferences API GET returns all three preference fields
- Preferences API PATCH accepts and validates all three fields
- Middleware protects /settings route
</success_criteria>

<output>
After completion, create `.planning/phases/37-app-shell/37-01-SUMMARY.md`
</output>
