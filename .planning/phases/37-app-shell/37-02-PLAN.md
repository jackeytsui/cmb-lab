---
phase: 37-app-shell
plan: 02
type: execute
wave: 2
depends_on: ["37-01"]
files_modified:
  - src/app/(dashboard)/layout.tsx
  - src/components/layout/AppSidebar.tsx
  - src/components/layout/NavMain.tsx
  - src/components/layout/NavUser.tsx
  - src/app/not-found.tsx
autonomous: true

must_haves:
  truths:
    - "Every page under (dashboard) renders inside a persistent sidebar layout"
    - "Sidebar shows role-appropriate navigation (student Learning, coach Coach Tools, admin Admin)"
    - "Current page is highlighted in the sidebar"
    - "Mobile viewport shows hamburger trigger that opens sidebar as offcanvas sheet"
    - "Sidebar collapses to icon-only mode and persists open/closed state across page loads"
    - "Navigating to a non-existent route shows a branded 404 page with link to dashboard"
  artifacts:
    - path: "src/app/(dashboard)/layout.tsx"
      provides: "SidebarProvider + AppSidebar + SidebarInset wrapping all dashboard pages"
      contains: "SidebarProvider"
      min_lines: 20
    - path: "src/components/layout/AppSidebar.tsx"
      provides: "Main sidebar component with role-filtered nav sections, header, footer"
      contains: "AppSidebar"
      min_lines: 40
    - path: "src/components/layout/NavMain.tsx"
      provides: "Navigation item renderer with active state via usePathname"
      contains: "usePathname"
    - path: "src/components/layout/NavUser.tsx"
      provides: "User info section in sidebar footer with settings link and sign-out"
      contains: "NavUser"
    - path: "src/app/not-found.tsx"
      provides: "Branded 404 page with link back to /dashboard"
      contains: "404"
  key_links:
    - from: "src/app/(dashboard)/layout.tsx"
      to: "src/components/layout/AppSidebar.tsx"
      via: "Imports and renders AppSidebar with role prop"
      pattern: "AppSidebar.*role"
    - from: "src/app/(dashboard)/layout.tsx"
      to: "src/components/ui/sidebar.tsx"
      via: "Imports SidebarProvider and SidebarInset"
      pattern: "SidebarProvider|SidebarInset"
    - from: "src/components/layout/NavMain.tsx"
      to: "src/components/ui/sidebar.tsx"
      via: "Uses SidebarMenuButton with isActive prop"
      pattern: "SidebarMenuButton.*isActive"
---

<objective>
Create the dashboard layout with SidebarProvider, build the AppSidebar with role-based navigation sections, implement active state highlighting, and add a branded 404 page.

Purpose: This is the core of Phase 37 -- wrapping all authenticated pages in a persistent sidebar layout. After this plan, the sidebar renders on every page (though pages still have their old wrappers until Plan 04 migrates them).
Output: Dashboard layout.tsx, AppSidebar, NavMain, NavUser, and not-found.tsx.
</objective>

<execution_context>
@/home/sheldon/.claude/get-shit-done/workflows/execute-plan.md
@/home/sheldon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-app-shell/37-RESEARCH.md
@.planning/phases/37-app-shell/37-01-SUMMARY.md

Key files:
@src/components/ui/sidebar.tsx
@src/lib/auth.ts
@src/types/globals.d.ts
@src/components/layout/AppHeader.tsx
@src/app/(dashboard)/error.tsx
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dashboard layout with SidebarProvider</name>
  <files>
    src/app/(dashboard)/layout.tsx
    src/components/layout/NavUser.tsx
  </files>
  <action>
    1. Create `src/app/(dashboard)/layout.tsx` as a server component:
       - Import `cookies` from `next/headers`
       - Import `auth` from `@clerk/nextjs/server`
       - Import `redirect` from `next/navigation`
       - Import `SidebarProvider`, `SidebarInset`, `SidebarTrigger` from `@/components/ui/sidebar`
       - Import `AppSidebar` from `@/components/layout/AppSidebar`
       - Import `Separator` from `@/components/ui/separator`
       - Import `SearchBar` from `@/components/search/SearchBar`
       - Import `NotificationBell` from `@/components/notifications/NotificationBell`
       - Get userId and sessionClaims from `await auth()`. If no userId, `redirect("/sign-in")`.
       - Extract role: `(sessionClaims?.metadata?.role || "student") as Roles`
       - Read sidebar cookie: `const cookieStore = await cookies(); const defaultOpen = cookieStore.get("sidebar_state")?.value !== "false";` (default to open)
       - Render layout structure:
         ```
         <SidebarProvider defaultOpen={defaultOpen}>
           <AppSidebar role={role} />
           <SidebarInset>
             <header className="flex h-14 shrink-0 items-center gap-2 border-b border-zinc-800 px-4">
               <SidebarTrigger className="-ml-1 text-zinc-400 hover:text-white" />
               <Separator orientation="vertical" className="mr-2 h-4 bg-zinc-700" />
               <div className="flex-1" />
               <div className="flex items-center gap-3">
                 <SearchBar />
                 <NotificationBell />
               </div>
             </header>
             <main className="flex-1 overflow-auto">
               {children}
             </main>
           </SidebarInset>
         </SidebarProvider>
         ```
       - The header replaces what AppHeader used to provide (search bar, notification bell). The UserButton moves into the sidebar footer (NavUser).

    2. Create `src/components/layout/NavUser.tsx` as a client component (`"use client"`):
       - Import `UserButton` from `@clerk/nextjs`
       - Import `SidebarFooter`, `SidebarMenu`, `SidebarMenuItem`, `SidebarMenuButton` from `@/components/ui/sidebar`
       - Import `Settings` icon from `lucide-react`
       - Import `Link` from `next/link`
       - Render inside `<SidebarFooter>`:
         - A `SidebarMenu` with two items:
           1. Settings link: `<SidebarMenuButton asChild tooltip="Settings"><Link href="/settings"><Settings /><span>Settings</span></Link></SidebarMenuButton>`
           2. User button: A div containing `<UserButton afterSignOutUrl="/sign-in" />` with the label "Account"
       - Accept no special props (Clerk UserButton handles user data internally).
  </action>
  <verify>
    - `grep "SidebarProvider" src/app/\(dashboard\)/layout.tsx` confirms layout uses SidebarProvider
    - `grep "AppSidebar" src/app/\(dashboard\)/layout.tsx` confirms sidebar is rendered
    - `grep "SidebarFooter" src/components/layout/NavUser.tsx` confirms footer component exists
    - `npx tsc --noEmit 2>&1 | head -20` — check for type errors
  </verify>
  <done>
    Dashboard layout wraps all (dashboard) pages in SidebarProvider + AppSidebar + SidebarInset. Header bar provides SidebarTrigger, SearchBar, and NotificationBell. NavUser provides Settings link and UserButton in sidebar footer.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build AppSidebar with role-based navigation and 404 page</name>
  <files>
    src/components/layout/AppSidebar.tsx
    src/components/layout/NavMain.tsx
    src/app/not-found.tsx
  </files>
  <action>
    1. Create `src/components/layout/NavMain.tsx` as a client component (`"use client"`):
       - Import `usePathname` from `next/navigation`
       - Import `Link` from `next/link`
       - Import sidebar primitives: `SidebarGroup`, `SidebarGroupLabel`, `SidebarGroupContent`, `SidebarMenu`, `SidebarMenuItem`, `SidebarMenuButton`
       - Import `type LucideIcon` from `lucide-react`
       - Define types:
         ```typescript
         type NavItem = { title: string; url: string; icon: LucideIcon };
         type NavSection = { label: string; items: NavItem[] };
         ```
       - Props: `{ sections: NavSection[] }`
       - Use `usePathname()` to get current route
       - Active state logic: `pathname === item.url || (item.url !== "/dashboard" && pathname.startsWith(item.url + "/"))`. Special case: `/dashboard` should only match exactly or `/dashboard/practice` prefix to avoid highlighting it for every nested route.
       - Render each section as a `<SidebarGroup>` with `<SidebarGroupLabel>{section.label}</SidebarGroupLabel>` and items as `<SidebarMenuButton asChild isActive={isActive} tooltip={item.title}>`.

    2. Create `src/components/layout/AppSidebar.tsx` as a client component (`"use client"`):
       - Import `Sidebar`, `SidebarContent`, `SidebarHeader`, `SidebarRail` from `@/components/ui/sidebar`
       - Import `NavMain` from `./NavMain`
       - Import `NavUser` from `./NavUser`
       - Import Lucide icons: `Home`, `BookOpen`, `ClipboardList`, `MessageSquare`, `MessageCircle`, `Users`, `Inbox`, `Mic`, `Shield`, `BarChart3`, `FileText`, `Megaphone`, `Upload`, `Brain`, `Sparkles`, `GraduationCap`
       - Import type `Roles` from `@/types/globals`
       - Props: `{ role: Roles }`
       - Define navigation data as an array of sections, each with a `minRole`:
         ```
         Learning (minRole: "student"):
           - Dashboard → /dashboard (Home icon)
           - My Courses → /courses (BookOpen icon) -- NOTE: links to /courses not /dashboard/courses
           - Practice → /dashboard/practice (ClipboardList icon)
           - Conversations → /my-conversations (MessageSquare icon)
           - Feedback → /my-feedback (MessageCircle icon)

         Coach Tools (minRole: "coach"):
           - Coach Dashboard → /coach (Users icon)
           - Submissions → /coach/submissions (Inbox icon) -- NOTE: check if this route exists; if coach page is the main queue, may just be /coach
           - Pronunciation → /coach/pronunciation (Mic icon)
           - Conversations → /coach/conversations (MessageSquare icon)
           - Students → /coach/students (Users icon)

         Admin (minRole: "admin"):
           - Admin Dashboard → /admin (Shield icon)
           - Courses → /admin/courses (BookOpen icon)
           - Exercises → /admin/exercises (ClipboardList icon)
           - Content → /admin/content (Upload icon)
           - Knowledge Base → /admin/knowledge (Brain icon)
           - AI Prompts → /admin/prompts (Sparkles icon)
           - AI Logs → /admin/ai-logs (FileText icon)
           - Analytics → /admin/analytics (BarChart3 icon)
           - Students → /admin/students (GraduationCap icon)
           - GHL Settings → /admin/ghl (Megaphone icon)
         ```
       - Filter sections by role hierarchy: `const roleHierarchy: Roles[] = ["student", "coach", "admin"]; const userLevel = roleHierarchy.indexOf(role);` Then filter to sections where `userLevel >= roleHierarchy.indexOf(section.minRole)`.
       - Map filtered sections to the format NavMain expects (drop the minRole field).
       - Render:
         ```
         <Sidebar collapsible="icon" className="border-r border-zinc-800">
           <SidebarHeader className="p-4">
             <Link href="/dashboard" className="flex items-center gap-2 group-data-[collapsible=icon]:justify-center">
               <GraduationCap className="h-6 w-6 text-cyan-400 shrink-0" />
               <span className="font-bold text-lg text-white group-data-[collapsible=icon]:hidden">CantoMando</span>
             </Link>
           </SidebarHeader>
           <SidebarContent>
             <NavMain sections={filteredSections} />
           </SidebarContent>
           <NavUser />
           <SidebarRail />
         </Sidebar>
         ```

    3. Create `src/app/not-found.tsx`:
       - A simple server component (no "use client")
       - Import `Link` from `next/link`
       - Import `Home` from `lucide-react`
       - Dark themed (bg-zinc-900) centered layout with:
         - Large "404" heading in zinc-600
         - "Page Not Found" subtitle in white
         - Description text in zinc-400: "The page you're looking for doesn't exist or has been moved."
         - A cyan button linking to `/dashboard` with Home icon and "Back to Dashboard" text
       - Export metadata: `export const metadata = { title: "Page Not Found - CantoMando" };`
  </action>
  <verify>
    - `grep "usePathname" src/components/layout/NavMain.tsx` confirms active state detection
    - `grep "isActive" src/components/layout/NavMain.tsx` confirms active prop usage
    - `grep "minRole" src/components/layout/AppSidebar.tsx` confirms role filtering
    - `grep "collapsible" src/components/layout/AppSidebar.tsx` confirms icon collapse mode
    - `grep "404" src/app/not-found.tsx` confirms branded 404 page
    - `npx tsc --noEmit 2>&1 | head -20` — check for type errors
  </verify>
  <done>
    AppSidebar renders role-filtered navigation sections with active state highlighting. NavMain handles pathname matching. Sidebar has CantoMando brand in header, collapses to icon mode, and includes SidebarRail for resize. Branded 404 page shows at /any-nonexistent-route.
  </done>
</task>

</tasks>

<verification>
1. Start dev server: `npm run dev`
2. Visit `/dashboard` — sidebar should appear on the left with Learning section
3. Click sidebar collapse button — sidebar should collapse to icon-only mode with tooltips
4. Refresh page — sidebar should remember collapsed/expanded state (cookie persistence)
5. Resize browser to mobile width (<768px) — sidebar trigger should appear as hamburger, tapping opens offcanvas sheet
6. Visit as coach role — sidebar should show Learning AND Coach Tools sections
7. Visit as admin role — sidebar should show all three sections
8. Visit `/nonexistent-route` — should see branded 404 page with "Back to Dashboard" link
9. Click through nav items — current page should be highlighted
10. NOTE: Pages will show double headers (old AppHeader + new layout header) until Plan 04 migrates them. This is expected and temporary.
</verification>

<success_criteria>
- Dashboard layout.tsx exists with SidebarProvider wrapping all (dashboard) pages
- AppSidebar shows role-filtered navigation sections (Learning, Coach Tools, Admin)
- NavMain highlights the active page using usePathname
- Sidebar collapses to icon-only mode via SidebarRail or collapse button
- Sidebar persists open/closed state via cookie
- Mobile viewports get offcanvas sheet (built into shadcn/ui sidebar)
- NavUser shows Settings link and Clerk UserButton in sidebar footer
- Branded 404 page renders for non-existent routes
</success_criteria>

<output>
After completion, create `.planning/phases/37-app-shell/37-02-SUMMARY.md`
</output>
