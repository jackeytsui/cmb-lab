---
phase: 57-builder-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema/video-threads.ts
  - src/db/migrations/0021_add_step_positions.sql
  - src/db/migrations/meta/_journal.json
  - src/app/api/admin/video-threads/[threadId]/steps/route.ts
  - src/app/(dashboard)/admin/video-threads/[threadId]/builder/page.tsx
  - src/components/video-thread/FlowEditor.tsx
autonomous: true

must_haves:
  truths:
    - "Coach clicks Save, sees a Saving spinner, and gets a success toast"
    - "Node positions persist to the database and restore on page reload"
    - "Logic rules (logicRules, fallbackStepId) persist through save and reload"
    - "Visual edges reconstruct correctly from DB data after page reload"
  artifacts:
    - path: "src/db/schema/video-threads.ts"
      provides: "positionX and positionY columns on video_thread_steps"
      contains: "positionX"
    - path: "src/db/migrations/0021_add_step_positions.sql"
      provides: "Migration to add position columns"
      contains: "ALTER TABLE"
    - path: "src/app/api/admin/video-threads/[threadId]/steps/route.ts"
      provides: "PUT handler that persists logicRules, fallbackStepId, positionX, positionY"
      contains: "logicRules"
    - path: "src/components/video-thread/FlowEditor.tsx"
      provides: "onSave callback that includes node positions in save payload; useMemo that reconstructs edges from step logicRules and fallbackStepId"
      contains: "positionX"
  key_links:
    - from: "src/components/video-thread/FlowEditor.tsx"
      to: "builder/page.tsx"
      via: "onSave callback passing node positions"
      pattern: "onSave|getNodePositions"
    - from: "builder/page.tsx"
      to: "/api/admin/video-threads/[threadId]/steps"
      via: "PUT fetch with positions + logicRules + fallbackStepId"
      pattern: "positionX.*positionY"
    - from: "/api/admin/video-threads/[threadId]/steps"
      to: "video_thread_steps table"
      via: "Drizzle upsert with position columns"
      pattern: "logicRules.*fallbackStepId.*positionX.*positionY"
    - from: "src/components/video-thread/FlowEditor.tsx"
      to: "React Flow edges"
      via: "useMemo that builds edges from step.logicRules + step.fallbackStepId"
      pattern: "useMemo.*logicRules|fallbackStepId.*Edge"
---

<objective>
Persist node layout and logic routing data through save/reload cycle.

Purpose: FLOW-08 (positions persist), FLOW-09 (save button with spinner/toast), FLOW-10 (edges map to DB columns). Without this, all work in the builder is lost on page refresh.

Output: Working save/load cycle where coach arranges nodes, configures logic rules, clicks Save, and sees the exact same layout and connections on reload.
</objective>

<execution_context>
@/Users/sheldonho/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sheldonho/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-flow-editor-node-ux/56-02-SUMMARY.md
@src/db/schema/video-threads.ts
@src/app/api/admin/video-threads/[threadId]/steps/route.ts
@src/components/video-thread/FlowEditor.tsx
@src/app/(dashboard)/admin/video-threads/[threadId]/builder/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add position columns to DB schema and update API</name>
  <files>
    src/db/schema/video-threads.ts
    src/db/migrations/0021_add_step_positions.sql
    src/db/migrations/meta/_journal.json
    src/app/api/admin/video-threads/[threadId]/steps/route.ts
  </files>
  <action>
1. In `src/db/schema/video-threads.ts`, add two integer columns to `videoThreadSteps`:
   - `positionX: integer("position_x").notNull().default(0)`
   - `positionY: integer("position_y").notNull().default(150)`
   Place them after the `sortOrder` column. These store the React Flow node x,y positions.

2. Create a hand-written SQL migration at `src/db/migrations/0021_add_step_positions.sql`:
   ```sql
   ALTER TABLE "video_thread_steps" ADD COLUMN "position_x" integer NOT NULL DEFAULT 0;
   ALTER TABLE "video_thread_steps" ADD COLUMN "position_y" integer NOT NULL DEFAULT 150;
   ```

3. Update `src/db/migrations/meta/_journal.json` to add a new entry for migration 0021. Follow the existing pattern -- add an object with `idx: 21`, `version: "7"`, `when: <current unix timestamp>`, `tag: "0021_add_step_positions"`, `breakpoints: true`.

4. In `src/app/api/admin/video-threads/[threadId]/steps/route.ts`, update the PUT handler's upsert to also persist these fields in BOTH the insert values and the onConflictDoUpdate set:
   - `logicRules: step.logicRules` (currently missing from both insert and set)
   - `fallbackStepId: step.fallbackStepId` (currently missing from both insert and set)
   - `positionX: step.positionX ?? 0` (new)
   - `positionY: step.positionY ?? 150` (new)

   Also update the GET handler to include these new fields (they come automatically from the SELECT *, but verify the query returns all columns including the new ones).

5. Update the `PlayerStep` type: In `src/types/video-thread-player.ts`, the PlayerStep extends VideoThreadStep which will automatically include the new columns since they're inferred from the schema. No changes needed there UNLESS the Omit clause strips them -- verify and fix if needed.
  </action>
  <verify>
    - `npx tsc --noEmit` passes with no new errors
    - The migration SQL file exists and has correct ALTER TABLE statements
    - The PUT handler code includes logicRules, fallbackStepId, positionX, positionY in both the `values` object AND the `onConflictDoUpdate.set` object (grep for both occurrences to confirm)
    - Verify GET handler returns all columns including positionX, positionY, logicRules, fallbackStepId (check the select/query)
  </verify>
  <done>
    video_thread_steps schema has positionX and positionY columns; PUT API persists logicRules, fallbackStepId, and position data; migration SQL ready to apply
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire FlowEditor positions into save/load cycle</name>
  <files>
    src/components/video-thread/FlowEditor.tsx
    src/app/(dashboard)/admin/video-threads/[threadId]/builder/page.tsx
  </files>
  <action>
1. **FlowEditor -- expose node positions:**
   Add a new callback prop `onPositionsChange?: (positions: Record<string, { x: number; y: number }>) => void` to `FlowEditorProps`.

   In `onNodesChange`, after calling `applyNodeChanges`, also call `onPositionsChange` with a map of nodeId -> { x, y } extracted from the updated localNodes. Debounce or fire on every drag end -- simplest approach is to fire in `onNodesChange` when a position change is detected. Filter changes for `type === 'position'` and `dragging === false` to only fire when drag ends.

2. **FlowEditor -- use persisted positions on load:**
   In the `useMemo` that converts steps to nodes, use `step.positionX` and `step.positionY` as the initial position instead of the computed `index * 450, 150`. Only fall back to `index * 450, 150` when positionX is 0 and positionY is the default (indicating never-saved positions).

   **CREATE a new useEffect** for position preservation (this does NOT exist yet -- it must be built from scratch). This useEffect should sync props-to-local-state when the `steps` prop changes: for each incoming step, check if a localNode already exists at that ID with a user-dragged position (i.e., the node was moved since mount). If so, keep the local position. Otherwise, use the step's DB position (`step.positionX`, `step.positionY`). Depend on a stable key like `steps.length` or a serialized step-ids string, NOT the `steps` array reference directly (to avoid infinite re-renders).

3. **Builder page -- integrate positions into save:**
   Add a `const [nodePositions, setNodePositions] = useState<Record<string, { x: number; y: number }>>({})` state variable.

   Pass `onPositionsChange={setNodePositions}` to `<FlowEditor>`.

   In `handleSave`, merge positions into steps before sending:
   ```typescript
   const stepsWithPositions = steps.map(step => ({
     ...step,
     positionX: Math.round(nodePositions[step.id]?.x ?? step.positionX ?? step.sortOrder * 450),
     positionY: Math.round(nodePositions[step.id]?.y ?? step.positionY ?? 150),
   }));
   ```
   Send `stepsWithPositions` instead of `steps` in the PUT body.

4. **Builder page -- load persisted positions on fetch:**
   After fetching steps in the useEffect, populate `nodePositions` from the step data:
   ```typescript
   const initialPositions: Record<string, { x: number; y: number }> = {};
   data.steps.forEach(step => {
     if (step.positionX !== undefined) {
       initialPositions[step.id] = { x: step.positionX, y: step.positionY };
     }
   });
   setNodePositions(initialPositions);
   ```

5. **Verify save button UX:** The builder page already has `saving` state with "Saving..." spinner and `toast.success()`. Confirm these work correctly. Also confirm `toast.error()` fires on failure. No changes needed here since it's already implemented -- just verify.
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - `npm run build` succeeds (next build)
    - FlowEditor has `onPositionsChange` prop
    - Builder page sends positionX/positionY in PUT body
    - Steps loaded from API use positionX/positionY for initial node positions in FlowEditor
  </verify>
  <done>
    Coach drags nodes on canvas; positions are tracked in state; Save button sends positions + logicRules + fallbackStepId to API; page reload restores exact node layout and edge connections from DB
  </done>
</task>

</tasks>

<verification>
1. Confirm `npx tsc --noEmit` passes with no new errors
2. Confirm `npm run build` succeeds
3. Verify migration SQL file has correct ALTER TABLE statements
4. Verify PUT endpoint persists logicRules, fallbackStepId, positionX, positionY
5. Verify FlowEditor reads positionX/positionY from step data for initial node positions
6. Verify builder page merges node positions into save payload
</verification>

<success_criteria>
- Schema has positionX/positionY columns with defaults
- API persists all routing + position fields on save
- FlowEditor positions are bidirectionally synced: load from DB, save back to DB
- Save button shows "Saving..." spinner and success/error toast (already works, just verify)
- Edges reconstruct from logicRules/fallbackStepId/logic after reload
</success_criteria>

<output>
After completion, create `.planning/phases/57-builder-completion/57-01-SUMMARY.md`
</output>
