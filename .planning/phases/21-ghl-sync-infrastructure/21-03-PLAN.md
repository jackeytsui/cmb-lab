---
phase: 21-ghl-sync-infrastructure
plan: 03
type: execute
wave: 3
depends_on: ["21-02"]
files_modified:
  - src/app/(admin)/admin/ghl/page.tsx
  - src/app/(admin)/admin/ghl/components/ConnectionStatus.tsx
  - src/app/(admin)/admin/ghl/components/FieldMappingTable.tsx
  - src/app/(admin)/admin/ghl/components/SyncEventLog.tsx
  - src/app/api/admin/ghl/sync-events/route.ts
autonomous: true

must_haves:
  truths:
    - "Admin can see GHL connection status (connected/disconnected) on a settings page"
    - "Admin can add, edit, and remove custom field mappings without touching code"
    - "Admin can view recent sync events in a filterable log table for debugging"
    - "Admin page is accessible from the admin navigation"
  artifacts:
    - path: "src/app/(admin)/admin/ghl/page.tsx"
      provides: "Admin GHL settings page with connection test, field mappings, and sync log"
      min_lines: 40
    - path: "src/app/(admin)/admin/ghl/components/FieldMappingTable.tsx"
      provides: "CRUD table for GHL custom field mappings"
      min_lines: 50
    - path: "src/app/(admin)/admin/ghl/components/SyncEventLog.tsx"
      provides: "Read-only sync event log with direction and status filters"
      min_lines: 40
    - path: "src/app/api/admin/ghl/sync-events/route.ts"
      provides: "GET endpoint for sync events with pagination and filtering"
      exports: ["GET"]
  key_links:
    - from: "src/app/(admin)/admin/ghl/components/ConnectionStatus.tsx"
      to: "/api/admin/ghl/test-connection"
      via: "fetch POST to test GHL connection"
      pattern: "fetch.*test-connection"
    - from: "src/app/(admin)/admin/ghl/components/FieldMappingTable.tsx"
      to: "/api/admin/ghl/field-mappings"
      via: "fetch GET/POST/DELETE for CRUD"
      pattern: "fetch.*field-mappings"
    - from: "src/app/(admin)/admin/ghl/components/SyncEventLog.tsx"
      to: "/api/admin/ghl/sync-events"
      via: "fetch GET with query params for filtering"
      pattern: "fetch.*sync-events"
---

<objective>
Build the admin GHL settings page with connection testing, custom field mapping management, and sync event log viewer.

Purpose: This page lets the admin configure the GHL integration (map custom fields to LMS concepts, verify connection) and debug sync issues (view event log). It satisfies success criteria #2 (field mapping without code changes) and #5 (queryable event table for debugging).
Output: Full admin UI page at /admin/ghl with three sections: connection status, field mappings CRUD, and sync event log.
</objective>

<execution_context>
@/home/sheldon/.claude/get-shit-done/workflows/execute-plan.md
@/home/sheldon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Prior plan context:
@.planning/phases/21-ghl-sync-infrastructure/21-01-SUMMARY.md
@.planning/phases/21-ghl-sync-infrastructure/21-02-SUMMARY.md

Key codebase files for UI patterns:
@src/app/(admin)/admin/page.tsx (admin dashboard pattern)
@src/app/(admin)/layout.tsx (admin layout with navigation)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sync events API route</name>
  <files>src/app/api/admin/ghl/sync-events/route.ts</files>
  <action>
Create `src/app/api/admin/ghl/sync-events/route.ts`:

GET handler (admin-only):
- Accepts query params: `direction` (optional: "inbound" | "outbound"), `status` (optional: "pending" | "completed" | "failed"), `limit` (optional, default 50, max 200), `offset` (optional, default 0)
- Queries sync_events table with optional where clauses, ordered by createdAt DESC
- Also returns total count for pagination
- Response: { events: SyncEvent[], total: number, limit: number, offset: number }

Use the same admin auth check pattern as the field-mappings route from plan 02.
  </action>
  <verify>
`npm run build` passes. Route file exists at correct path and exports GET handler.
  </verify>
  <done>Sync events API returns paginated, filterable event list for admin consumption.</done>
</task>

<task type="auto">
  <name>Task 2: Admin GHL settings page with components</name>
  <files>src/app/(admin)/admin/ghl/page.tsx, src/app/(admin)/admin/ghl/components/ConnectionStatus.tsx, src/app/(admin)/admin/ghl/components/FieldMappingTable.tsx, src/app/(admin)/admin/ghl/components/SyncEventLog.tsx</files>
  <action>
First, check the existing admin layout and navigation to understand the pattern for adding a new admin page. Look at the admin dashboard page and how sidebar/nav links are structured.

Create `src/app/(admin)/admin/ghl/page.tsx`:
- Server component that renders the page title "GoHighLevel Integration" with a subtitle "Manage CRM connection, field mappings, and sync events"
- Three sections stacked vertically:
  1. Connection Status section (ConnectionStatus client component)
  2. Custom Field Mappings section (FieldMappingTable client component)
  3. Sync Event Log section (SyncEventLog client component)
- Follow existing admin page dark mode styling (zinc backgrounds, white/zinc text)

Create `src/app/(admin)/admin/ghl/components/ConnectionStatus.tsx` ("use client"):
- Shows current connection status with a colored indicator (green dot = connected, red dot = not configured)
- Displays GHL_LOCATION_ID status (configured or not -- check via a lightweight API call)
- "Test Connection" button that POSTs to /api/admin/ghl/test-connection
- Shows result: success (green check + location name) or failure (red X + error message)
- Loading state while testing
- If GHL_API_TOKEN is not set, show a message: "Set GHL_API_TOKEN and GHL_LOCATION_ID environment variables to enable GHL integration"

Create `src/app/(admin)/admin/ghl/components/FieldMappingTable.tsx` ("use client"):
- Table with columns: LMS Concept, GHL Field ID, GHL Field Name, Active, Actions
- Loads existing mappings on mount via GET /api/admin/ghl/field-mappings
- "Add Mapping" button opens an inline form row at top of table with three text inputs (lmsConcept, ghlFieldId, ghlFieldName) and Save/Cancel buttons
- Each row has Edit and Delete action buttons
- Edit makes the row editable inline (same inputs)
- Delete shows confirmation before calling DELETE /api/admin/ghl/field-mappings/[id]
- Suggested LMS concepts shown as placeholder hints: timezone, goals, native_language, learning_level
- Empty state: "No field mappings configured. Add mappings to sync GHL custom fields to LMS concepts."

Create `src/app/(admin)/admin/ghl/components/SyncEventLog.tsx` ("use client"):
- Table showing recent sync events with columns: Time (relative, e.g., "2m ago"), Direction (inbound/outbound badge), Event Type, Entity, Status (colored badge), Error
- Filter dropdowns: Direction (All/Inbound/Outbound), Status (All/Pending/Completed/Failed)
- Pagination: "Load More" button at bottom (loads next 50)
- Auto-refreshes every 30 seconds (useEffect with setInterval)
- Status badges: pending = yellow, completed = green, failed = red
- Direction badges: inbound = blue, outbound = purple
- Empty state: "No sync events yet. Events will appear here as the GHL integration processes data."
- Show the event payload in an expandable row (click to toggle JSON view)

Add a link to the GHL settings page in the admin navigation. Look at the existing admin layout to find where nav links are defined and add: label "GHL Integration", href "/admin/ghl", icon from lucide-react (use `Link2` or `RefreshCw` icon).

Use existing shadcn/ui components where available (Button, etc). Use Tailwind for layout and styling consistent with the existing dark-mode admin aesthetic.
  </action>
  <verify>
`npm run build` passes with no errors. Admin GHL page exists at /admin/ghl. Navigation link added to admin sidebar/nav. All three component files exist and are imported correctly.
  </verify>
  <done>Admin can visit /admin/ghl to test GHL connection, manage custom field mappings (add/edit/delete without code changes), and view sync event log with filtering. Page is linked from admin navigation.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes with zero errors
2. `/admin/ghl` page renders with three sections
3. Connection test button calls API and shows result
4. Field mapping table supports full CRUD (add, edit, delete)
5. Sync event log loads and filters by direction/status
6. Admin nav includes link to GHL Integration page
</verification>

<success_criteria>
- Admin can configure GHL custom field mappings (timezone, goals, native_language) via UI without code changes
- Admin can verify GHL connection is working with test button
- Admin can view and filter sync events for debugging
- Page is accessible from admin navigation at /admin/ghl
</success_criteria>

<output>
After completion, create `.planning/phases/21-ghl-sync-infrastructure/21-03-SUMMARY.md`
</output>
