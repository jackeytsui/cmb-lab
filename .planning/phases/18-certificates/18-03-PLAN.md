---
phase: 18-certificates
plan: 03
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - src/components/certificate/CertificateDownloadButton.tsx
  - src/app/(dashboard)/dashboard/page.tsx
  - src/components/course/CourseCard.tsx
autonomous: true

must_haves:
  truths:
    - "Student sees download button on dashboard for each completed course"
    - "Download button triggers certificate generation and PDF download"
  artifacts:
    - path: "src/components/certificate/CertificateDownloadButton.tsx"
      provides: "Client button that generates certificate and downloads PDF"
      exports: ["CertificateDownloadButton"]
    - path: "src/app/(dashboard)/dashboard/page.tsx"
      provides: "Dashboard queries certificates for user and passes to CourseCard"
      contains: "certificates"
    - path: "src/components/course/CourseCard.tsx"
      provides: "CourseCard renders download button when course is completed"
      contains: "CertificateDownloadButton"
  key_links:
    - from: "src/components/certificate/CertificateDownloadButton.tsx"
      to: "/api/certificates/generate"
      via: "fetch POST to generate certificate"
      pattern: "fetch.*certificates/generate"
    - from: "src/components/certificate/CertificateDownloadButton.tsx"
      to: "/api/certificates/[certificateId]/download"
      via: "window.open or anchor href to download PDF"
      pattern: "certificates.*download"
    - from: "src/app/(dashboard)/dashboard/page.tsx"
      to: "src/components/course/CourseCard.tsx"
      via: "passes certificate data as prop"
      pattern: "certificate"
---

<objective>
Add a certificate download button to the student dashboard for completed courses. The button generates the certificate (if not already created) and downloads the PDF.

Purpose: This is the student-facing feature -- the visible payoff of course completion.
Output: Download button component, updated CourseCard, updated dashboard query.
</objective>

<execution_context>
@/home/sheldon/.claude/get-shit-done/workflows/execute-plan.md
@/home/sheldon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-certificates/18-RESEARCH.md
@.planning/phases/18-certificates/18-01-SUMMARY.md

@src/app/(dashboard)/dashboard/page.tsx
@src/components/course/CourseCard.tsx
@src/db/schema/certificates.ts
@src/lib/certificates.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: CertificateDownloadButton client component</name>
  <files>
    src/components/certificate/CertificateDownloadButton.tsx
  </files>
  <action>
    Create `src/components/certificate/CertificateDownloadButton.tsx`:
    - "use client" component
    - Props: `{ courseId: string; existingVerificationId?: string | null }`
    - State: `loading` (boolean), `verificationId` (string | null, initialized from existingVerificationId)
    - onClick handler:
      1. If verificationId already exists, directly open download URL in new tab: `/api/certificates/{verificationId}/download`
      2. If no verificationId yet:
         - Set loading = true
         - POST to `/api/certificates/generate` with `{ courseId }`
         - Parse response to get `certificate.verificationId`
         - Set verificationId state
         - Open download URL in new tab: `/api/certificates/{verificationId}/download`
         - Set loading = false
      3. Handle errors: show console.error, could use toast if sonner is available
    - Render: A button with download icon (use a simple SVG download arrow or the lucide-react Download icon if available)
      - Text: "Certificate" (short, fits on card)
      - Loading state: spinner or "..." text
      - Style: Small button, cyan/emerald accent to match the cinematic theme
      - `e.preventDefault()` and `e.stopPropagation()` to prevent the CourseCard Link from navigating when clicking the download button
  </action>
  <verify>
    - `npm run build` passes
    - Component exports CertificateDownloadButton
    - onClick prevents event propagation (stopPropagation)
  </verify>
  <done>
    CertificateDownloadButton generates certificate on first click (or uses existing), then downloads PDF via new tab.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire certificate download into dashboard and CourseCard</name>
  <files>
    src/app/(dashboard)/dashboard/page.tsx
    src/components/course/CourseCard.tsx
  </files>
  <action>
    1. Update `src/app/(dashboard)/dashboard/page.tsx`:
       - Import `certificates` from schema
       - After the existing userCourses query, query certificates for this user:
         ```
         SELECT verificationId, courseId FROM certificates WHERE userId = user.id
         ```
         Use the internal user ID (query users table by clerkId first to get the DB user ID, or join certificates in the existing query)
       - Build a map: `Map<courseId, verificationId>` from the certificates query
       - Pass to each CourseCard: a new prop `certificateVerificationId` (string | null)
         - Look up from the map using course.id

    2. Update `src/components/course/CourseCard.tsx`:
       - Add optional prop to CourseCardProps: `certificateVerificationId?: string | null`
       - Determine if course is completed: `progress.totalLessons > 0 && progress.completedLessons === progress.totalLessons`
       - When course IS completed, render `<CertificateDownloadButton>` in the card:
         - Place it below the progress bar, next to the access tier badge
         - Pass `courseId={course.id}` and `existingVerificationId={certificateVerificationId}`
       - Import `CertificateDownloadButton` from the certificate component
       - The download button must NOT trigger the Link navigation -- the CertificateDownloadButton handles stopPropagation internally

    IMPORTANT: The CourseCard is already "use client", so importing CertificateDownloadButton (also client) is fine. The dashboard page (server component) passes data as props.
  </action>
  <verify>
    - `npm run build` passes
    - CourseCard renders CertificateDownloadButton only when all lessons are completed
    - Dashboard queries certificates table and passes verificationId to CourseCard
    - Clicking download button does NOT navigate to course page (stopPropagation works)
  </verify>
  <done>
    Completed courses show a certificate download button on the dashboard. The button generates and downloads a PDF certificate. Courses that are not fully completed do not show the button.
  </done>
</task>

</tasks>

<verification>
- `npm run build` passes
- Dashboard shows download button only for courses where completedLessons === totalLessons
- CertificateDownloadButton fetches /api/certificates/generate then opens download URL
- Button click does not navigate away from dashboard
</verification>

<success_criteria>
- Completed courses display a certificate download button on the student dashboard
- Clicking the button generates a certificate (idempotent) and downloads the PDF
- Incomplete courses do NOT show the download button
- Build passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/18-certificates/18-03-SUMMARY.md`
</output>
