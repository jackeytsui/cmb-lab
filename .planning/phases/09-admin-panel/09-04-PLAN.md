---
phase: 09-admin-panel
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/admin/page.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Admin can navigate to Students page from dashboard"
    - "Admin can navigate to AI Logs page from dashboard"
  artifacts:
    - path: "src/app/(dashboard)/admin/page.tsx"
      provides: "Admin dashboard with working navigation cards"
      contains: "Link href=\"/admin/students\""
  key_links:
    - from: "src/app/(dashboard)/admin/page.tsx"
      to: "/admin/students"
      via: "Link component"
      pattern: "href=\"/admin/students\""
    - from: "src/app/(dashboard)/admin/page.tsx"
      to: "/admin/ai-logs"
      via: "Link component"
      pattern: "href=\"/admin/ai-logs\""
---

<objective>
Fix admin dashboard navigation to link to Students and AI Logs pages.

Purpose: Close verification gap where admin dashboard shows Students and AI Logs as "Coming in Plan 03" placeholders despite those pages being fully implemented in 09-03.

Output: Admin dashboard with all three navigation cards (Courses, Students, AI Logs) as clickable links with consistent styling.
</objective>

<execution_context>
@/home/sheldon/.claude/get-shit-done/workflows/execute-plan.md
@/home/sheldon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-admin-panel/09-VERIFICATION.md
@src/app/(dashboard)/admin/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update admin dashboard navigation cards</name>
  <files>src/app/(dashboard)/admin/page.tsx</files>
  <action>
Update the Students and AI Logs cards (lines 112-168) to match the Courses card pattern:

1. **Students card (lines 112-138):** Replace `<div className="rounded-lg border border-zinc-700 bg-zinc-800/50 p-6 opacity-60">` with `<Link href="/admin/students" className="group rounded-lg border border-zinc-700 bg-zinc-800 p-6 transition-colors hover:border-zinc-500 hover:bg-zinc-700">`
   - Change `bg-zinc-800/50` to `bg-zinc-800` (full opacity background)
   - Remove `opacity-60` class
   - Add `group` class for hover state
   - Add hover classes: `transition-colors hover:border-zinc-500 hover:bg-zinc-700`
   - Update `<h3>` to include `group-hover:text-purple-400` (matches icon color)
   - Remove the "Coming in Plan 03" badge `<span>` element
   - Close with `</Link>` instead of `</div>`

2. **AI Logs card (lines 140-168):** Same pattern:
   - Replace `<div>` with `<Link href="/admin/ai-logs">`
   - Change `bg-zinc-800/50` to `bg-zinc-800`
   - Remove `opacity-60`
   - Add `group`, `transition-colors`, `hover:border-zinc-500`, `hover:bg-zinc-700`
   - Update `<h3>` to include `group-hover:text-green-400` (matches icon color)
   - Remove the "Coming in Plan 03" badge `<span>` element
   - Close with `</Link>` instead of `</div>`

Note: The `Link` import already exists on line 7.
  </action>
  <verify>
    - `grep -q 'href="/admin/students"' src/app/(dashboard)/admin/page.tsx` returns 0
    - `grep -q 'href="/admin/ai-logs"' src/app/(dashboard)/admin/page.tsx` returns 0
    - `grep -c 'Coming in Plan 03' src/app/(dashboard)/admin/page.tsx` returns 0
    - `npm run lint` passes
    - `npm run build` passes
  </verify>
  <done>
    - Students card is a clickable Link to /admin/students with hover states
    - AI Logs card is a clickable Link to /admin/ai-logs with hover states
    - Both cards use consistent styling with the Courses card (full opacity, hover effects)
    - No "Coming in Plan 03" badges remain
  </done>
</task>

</tasks>

<verification>
1. Build check: `npm run build` passes without errors
2. Grep verification:
   - `grep 'Link href="/admin/students"' src/app/(dashboard)/admin/page.tsx` shows the Students link
   - `grep 'Link href="/admin/ai-logs"' src/app/(dashboard)/admin/page.tsx` shows the AI Logs link
   - `grep 'Coming in Plan 03' src/app/(dashboard)/admin/page.tsx` returns no matches
3. All three cards (Courses, Students, AI Logs) now use the same Link pattern with hover states
</verification>

<success_criteria>
- Admin dashboard has three clickable navigation cards
- Students card links to /admin/students with purple hover color
- AI Logs card links to /admin/ai-logs with green hover color
- No placeholder badges or reduced opacity on any cards
- Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/09-admin-panel/09-04-SUMMARY.md`
</output>
