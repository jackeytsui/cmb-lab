---
phase: 25-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/progress.ts
  - src/app/page.tsx
  - src/components/student/FeedbackCard.tsx
  - src/components/certificate/CertificateDownloadButton.tsx
  - src/components/notifications/NotificationPanel.tsx
  - src/app/(dashboard)/lessons/[lessonId]/page.tsx
  - src/app/(dashboard)/my-feedback/page.tsx
  - src/app/(dashboard)/coach/submissions/[submissionId]/page.tsx
autonomous: true

must_haves:
  truths:
    - "Progress tracking uses !!progress?.videoCompletedAt (not !== null) to correctly handle undefined"
    - "Root URL (/) redirects to /dashboard instead of showing Next.js default template"
    - "FeedbackCard Replay Lesson links to /lessons/${feedback.lessonId} not /courses"
    - "CertificateDownloadButton renders a visible red error message when download fails"
    - "NotificationPanel renders an error state with retry button when API fetch fails"
    - "Lesson page shows yellow warning banner when muxPlaybackId is missing"
    - "Loom regex character class includes hyphens and underscores: [a-zA-Z0-9_-]+"
    - "my-feedback page queries database directly via Drizzle (no fetch to own API)"
    - "coach submissions page queries database directly via Drizzle (no fetch to own API)"
  artifacts:
    - path: "src/lib/progress.ts"
      provides: "Correct video completion check"
      contains: "!!progress?.videoCompletedAt"
    - path: "src/app/page.tsx"
      provides: "Homepage redirect"
      contains: "redirect.*dashboard"
    - path: "src/components/student/FeedbackCard.tsx"
      provides: "Correct replay link and Loom regex"
      contains: "/lessons/${feedback.lessonId}"
    - path: "src/components/certificate/CertificateDownloadButton.tsx"
      provides: "Visible error state on certificate failure"
      contains: "text-red-400"
    - path: "src/components/notifications/NotificationPanel.tsx"
      provides: "Error state with retry button"
      contains: "Failed to load notifications"
    - path: "src/app/(dashboard)/lessons/[lessonId]/page.tsx"
      provides: "Demo video warning banner"
      contains: "Video not yet available"
    - path: "src/app/(dashboard)/my-feedback/page.tsx"
      provides: "Direct DB query for feedback"
      contains: "db.select"
    - path: "src/app/(dashboard)/coach/submissions/[submissionId]/page.tsx"
      provides: "Direct DB query for submission detail"
      contains: "db.select"
  key_links:
    - from: "src/lib/progress.ts"
      to: "lesson completion logic"
      via: "checkLessonCompletion function"
      pattern: "!!progress\\?\\.videoCompletedAt"
    - from: "src/app/(dashboard)/my-feedback/page.tsx"
      to: "src/db/index.ts"
      via: "direct Drizzle import"
      pattern: "from \"@/db\""
    - from: "src/app/(dashboard)/coach/submissions/[submissionId]/page.tsx"
      to: "src/db/index.ts"
      via: "direct Drizzle import"
      pattern: "from \"@/db\""
---

<objective>
Verify and commit 9 bug fixes that already exist as uncommitted changes in the working tree.

Purpose: These fixes address critical/major bugs found during a comprehensive audit. The code is already written — this plan verifies each fix is correct, confirms no type errors, and commits the changes as a clean baseline for the v3.1 polish phases.

Output: All 8 modified files committed with verified fixes for BUG-01 through BUG-09.
</objective>

<execution_context>
@/home/sheldon/.claude/get-shit-done/workflows/execute-plan.md
@/home/sheldon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify all 9 bug fixes are correct</name>
  <files>
    src/lib/progress.ts
    src/app/page.tsx
    src/components/student/FeedbackCard.tsx
    src/components/certificate/CertificateDownloadButton.tsx
    src/components/notifications/NotificationPanel.tsx
    src/app/(dashboard)/lessons/[lessonId]/page.tsx
    src/app/(dashboard)/my-feedback/page.tsx
    src/app/(dashboard)/coach/submissions/[submissionId]/page.tsx
  </files>
  <action>
    Read each of the 8 modified files and verify the fix logic matches the bug description. Confirm each fix individually:

    **BUG-01 (CRITICAL) — src/lib/progress.ts (~line 152):**
    Verify the video completion check is `!!progress?.videoCompletedAt` (NOT `progress?.videoCompletedAt !== null`).
    The old check returned `true` when `progress` was `undefined` because `undefined !== null` is `true`.
    The double-bang correctly handles both `undefined` and `null`.

    **BUG-02 — src/app/page.tsx:**
    Verify the file imports `redirect` from `next/navigation` and calls `redirect("/dashboard")`.
    The entire default Next.js template (Image imports, hero section, deploy links) should be gone.

    **BUG-03 — src/components/student/FeedbackCard.tsx (~line 192):**
    Verify the Replay Lesson link uses `` `/lessons/${feedback.lessonId}` `` (NOT `/courses`).

    **BUG-04 — src/components/certificate/CertificateDownloadButton.tsx:**
    Verify there is an `error` state variable (`useState<string | null>(null)`), that the catch block sets `setError("Failed to generate certificate. Please try again.")`, and that the JSX renders `{error && <span className="text-xs text-red-400">{error}</span>}`.

    **BUG-05 — src/components/notifications/NotificationPanel.tsx:**
    Verify there is an `error` state variable, that the catch block sets `setError(true)`, and that when `error` is true, the component renders "Failed to load notifications" with a "Try again" button that calls `fetchNotifications`.

    **BUG-06 — src/app/(dashboard)/lessons/[lessonId]/page.tsx:**
    Verify there is a conditional `{!lesson.muxPlaybackId && (...)}` block that renders a yellow warning banner with text "Video not yet available for this lesson -- showing demo content."

    **BUG-07 — src/components/student/FeedbackCard.tsx (~line 30):**
    Verify the Loom regex character class is `[a-zA-Z0-9_-]+` (includes underscore and hyphen). The old regex was `[a-zA-Z0-9]+` which would fail on Loom IDs containing hyphens or underscores.

    **BUG-08 — src/app/(dashboard)/my-feedback/page.tsx:**
    Verify the `getFeedback` function imports `db` from `@/db` and uses `db.select()` / `db.query` to fetch data directly. There should be NO `fetch()` call to `/api/my-feedback`. The function should accept a `userId: string` parameter.

    **BUG-09 — src/app/(dashboard)/coach/submissions/[submissionId]/page.tsx:**
    Verify the `getSubmission` function imports `db` from `@/db` and uses `db.select()` to query directly. There should be NO `fetch()` call to a base URL. The function should accept both `submissionId` and `coachUserId` parameters.

    After reading all files, if any fix is missing or incorrect, note it. Do NOT modify any code — just verify.
  </action>
  <verify>
    All 9 fixes confirmed present and logically correct by reading the source files.
  </verify>
  <done>Each of the 9 bug fixes has been verified as present and correct in the uncommitted working tree changes.</done>
</task>

<task type="auto">
  <name>Task 2: Type check and commit all bug fixes</name>
  <files>
    src/lib/progress.ts
    src/app/page.tsx
    src/components/student/FeedbackCard.tsx
    src/components/certificate/CertificateDownloadButton.tsx
    src/components/notifications/NotificationPanel.tsx
    src/app/(dashboard)/lessons/[lessonId]/page.tsx
    src/app/(dashboard)/my-feedback/page.tsx
    src/app/(dashboard)/coach/submissions/[submissionId]/page.tsx
  </files>
  <action>
    1. Run `npx tsc --noEmit` to confirm there are no TypeScript errors across the entire project. If there are type errors in the modified files, fix them (but do not change any fix logic).

    2. Run `npm run lint` to confirm no linting errors in the changed files.

    3. Stage all 8 modified files explicitly (do NOT use `git add -A`):
       ```
       git add src/lib/progress.ts \
               src/app/page.tsx \
               src/components/student/FeedbackCard.tsx \
               src/components/certificate/CertificateDownloadButton.tsx \
               src/components/notifications/NotificationPanel.tsx \
               "src/app/(dashboard)/lessons/[lessonId]/page.tsx" \
               "src/app/(dashboard)/my-feedback/page.tsx" \
               "src/app/(dashboard)/coach/submissions/[submissionId]/page.tsx"
       ```

    4. Commit with a descriptive message covering all 9 fixes:
       ```
       fix(25): verify and commit 9 bug fixes from comprehensive audit

       BUG-01: Fix false positive in video completion check (undefined !== null)
       BUG-02: Replace default Next.js homepage with redirect to /dashboard
       BUG-03: Fix FeedbackCard "Replay Lesson" link to correct lesson page
       BUG-04: Add visible error state to CertificateDownloadButton
       BUG-05: Add error state with retry to NotificationPanel
       BUG-06: Add yellow warning banner when lesson has no Mux video
       BUG-07: Fix Loom URL regex to handle hyphens and underscores
       BUG-08: Refactor my-feedback page from self-fetch to direct DB query
       BUG-09: Refactor coach submissions page from self-fetch to direct DB query
       ```

    5. Run `git status` after commit to confirm clean working tree (for these files).
  </action>
  <verify>
    - `npx tsc --noEmit` exits with code 0 (no type errors)
    - `git log --oneline -1` shows the new commit with the bug fix message
    - `git status` shows no remaining uncommitted changes in the 8 bug fix files
  </verify>
  <done>All 9 bug fixes pass type checking and are committed as a single clean commit.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npx tsc --noEmit` passes (no type errors)
2. `git log --oneline -1` shows the bug fix commit
3. Each fix can be verified by grepping:
   - `grep -n "!!progress?.videoCompletedAt" src/lib/progress.ts` returns the fix line
   - `grep -n "redirect.*dashboard" src/app/page.tsx` returns the redirect
   - `grep -n "lessons/\${feedback.lessonId}" src/components/student/FeedbackCard.tsx` returns correct link
   - `grep -n "text-red-400" src/components/certificate/CertificateDownloadButton.tsx` returns error display
   - `grep -n "Failed to load notifications" src/components/notifications/NotificationPanel.tsx` returns error state
   - `grep -n "Video not yet available" "src/app/(dashboard)/lessons/[lessonId]/page.tsx"` returns warning banner
   - `grep -n "\[a-zA-Z0-9_-\]" src/components/student/FeedbackCard.tsx` returns fixed regex
   - `grep -n "from \"@/db\"" "src/app/(dashboard)/my-feedback/page.tsx"` returns direct DB import
   - `grep -n "from \"@/db\"" "src/app/(dashboard)/coach/submissions/[submissionId]/page.tsx"` returns direct DB import
</verification>

<success_criteria>
- All 9 bug fixes verified as present and logically correct
- TypeScript compilation passes with zero errors in modified files
- Single commit containing all 8 files with descriptive message covering BUG-01 through BUG-09
- Working tree is clean for the bug fix files after commit
</success_criteria>

<output>
After completion, create `.planning/phases/25-bug-fixes/25-01-SUMMARY.md`
</output>
