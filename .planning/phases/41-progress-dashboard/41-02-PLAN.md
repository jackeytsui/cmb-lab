---
phase: 41-progress-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["41-01"]
files_modified:
  - src/components/progress/XPTimeline.tsx
  - src/components/progress/ActivityHeatmap.tsx
autonomous: true

must_haves:
  truths:
    - "XP timeline area chart renders with daily/weekly/monthly toggle using Recharts via shadcn/ui ChartContainer"
    - "Activity heatmap displays 365 days of learning activity in a GitHub-style contribution calendar"
  artifacts:
    - path: "src/components/progress/XPTimeline.tsx"
      provides: "Client component with AreaChart, period toggle tabs, and ChartTooltip"
      contains: "ChartContainer"
    - path: "src/components/progress/ActivityHeatmap.tsx"
      provides: "Client component wrapping react-activity-calendar with dark theme"
      contains: "ActivityCalendar"
  key_links:
    - from: "src/components/progress/XPTimeline.tsx"
      to: "src/components/ui/chart.tsx"
      via: "ChartContainer, ChartTooltip, ChartTooltipContent imports"
      pattern: "ChartContainer"
    - from: "src/components/progress/ActivityHeatmap.tsx"
      to: "react-activity-calendar"
      via: "ActivityCalendar default import"
      pattern: "ActivityCalendar"
---

<objective>
Build the XP Timeline area chart and Activity Heatmap client components.

Purpose: These are the two primary data visualization widgets on the progress dashboard — the timeline shows XP earning trends over time, and the heatmap shows daily learning consistency.
Output: Two client components (`XPTimeline.tsx`, `ActivityHeatmap.tsx`) ready to receive data props from the server component page.
</objective>

<execution_context>
@/home/sheldon/.claude/get-shit-done/workflows/execute-plan.md
@/home/sheldon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/41-progress-dashboard/41-RESEARCH.md

@src/components/ui/chart.tsx
@src/components/ui/tabs.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create XPTimeline area chart component</name>
  <files>src/components/progress/XPTimeline.tsx</files>
  <action>
Create `src/components/progress/XPTimeline.tsx` as a `"use client"` component.

**Props interface:**
```typescript
interface XPTimelineProps {
  dailyData: { date: string; xp: number }[];
  weeklyData: { date: string; xp: number }[];
  monthlyData: { date: string; xp: number }[];
}
```

**Implementation:**
1. Use `useState` for the active period tab (`"daily" | "weekly" | "monthly"`, default `"daily"`).

2. Use shadcn/ui `Tabs`, `TabsList`, `TabsTrigger` for the period toggle (Daily / Weekly / Monthly). Style TabsList with `bg-zinc-800/50` and TabsTrigger with `data-[state=active]:bg-zinc-700`.

3. Select the correct data array based on active tab.

4. Render using shadcn/ui ChartContainer + Recharts AreaChart:
   - `ChartContainer` with `className="min-h-[250px] w-full"` and a `chartConfig` for XP series (color: `#10b981` emerald to match existing activity rings).
   - Inside: Recharts `AreaChart` with `margin={{ left: 12, right: 12 }}`.
   - `defs` block with a `linearGradient` (id="fillXP") from `var(--color-xp)` opacity 0.8 at top to 0.1 at bottom.
   - `CartesianGrid vertical={false}` with `stroke="#27272a"` (zinc-800).
   - `XAxis` with `dataKey="date"`, `tickLine={false}`, `axisLine={false}`, `tickMargin={8}`, `tick={{ fill: '#a1a1aa' }}` (zinc-400). tickFormatter: for daily show "MMM d", for weekly show "MMM d", for monthly show "MMM".
   - `YAxis` with `tickLine={false}`, `axisLine={false}`, `tickMargin={8}`, `tick={{ fill: '#a1a1aa' }}`, `width={40}`.
   - `ChartTooltip` with `content={<ChartTooltipContent />}`.
   - `Area` with `type="monotone"`, `dataKey="xp"`, `stroke="var(--color-xp)"`, `fill="url(#fillXP)"`, `strokeWidth={2}`.

5. Wrap in a card-style container: `bg-zinc-900/50 border border-zinc-800 rounded-lg p-6`. Title "XP Timeline" in `text-lg font-semibold text-white mb-4`. Tabs below title, chart below tabs.

6. Handle empty data: if selected data array is empty, show a centered message "No XP data yet" in `text-zinc-500`.

**IMPORTANT:** Do NOT wrap AreaChart in a separate ResponsiveContainer — ChartContainer already handles responsiveness. Do NOT use min-h less than 250px on ChartContainer.
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - Component uses ChartContainer (not ResponsiveContainer)
    - Component has min-h-[250px] on ChartContainer
    - Component handles empty data state
    - Three period tabs render (Daily, Weekly, Monthly)
  </verify>
  <done>
    XPTimeline client component renders a Recharts AreaChart inside shadcn ChartContainer with daily/weekly/monthly toggle tabs, emerald gradient fill, dark theme axes, and empty state handling
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ActivityHeatmap component</name>
  <files>src/components/progress/ActivityHeatmap.tsx</files>
  <action>
Create `src/components/progress/ActivityHeatmap.tsx` as a `"use client"` component.

**Props interface:**
```typescript
interface HeatmapDay {
  date: string;   // "YYYY-MM-DD"
  count: number;  // total XP
  level: 0 | 1 | 2 | 3 | 4;
}

interface ActivityHeatmapProps {
  data: HeatmapDay[];
}
```

**Implementation:**
1. Import `ActivityCalendar` from `"react-activity-calendar"`.

2. Render inside a card container: `bg-zinc-900/50 border border-zinc-800 rounded-lg p-6`.
   - Title "Activity" in `text-lg font-semibold text-white mb-4`.

3. Configure ActivityCalendar:
   - `data={data}` — expects the contiguous 365-day array from the server.
   - `maxLevel={4}` — matches the 0-4 level scale.
   - `colorScheme="dark"` — dark background theme.
   - `theme={{ dark: ["#27272a", "#065f46", "#047857", "#059669", "#10b981"] }}` — zinc-800 for no activity, then emerald shades from darkest to brightest (level 1-4).
   - `labels={{ totalCount: "{{count}} XP earned in the last year" }}`.
   - `fontSize={12}`.
   - `blockSize={12}` — cell size.
   - `blockMargin={3}` — gap between cells.
   - `hideTotalCount={false}` — show the total at bottom.
   - `hideColorLegend={false}` — show the color scale legend.

4. Below the calendar, render a legend row showing the XP thresholds: "0 XP", "<25", "<75", "<150", "150+" next to colored squares matching the theme. Use `text-xs text-zinc-500` for labels. (Only needed if react-activity-calendar's built-in legend is insufficient — check the library's rendering and skip custom legend if the built-in one is clear enough.)

5. Handle empty data: if data array is empty or all counts are 0, still render the calendar (it will show all-grey squares, which is correct behavior for a new user).
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - Component imports from react-activity-calendar
    - Dark theme configured with emerald color scale
    - Component accepts HeatmapDay[] prop matching react-activity-calendar's expected format
    - Card wrapper with title renders
  </verify>
  <done>
    ActivityHeatmap client component wraps react-activity-calendar with dark emerald theme, proper maxLevel, XP total label, and card styling matching the dashboard design
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- Both components exist in `src/components/progress/`
- XPTimeline uses ChartContainer (NOT ResponsiveContainer) with min-h-[250px]
- ActivityHeatmap uses react-activity-calendar with dark theme
- Both are `"use client"` components accepting data via props (no fetch calls)
</verification>

<success_criteria>
Two chart-based client components are ready for integration: XPTimeline renders an area chart with period toggle, and ActivityHeatmap renders a GitHub-style contribution calendar. Both accept server-fetched data as props and handle empty states.
</success_criteria>

<output>
After completion, create `.planning/phases/41-progress-dashboard/41-02-SUMMARY.md`
</output>
