---
phase: 61-assignments-tracking
plan: 02
type: execute
wave: 2
depends_on: ["61-01"]
files_modified:
  - src/app/(dashboard)/coach/thread-assignments/page.tsx
  - src/app/(dashboard)/coach/thread-assignments/ThreadAssignmentsClient.tsx
  - src/app/(dashboard)/coach/thread-assignments/[assignmentId]/page.tsx
  - src/components/coach/ThreadAssignmentDialog.tsx
  - src/components/coach/ThreadAssignmentProgress.tsx
  - src/app/(dashboard)/dashboard/page.tsx
  - src/components/video-thread/AssignedThreadCard.tsx
  - src/app/(dashboard)/coach/page.tsx

autonomous: true

must_haves:
  truths:
    - "Coach can assign a thread to individual students or to entire courses from a dedicated page"
    - "Assigned threads appear on the student dashboard alongside practice and video assignments"
    - "Student sees completion status (not started, in progress, completed) on each assigned thread card"
    - "Coach can view per-student completion rates and response counts for each thread assignment"
  artifacts:
    - path: "src/app/(dashboard)/coach/thread-assignments/page.tsx"
      provides: "Coach thread assignments list page"
      contains: "ThreadAssignmentsClient"
    - path: "src/app/(dashboard)/coach/thread-assignments/ThreadAssignmentsClient.tsx"
      provides: "Client component for assignment list with create/delete"
      contains: "ThreadAssignmentDialog"
    - path: "src/app/(dashboard)/coach/thread-assignments/[assignmentId]/page.tsx"
      provides: "Per-assignment progress page showing per-student completion"
      contains: "ThreadAssignmentProgress"
    - path: "src/components/coach/ThreadAssignmentDialog.tsx"
      provides: "Dialog for creating thread assignments with thread picker and target selector"
      contains: "ThreadAssignmentDialog"
    - path: "src/components/coach/ThreadAssignmentProgress.tsx"
      provides: "Per-student progress table for thread assignment"
      contains: "ThreadAssignmentProgress"
    - path: "src/app/(dashboard)/dashboard/page.tsx"
      provides: "Student dashboard with thread assignments section"
      contains: "getStudentThreadAssignments"
    - path: "src/components/video-thread/AssignedThreadCard.tsx"
      provides: "Card component for assigned thread on student dashboard"
      contains: "AssignedThreadCard"
    - path: "src/app/(dashboard)/coach/page.tsx"
      provides: "Coach dashboard with Thread Assignments nav card"
      contains: "thread-assignments"
  key_links:
    - from: "src/app/(dashboard)/coach/thread-assignments/ThreadAssignmentsClient.tsx"
      to: "/api/admin/thread-assignments"
      via: "fetch POST/DELETE"
      pattern: "fetch.*thread-assignments"
    - from: "src/app/(dashboard)/dashboard/page.tsx"
      to: "src/lib/thread-assignments.ts"
      via: "import getStudentThreadAssignments"
      pattern: "getStudentThreadAssignments"
    - from: "src/components/video-thread/AssignedThreadCard.tsx"
      to: "/dashboard/threads/[threadId]"
      via: "Link to player route"
      pattern: "dashboard/threads"
---

<objective>
Build the UI layer for thread assignments: coach assignment management page, student dashboard integration, and coach completion tracking.

Purpose: Completes the user-facing features so coaches can assign threads and track completion, and students see their assigned threads.
Output: Coach thread assignment pages, student dashboard thread cards, and coach progress tracking view.
</objective>

<execution_context>
@/Users/sheldonho/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sheldonho/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-assignments-tracking/61-01-SUMMARY.md

Key patterns to follow:
- Coach video assignments UI: src/app/(dashboard)/coach/video-assignments/ (page.tsx, VideoAssignmentsClient.tsx, [assignmentId]/page.tsx)
- VideoAssignmentDialog: src/components/coach/VideoAssignmentDialog.tsx (target type selector + target options loader)
- VideoAssignmentProgress: src/components/coach/VideoAssignmentProgress.tsx (per-student table)
- Student dashboard: src/app/(dashboard)/dashboard/page.tsx (video assignments section pattern)
- AssignedVideoCard: src/components/video/AssignedVideoCard.tsx (card pattern)
- Coach dashboard nav cards: src/app/(dashboard)/coach/page.tsx (existing 6 cards)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create coach thread assignment pages and components</name>
  <files>
    src/app/(dashboard)/coach/thread-assignments/page.tsx
    src/app/(dashboard)/coach/thread-assignments/ThreadAssignmentsClient.tsx
    src/app/(dashboard)/coach/thread-assignments/[assignmentId]/page.tsx
    src/components/coach/ThreadAssignmentDialog.tsx
    src/components/coach/ThreadAssignmentProgress.tsx
    src/app/(dashboard)/coach/page.tsx
  </files>
  <action>
**1. Create src/components/coach/ThreadAssignmentDialog.tsx** following VideoAssignmentDialog.tsx pattern:

Dialog with fields:
- Thread picker: dropdown that fetches available threads from GET /api/admin/video-threads (existing endpoint). Show thread title in options.
- Target type selector: same 5 options (course, module, lesson, student, tag) using the same Select component pattern.
- Target entity selector: loads entities dynamically when targetType changes (same pattern as VideoAssignmentDialog — fetches /api/admin/courses, /api/admin/students, etc.).
- Notes field: optional textarea.
- Due date: optional date input.
- Submit button: POSTs to /api/admin/thread-assignments.

Props: `{ open, onOpenChange, onCreated }`.

**2. Create src/components/coach/ThreadAssignmentProgress.tsx** following VideoAssignmentProgress.tsx pattern:

Receives `data: ThreadAssignmentProgressResult` (from 61-01 lib types).
- Summary bar: Total Students, Completed, In Progress, Not Started counts.
- Table with columns: Student (name + email), Status (completed/in_progress/not_started badge), Responses (count), Started (relative time), Completed (relative time or "—").
- Sort: completed first, then in-progress, then not-started; alphabetical within groups.
- Status badges use same color scheme: emerald for completed, yellow for in-progress, zinc for not-started.
- Use same CheckCircle/Clock/MinusCircle icons as VideoAssignmentProgress.

Import ThreadAssignmentProgressResult type from src/lib/thread-assignments.ts.

**3. Create src/app/(dashboard)/coach/thread-assignments/page.tsx:**

Server component (same pattern as video-assignments/page.tsx):
- Check coach role, redirect if not.
- Query listCoachThreadAssignments via direct import from lib (server component pattern).
- Render ThreadAssignmentsClient with initialAssignments.

**4. Create src/app/(dashboard)/coach/thread-assignments/ThreadAssignmentsClient.tsx:**

Client component following VideoAssignmentsClient.tsx pattern:
- Receives initialAssignments prop (array of assignments with threadTitle).
- "Assign Thread" button opens ThreadAssignmentDialog.
- Assignment list as cards with: thread title, target type badge, notes preview, due date, delete button.
- Each card links to /coach/thread-assignments/[assignmentId] for progress view.
- Delete handler: calls DELETE /api/admin/thread-assignments/[id], then router.refresh().

**5. Create src/app/(dashboard)/coach/thread-assignments/[assignmentId]/page.tsx:**

Server component following video-assignments/[assignmentId]/page.tsx pattern:
- Check coach role.
- Fetch progress via getThreadAssignmentProgress(assignmentId) from lib.
- If null, show not-found state.
- Render ThreadAssignmentProgress component with data.
- Back link to /coach/thread-assignments.

**6. Update src/app/(dashboard)/coach/page.tsx:**

Add a 7th navigation card for "Thread Assignments" with:
- Link to /coach/thread-assignments
- Icon: `GitBranch` from lucide-react (represents branching thread paths — distinct from PlaySquare used by Thread Reviews and Video used by Video Assignments)
- Color: indigo accent (bg-indigo-500/10, text-indigo-400, hover:border-indigo-500/50) — distinct from existing cyan, violet, amber, rose, pink, teal
- Description: "Assign video threads to students as homework"
  </action>
  <verify>Run `npx tsc --noEmit` to confirm all components and pages compile. Verify coach dashboard has 7 navigation cards.</verify>
  <done>Coach can create, view, and delete thread assignments from /coach/thread-assignments. Coach can view per-student completion rates at /coach/thread-assignments/[id]. Coach dashboard has Thread Assignments nav card.</done>
</task>

<task type="auto">
  <name>Task 2: Add assigned threads to student dashboard</name>
  <files>
    src/components/video-thread/AssignedThreadCard.tsx
    src/app/(dashboard)/dashboard/page.tsx
  </files>
  <action>
**1. Create src/components/video-thread/AssignedThreadCard.tsx:**

Card component following AssignedVideoCard.tsx pattern. Props: `{ assignment: ResolvedThreadAssignment }`.

Displays:
- Thread title (bold)
- Notes preview (truncated to 2 lines, text-zinc-400)
- Due date (formatted, with "Overdue" badge in red if past due)
- Completion status badge: "Completed" (emerald), "In Progress" (yellow), "Not Started" (zinc) — same badge pattern as VideoAssignmentProgress
- Links to `/dashboard/threads/${assignment.threadId}` (existing player route from Phase 58)

Card styling: bg-zinc-900/50 border-zinc-800 with hover effect, same as other assignment cards.

**2. Update src/app/(dashboard)/dashboard/page.tsx:**

Add thread assignments section following the exact pattern of the video assignments section:

- Import `getStudentThreadAssignments` from `@/lib/thread-assignments`.
- Import `AssignedThreadCard` from `@/components/video-thread/AssignedThreadCard`.
- Import `GitBranch` from lucide-react for section icon.
- In the try block, add a non-blocking fetch: `let threadAssignments = []; try { if (dbUser) { threadAssignments = await getStudentThreadAssignments(dbUser.id); } } catch (err) { console.error("Failed to load thread assignments:", err); }`
- Filter to pending only (status !== "completed"), sort by dueDate soonest first (same sort as video assignments).
- Render section between video assignments and course grid (or after video assignments), with:
  - Section header: GitBranch icon (purple-400), "Thread Assignments" title, pending count badge (purple accent)
  - "View all" link pointing to `/dashboard/threads` (or omit if no dedicated list page exists — just show all pending)
  - Grid of up to 3 AssignedThreadCard components
  - Only render section if threadAssignments.length > 0

Use purple accent color for the thread assignments section (distinct from emerald for practice and blue for video assignments).
  </action>
  <verify>Run `npx tsc --noEmit` to confirm dashboard page and card component compile. Verify thread assignments section is conditionally rendered.</verify>
  <done>Assigned threads appear on student dashboard with completion status badges, due dates, and links to the thread player. Section only shows when assignments exist.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. Coach thread-assignments page exists at /coach/thread-assignments with create/delete functionality
3. Coach can view per-student progress at /coach/thread-assignments/[assignmentId]
4. Student dashboard shows thread assignments section with status badges
5. AssignedThreadCard links to /dashboard/threads/[threadId] player route
6. Coach dashboard has 7 navigation cards (existing 6 + Thread Assignments)
</verification>

<success_criteria>
- Coach can assign threads to students/courses from the UI
- Students see assigned threads on their dashboard with completion status
- Coach can view completion rates and response counts per student
- All UI follows established patterns (video assignments, practice assignments)
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/61-assignments-tracking/61-02-SUMMARY.md`
</output>
