---
phase: 13-ai-chatbot
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/chat/ChatWidget.tsx
  - src/components/chat/ChatPanel.tsx
  - src/components/chat/ChatMessage.tsx
autonomous: true

must_haves:
  truths:
    - "ChatWidget renders a floating button in the bottom-right corner"
    - "Clicking the button opens a chat panel with smooth animation"
    - "Chat panel has header, scrollable message area, and input form"
    - "Each message renders with role-appropriate styling (user right-aligned, assistant left-aligned)"
  artifacts:
    - path: "src/components/chat/ChatWidget.tsx"
      provides: "Floating chat button with panel toggle"
      exports: ["ChatWidget"]
    - path: "src/components/chat/ChatPanel.tsx"
      provides: "Chat panel with messages, input, and controls"
      exports: ["ChatPanel"]
    - path: "src/components/chat/ChatMessage.tsx"
      provides: "Single message bubble component"
      exports: ["ChatMessage"]
  key_links:
    - from: "src/components/chat/ChatWidget.tsx"
      to: "src/components/chat/ChatPanel.tsx"
      via: "import ChatPanel"
      pattern: "ChatPanel"
    - from: "src/components/chat/ChatPanel.tsx"
      to: "src/components/chat/ChatMessage.tsx"
      via: "import ChatMessage"
      pattern: "ChatMessage"
---

<objective>
Create the chat UI components: floating widget button, chat panel with message list and input, and individual message rendering.

Purpose: Build the visual chat interface that will be wired to the AI SDK in a later plan. These components are pure UI — no API calls yet.
Output: Three React components that form the complete chat UI structure.
</objective>

<execution_context>
@/home/sheldon/.claude/get-shit-done/workflows/execute-plan.md
@/home/sheldon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/13-ai-chatbot/13-RESEARCH.md
@src/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ChatWidget floating button component</name>
  <files>src/components/chat/ChatWidget.tsx</files>
  <action>
    Create `src/components/chat/ChatWidget.tsx` as a client component ('use client'):

    - Import: `useState` from 'react', `MessageCircle` and `X` from 'lucide-react', `AnimatePresence` and `motion` from 'framer-motion', `ChatPanel` from './ChatPanel'
    - State: `isOpen` boolean (default false)
    - Render a fixed-position container: `className="fixed bottom-6 right-6 z-50"`
    - Inside, use `AnimatePresence` to conditionally render the chat panel when `isOpen`:
      - `motion.div` with: `initial={{ opacity: 0, y: 20, scale: 0.95 }}`, `animate={{ opacity: 1, y: 0, scale: 1 }}`, `exit={{ opacity: 0, y: 20, scale: 0.95 }}`, `transition={{ duration: 0.2 }}`
      - Panel positioned: `className="absolute bottom-16 right-0 w-[380px] h-[520px] bg-gray-900 border border-gray-700 rounded-2xl shadow-2xl overflow-hidden flex flex-col"`
      - Render `<ChatPanel onClose={() => setIsOpen(false)} />` inside
    - Toggle button below: `w-14 h-14 rounded-full bg-cyan-600 hover:bg-cyan-500 text-white shadow-lg flex items-center justify-center transition-colors`
      - Show `X` icon when open, `MessageCircle` icon when closed
    - Export `ChatWidget` as named export
  </action>
  <verify>File exists and exports ChatWidget. No TypeScript errors.</verify>
  <done>Floating chat widget with animated panel toggle renders correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Create ChatPanel and ChatMessage components</name>
  <files>src/components/chat/ChatPanel.tsx, src/components/chat/ChatMessage.tsx</files>
  <action>
    **Create `src/components/chat/ChatMessage.tsx`** as a client component:
    - Props: `message: { id: string; role: 'user' | 'assistant'; parts: Array<{ type: string; text?: string }> }`, `isStreaming?: boolean`
    - User messages: right-aligned with `bg-cyan-600 text-white rounded-2xl rounded-br-sm px-4 py-2 max-w-[85%] ml-auto`
    - Assistant messages: left-aligned with `bg-gray-800 text-gray-100 rounded-2xl rounded-bl-sm px-4 py-2 max-w-[85%]`
    - Render text parts only: `message.parts.filter(p => p.type === 'text').map(p => p.text)`
    - Wrap text in `<p>` with `whitespace-pre-wrap` for line breaks
    - If `isStreaming` and no text parts yet, show a pulsing "..." indicator
    - Export as named export

    **Create `src/components/chat/ChatPanel.tsx`** as a client component:
    - Props: `onClose: () => void`
    - This is a STUB that will be wired to useChat in Plan 03. For now, use local state for demonstration:
      - `messages` state array (initially empty)
      - `input` state string
      - `status` state string ('ready')
    - Layout: flex column, full height
    - **Header:** flex row with justify-between, `px-4 py-3 border-b border-gray-700`
      - Left: `<h3 className="font-semibold text-white text-sm">Learning Assistant</h3>`
      - Right: two icon buttons — `Trash2` (size 16, for clear) and `X` (size 16, calls `onClose`)
      - Style buttons: `text-gray-400 hover:text-white transition-colors`
    - **Messages area:** `flex-1 overflow-y-auto p-4 space-y-3`
      - Map over messages, render `<ChatMessage>` for each
      - If messages empty, show welcome message: "Hi! I'm your learning assistant. Ask me anything about your courses, Chinese language, or the platform."
      - Style welcome: `text-gray-400 text-sm text-center mt-8`
    - **Input form:** `p-3 border-t border-gray-700 flex gap-2`
      - Input: `flex-1 bg-gray-800 border border-gray-700 rounded-lg px-3 py-2 text-sm text-white placeholder-gray-500 focus:outline-none focus:ring-1 focus:ring-cyan-500`
      - Placeholder: "Ask a question..."
      - Submit button: `p-2 rounded-lg bg-cyan-600 hover:bg-cyan-500 text-white disabled:opacity-50 disabled:cursor-not-allowed transition-colors`
      - Disable input and button when `status !== 'ready'`
      - Use `Send` icon (size 16) for submit button
    - Import icons: `Send`, `Trash2`, `X` from 'lucide-react'
    - Export as named export
  </action>
  <verify>Both files exist and export correctly. `npx tsc --noEmit` passes.</verify>
  <done>ChatPanel renders with header, scrollable messages area (with welcome message when empty), and input form. ChatMessage renders user/assistant bubbles with appropriate styling.</done>
</task>

</tasks>

<verification>
- `src/components/chat/ChatWidget.tsx` exports ChatWidget
- `src/components/chat/ChatPanel.tsx` exports ChatPanel
- `src/components/chat/ChatMessage.tsx` exports ChatMessage
- `npx tsc --noEmit` passes
- ChatWidget uses framer-motion AnimatePresence for panel animation
- ChatPanel has header with close/clear buttons, message area, input form
</verification>

<success_criteria>
- Three chat components created with dark mode styling matching the app
- Floating button positioned bottom-right with z-50
- Panel animates in/out with framer-motion
- Message bubbles styled differently for user vs assistant
- Empty state shows welcome message
</success_criteria>

<output>
After completion, create `.planning/phases/13-ai-chatbot/13-02-SUMMARY.md`
</output>
