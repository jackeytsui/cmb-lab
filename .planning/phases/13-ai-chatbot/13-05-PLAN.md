---
phase: 13-ai-chatbot
plan: 05
type: execute
wave: 3
depends_on: ["13-03", "13-04"]
files_modified:
  - src/components/chat/ChatPanel.tsx
  - src/components/chat/ChatWidget.tsx
  - src/components/chat/ChatMessage.tsx
autonomous: false

must_haves:
  truths:
    - "Student sees floating chatbot widget in bottom-right corner on all pages"
    - "Chatbot responses appear word-by-word as they stream in"
    - "Chatbot remembers earlier messages in the current conversation"
    - "Student can clear the conversation and start fresh"
    - "Chatbot can respond in Simplified Chinese, Traditional Chinese, or Cantonese"
    - "Chinese text renders with Pinyin/Jyutping annotations"
    - "Chatbot automatically uses student's saved language preference"
    - "Chatbot searches knowledge base to answer questions"
  artifacts:
    - path: "src/components/chat/ChatPanel.tsx"
      provides: "Polished chat panel with keyboard shortcuts and error recovery"
    - path: "src/components/chat/ChatWidget.tsx"
      provides: "Accessible floating widget"
    - path: "src/components/chat/ChatMessage.tsx"
      provides: "Message rendering with annotation support"
  key_links:
    - from: "src/components/chat/ChatWidget.tsx"
      to: "src/app/layout.tsx"
      via: "Rendered in root layout"
      pattern: "ChatWidget"
---

<objective>
Polish the chatbot with keyboard shortcuts, accessibility, error handling, mobile responsiveness, and run end-to-end verification.

Purpose: Ensure the complete chatbot experience is production-ready, covering edge cases like mobile viewport, keyboard navigation, error states, and the full CHAT-01 through CHAT-08 requirements.
Output: Polished, accessible chatbot ready for verification.
</objective>

<execution_context>
@/home/sheldon/.claude/get-shit-done/workflows/execute-plan.md
@/home/sheldon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/13-ai-chatbot/13-RESEARCH.md
@.planning/phases/13-ai-chatbot/13-03-SUMMARY.md
@.planning/phases/13-ai-chatbot/13-04-SUMMARY.md
@src/components/chat/ChatWidget.tsx
@src/components/chat/ChatPanel.tsx
@src/components/chat/ChatMessage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add polish — mobile responsiveness, keyboard handling, error recovery, accessibility</name>
  <files>src/components/chat/ChatPanel.tsx, src/components/chat/ChatWidget.tsx, src/components/chat/ChatMessage.tsx</files>
  <action>
    **Update `src/components/chat/ChatWidget.tsx`:**
    - Mobile responsiveness: On small screens (below sm breakpoint), make the chat panel full-width and taller:
      - Change panel class to include responsive: `w-[380px] h-[520px] sm:w-[380px] sm:h-[520px]` but on mobile `w-[calc(100vw-2rem)] h-[calc(100vh-8rem)]` using a media query approach or responsive Tailwind:
      - Use: `className="absolute bottom-16 right-0 w-[calc(100vw-3rem)] sm:w-[380px] h-[70vh] sm:h-[520px] ..."`
    - Escape key: Add `useEffect` with keydown listener for Escape to close panel when open
    - Add `aria-label="Open chat assistant"` / `"Close chat assistant"` to the toggle button

    **Update `src/components/chat/ChatPanel.tsx`:**
    - Auto-focus input when panel opens: add `autoFocus` to the input element
    - Error recovery: if `error` is set, show a retry message with a "Try again" button that clears the error. Display error as: `<div className="text-red-400 text-xs px-4 py-1">Something went wrong. <button className="underline" onClick={() => /* retry last message or clear error */}>Try again</button></div>`
    - Keyboard shortcut: Enter to send (already via form submit), Shift+Enter for newline if using textarea (keep as input for simplicity since messages are short)
    - Loading state: while `useLanguagePreference` is loading, the chatbot still works — the preference defaults to 'both'
    - Stop button: when `status === 'streaming'`, show a small "Stop" button (square icon) next to the input that calls `stop()` from useChatbot

    **Update `src/components/chat/ChatMessage.tsx`:**
    - Add smooth fade-in animation for new messages: wrap in `motion.div` with `initial={{ opacity: 0, y: 8 }}` and `animate={{ opacity: 1, y: 0 }}` (import from framer-motion)
    - Ensure long messages don't overflow: `break-words` class on the message container
    - Tool call parts: if a message part has `type === 'tool-invocation'`, show a subtle "Searching knowledge base..." text in gray italic while the tool runs (the AI SDK includes these in the parts array)
  </action>
  <verify>
    `npx tsc --noEmit` passes.
    Grep for 'aria-label' in ChatWidget.tsx — accessibility labels present.
    Grep for 'Escape' in ChatWidget.tsx — keyboard handler present.
    Grep for 'stop' in ChatPanel.tsx — stop function used.
  </verify>
  <done>Chatbot is responsive on mobile, has keyboard navigation, shows error states with recovery, and accessible labels.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete AI chatbot with streaming, RAG, Chinese annotations, and language preference</what-built>
  <how-to-verify>
    1. Start the dev server: `npm run dev`
    2. Open browser to localhost:3000 and sign in
    3. Verify: floating chat button appears in bottom-right corner (CHAT-01)
    4. Click the button — chat panel opens with smooth animation
    5. Type "Hello, what is this platform about?" and send
    6. Verify: response streams in word-by-word (CHAT-02)
    7. Ask a follow-up: "Tell me more about the courses" — verify it remembers context (CHAT-03)
    8. Click trash icon — verify conversation clears (CHAT-04)
    9. Type "How do you say hello in Cantonese?" — verify Chinese output with annotations (CHAT-05, CHAT-06)
    10. Navigate to a different page — verify widget persists (global mount)
    11. On mobile viewport (responsive), verify panel resizes appropriately
    12. Press Escape — verify panel closes

    Note: For CHAT-08 (RAG), if knowledge base has seeded data, ask a question about packages or coaching. If KB is empty, the chatbot should still work but answer from general knowledge.
    Note: For CHAT-07 (language preference), the chatbot sends the saved preference automatically — verify by checking the response language matches preference.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Requirements coverage:
- CHAT-01: Floating widget in bottom-right corner ✓ (ChatWidget fixed positioning)
- CHAT-02: Streaming responses ✓ (useChat + streamText)
- CHAT-03: Conversation context ✓ (useChat state management)
- CHAT-04: Clear conversation ✓ (setMessages([]))
- CHAT-05: Chinese language output ✓ (system prompt instructs language)
- CHAT-06: Pinyin/Jyutping annotations ✓ (ChineseAnnotation + ruby elements)
- CHAT-07: Language preference ✓ (useChatbot injects preference via DefaultChatTransport body)
- CHAT-08: Knowledge base search ✓ (searchKnowledgeBase tool in API route)
</verification>

<success_criteria>
- All 8 CHAT requirements verified
- Mobile responsive
- Accessible with keyboard navigation
- Error states handled with recovery option
- Production-ready chatbot experience
</success_criteria>

<output>
After completion, create `.planning/phases/13-ai-chatbot/13-05-SUMMARY.md`
</output>
