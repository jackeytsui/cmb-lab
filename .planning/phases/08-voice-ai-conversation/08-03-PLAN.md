---
phase: 08-voice-ai-conversation
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - src/components/voice/VoiceConversation.tsx
  - src/components/voice/ConversationTranscript.tsx
  - src/app/(dashboard)/lessons/[lessonId]/page.tsx
  - src/app/(dashboard)/my-conversations/page.tsx
  - src/app/(dashboard)/coach/conversations/page.tsx
  - src/app/(dashboard)/coach/conversations/[conversationId]/page.tsx
autonomous: false

must_haves:
  truths:
    - "Student can start voice conversation from lesson page"
    - "Live transcript shows during conversation"
    - "Student can view their past conversations"
    - "Coach/admin can view any student's conversation history"
  artifacts:
    - path: "src/components/voice/VoiceConversation.tsx"
      provides: "Voice conversation UI component"
      min_lines: 100
    - path: "src/components/voice/ConversationTranscript.tsx"
      provides: "Transcript display component"
      min_lines: 40
    - path: "src/app/(dashboard)/my-conversations/page.tsx"
      provides: "Student conversation history"
    - path: "src/app/(dashboard)/coach/conversations/page.tsx"
      provides: "Coach conversation review list"
  key_links:
    - from: "src/components/voice/VoiceConversation.tsx"
      to: "src/hooks/useRealtimeConversation.ts"
      via: "useRealtimeConversation hook"
      pattern: "useRealtimeConversation"
    - from: "src/app/(dashboard)/my-conversations/page.tsx"
      to: "/api/conversations"
      via: "fetch conversation list"
      pattern: "fetch.*api/conversations"
---

<objective>
Build conversation UI components and review pages for students and coaches.

Purpose: Complete the voice AI feature with user-facing interfaces and admin review capability.
Output: VoiceConversation component on lesson page, conversation history pages for students and coaches.
</objective>

<execution_context>
@/home/sheldon/.claude/get-shit-done/workflows/execute-plan.md
@/home/sheldon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-voice-ai-conversation/08-01-SUMMARY.md
@.planning/phases/08-voice-ai-conversation/08-02-SUMMARY.md
@src/app/(dashboard)/lessons/[lessonId]/page.tsx
@src/components/ui/button.tsx
@src/components/ui/card.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create VoiceConversation component and integrate with lesson page</name>
  <files>
    src/components/voice/VoiceConversation.tsx
    src/components/voice/ConversationTranscript.tsx
    src/app/(dashboard)/lessons/[lessonId]/page.tsx
  </files>
  <action>
**src/components/voice/ConversationTranscript.tsx** - Display live transcript:

Props:
- turns: Array<{ role: 'user' | 'assistant', content: string, timestamp: number }>
- isLive: boolean (show typing indicator if true and last turn is user)

Render:
- Scrollable container with max-height
- Each turn as a message bubble
- User messages on right (blue/purple), AI messages on left (gray)
- Show role label and timestamp
- Auto-scroll to bottom on new turns
- Use framer-motion for smooth entry animations

**src/components/voice/VoiceConversation.tsx** - Main conversation UI:

Props:
- lessonId: string
- lessonTitle: string

States (from hook):
- Idle: Show "Start Conversation" button
- Connecting: Show loading spinner
- Connected: Show active conversation UI
- Error: Show error message with retry button

Connected UI:
- Microphone status indicator (recording/muted icon)
- Mute/unmute button
- Live transcript using ConversationTranscript
- End conversation button
- Visual audio level indicator (optional, nice-to-have)

Use useRealtimeConversation hook:
- connect(lessonId) on start
- disconnect() on end
- toggleMute() for mute button
- Show turns from hook state

Styling:
- Dark theme consistent with app
- Card container
- Prominent call-to-action buttons
- Smooth state transitions

**src/app/(dashboard)/lessons/[lessonId]/page.tsx** - Add voice conversation:

After the video player section, add:
```tsx
<VoiceConversation
  lessonId={lesson.id}
  lessonTitle={lesson.title}
/>
```

Position below video, above any existing content. Use a Card wrapper with heading "Practice Conversation".
  </action>
  <verify>
npm run build passes (or shows only Clerk warnings). Visit /lessons/[lessonId] - VoiceConversation component renders with "Start Conversation" button. Clicking connects (requires OPENAI_API_KEY configured).
  </verify>
  <done>
Students can start voice conversations from lesson page. Live transcript displays during conversation. Mute/unmute and end conversation controls work.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create conversation history pages</name>
  <files>
    src/app/(dashboard)/my-conversations/page.tsx
    src/app/(dashboard)/coach/conversations/page.tsx
    src/app/(dashboard)/coach/conversations/[conversationId]/page.tsx
  </files>
  <action>
**src/app/(dashboard)/my-conversations/page.tsx** - Student's conversation history:

Server component that:
1. Gets current user
2. Fetches GET /api/conversations (own conversations)
3. Renders list of past conversations

Display for each conversation:
- Lesson title
- Date/time (formatDistanceToNow from date-fns)
- Duration (if ended)
- Click to expand and see transcript

Use Card component for each conversation. Show empty state if no conversations.

Add navigation link to sidebar (in dashboard layout) pointing to /my-conversations.

**src/app/(dashboard)/coach/conversations/page.tsx** - Coach conversation review:

Server component that:
1. Requires coach role (redirect if student)
2. Fetches all conversations (or filtered by student)
3. Query param: ?studentId= for filtering

Display:
- Student name/email for each conversation
- Lesson title
- Date/time
- Duration
- Link to detail view

Filters:
- Student selector (dropdown or search)
- Date range (optional, nice-to-have)

**src/app/(dashboard)/coach/conversations/[conversationId]/page.tsx** - Conversation detail:

Server component that:
1. Requires coach role
2. Fetches GET /api/conversations/[conversationId]
3. Displays full transcript

Display:
- Student info
- Lesson info
- Conversation metadata (start time, duration)
- Full transcript using ConversationTranscript component (isLive=false)
- Back button to list

Add navigation link to coach sidebar pointing to /coach/conversations.
  </action>
  <verify>
npm run build passes. Start dev server with `npm run dev`. As student, visit /my-conversations - page renders without errors, shows list of conversations or empty state. As coach, visit /coach/conversations - page renders with student filter dropdown. Click any conversation - detail page shows full transcript with ConversationTranscript component.
  </verify>
  <done>
Students can review their conversation history. Coaches can view any student's conversations. Full transcripts accessible for review.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete voice AI conversation feature:
- WebRTC connection to OpenAI Realtime API
- Lesson-aware AI instructions with pronunciation feedback
- Real-time bidirectional audio
- Live transcript capture
- Conversation history storage
- Student and coach review pages
  </what-built>
  <how-to-verify>
1. Ensure OPENAI_API_KEY is set in .env.local
2. Run `npm run dev`
3. Navigate to /lessons/[lessonId] (use a seeded lesson)
4. Click "Start Conversation"
5. Grant microphone permission
6. Speak to the AI - verify AI responds with lesson-relevant content
7. Mispronounce a word intentionally - verify AI provides pronunciation correction
8. Verify transcript updates in real-time
9. Click "End Conversation"
10. Navigate to /my-conversations - verify conversation appears in history
11. Log in as coach, navigate to /coach/conversations - verify can see student conversations
12. Click a conversation to view full transcript
  </how-to-verify>
  <resume-signal>
Type "approved" if voice conversation works end-to-end with pronunciation feedback, or describe any issues.
  </resume-signal>
</task>

</tasks>

<verification>
1. VoiceConversation component renders on lesson page
2. Conversation connects and bidirectional audio works
3. AI provides pronunciation feedback when student mispronounces
4. Live transcript updates during conversation
5. Conversation saved on end
6. /my-conversations shows student's history
7. /coach/conversations shows all students (coach only)
8. Full transcript viewable on detail page
</verification>

<success_criteria>
- Student starts voice conversation from lesson page
- AI responds with lesson-aware content
- AI provides pronunciation corrections when needed
- Live transcript shows both parties
- Conversation history accessible to student
- Coach can review any student's conversations
- All UI follows app design (dark theme, cards, animations)
</success_criteria>

<output>
After completion, create `.planning/phases/08-voice-ai-conversation/08-03-SUMMARY.md`
</output>
