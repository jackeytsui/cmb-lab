---
phase: 17-analytics
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - src/app/(dashboard)/admin/analytics/page.tsx
  - src/app/(dashboard)/admin/analytics/AnalyticsDashboard.tsx
  - src/app/(dashboard)/admin/analytics/components/OverviewCards.tsx
  - src/app/(dashboard)/admin/analytics/components/CompletionTable.tsx
  - src/app/(dashboard)/admin/analytics/components/DropoffTable.tsx
  - src/app/(dashboard)/admin/analytics/components/AtRiskTable.tsx
  - src/app/(dashboard)/admin/analytics/components/DifficultyTable.tsx
  - src/app/(dashboard)/admin/analytics/components/DateRangeFilter.tsx
  - src/app/(dashboard)/admin/page.tsx
autonomous: true

must_haves:
  truths:
    - "Admin sees dashboard with overview cards showing active students, pending reviews, total students, total courses"
    - "Admin sees course completion rate percentages in a table"
    - "Admin can identify which lessons students abandon most often in a drop-off table"
    - "Admin can see list of at-risk students with days since last activity"
    - "Admin can see average interaction attempts per lesson in a difficulty table"
    - "All tables update when date range filter is changed"
    - "Coach/admin can click export CSV button for each metric section"
    - "Analytics link appears on admin dashboard page"
  artifacts:
    - path: "src/app/(dashboard)/admin/analytics/page.tsx"
      provides: "Server page with admin access check"
    - path: "src/app/(dashboard)/admin/analytics/AnalyticsDashboard.tsx"
      provides: "Client component orchestrating all analytics sections"
    - path: "src/app/(dashboard)/admin/analytics/components/DateRangeFilter.tsx"
      provides: "Date range picker with from/to inputs"
    - path: "src/app/(dashboard)/admin/analytics/components/OverviewCards.tsx"
      provides: "ANLYT-03 and ANLYT-04 stat cards"
    - path: "src/app/(dashboard)/admin/analytics/components/CompletionTable.tsx"
      provides: "ANLYT-01 course completion rates table"
    - path: "src/app/(dashboard)/admin/analytics/components/DropoffTable.tsx"
      provides: "ANLYT-02 lesson drop-off points table"
    - path: "src/app/(dashboard)/admin/analytics/components/AtRiskTable.tsx"
      provides: "ANLYT-05 at-risk students table"
    - path: "src/app/(dashboard)/admin/analytics/components/DifficultyTable.tsx"
      provides: "ANLYT-06 average attempts table"
  key_links:
    - from: "src/app/(dashboard)/admin/analytics/AnalyticsDashboard.tsx"
      to: "/api/admin/analytics/*"
      via: "fetch calls with date range params"
      pattern: "fetch.*api/admin/analytics"
    - from: "src/app/(dashboard)/admin/analytics/AnalyticsDashboard.tsx"
      to: "/api/admin/analytics/export"
      via: "CSV download link with metric param"
      pattern: "api/admin/analytics/export\\?metric="
    - from: "src/app/(dashboard)/admin/page.tsx"
      to: "/admin/analytics"
      via: "Link component"
      pattern: "href.*admin/analytics"
---

<objective>
Build the analytics dashboard UI that displays all metrics from the Plan 01 API routes, with date range filtering and CSV export buttons.

Purpose: Give admins and coaches a visual dashboard to monitor student engagement, identify struggles, and export data.
Output: Analytics dashboard page with 5 sections (overview, completion, drop-off, at-risk, difficulty), date range filter, CSV export, and admin dashboard link.
</objective>

<execution_context>
@/home/sheldon/.claude/get-shit-done/workflows/execute-plan.md
@/home/sheldon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-analytics/17-01-SUMMARY.md

UI patterns to follow:
@src/app/(dashboard)/admin/page.tsx - admin dashboard with stat cards and nav links
@src/components/layout/AppHeader.tsx - shared header component
@src/components/ui/card.tsx - shadcn card component
@src/components/ui/button.tsx - shadcn button component
@src/components/ui/input.tsx - shadcn input component
@src/components/ui/skeleton.tsx - loading skeleton component
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analytics page, dashboard client component, date filter, and overview cards</name>
  <files>
    src/app/(dashboard)/admin/analytics/page.tsx
    src/app/(dashboard)/admin/analytics/AnalyticsDashboard.tsx
    src/app/(dashboard)/admin/analytics/components/DateRangeFilter.tsx
    src/app/(dashboard)/admin/analytics/components/OverviewCards.tsx
  </files>
  <action>
    1. Create `src/app/(dashboard)/admin/analytics/page.tsx` as a server component:
       - Check admin role with hasMinimumRole("admin"), redirect to /dashboard if not admin
       - Render AppHeader with title "Analytics Dashboard"
       - Render the AnalyticsDashboard client component

    2. Create `src/app/(dashboard)/admin/analytics/AnalyticsDashboard.tsx` as a "use client" component:
       - Manages state: `dateRange: { from: string; to: string }` (ISO date strings, initially empty = "all time")
       - On mount and when dateRange changes, fetches all 5 analytics endpoints in parallel using Promise.all
       - Passes date range as query params: `?from=${dateRange.from}&to=${dateRange.to}` (omit empty ones)
       - Renders sections in order: DateRangeFilter, OverviewCards, CompletionTable, DropoffTable, AtRiskTable, DifficultyTable
       - Each section has a header with title and a "Export CSV" button that triggers download from /api/admin/analytics/export?metric=X&from=...&to=...
       - Export button: create an anchor tag with href pointing to the export URL, or use window.open(). The browser handles the download automatically since the API returns Content-Disposition: attachment.
       - Show loading skeletons while fetching (use Skeleton from shadcn)
       - Dark theme styling consistent with admin pages: bg-zinc-900, text-white, zinc borders

    3. Create `src/app/(dashboard)/admin/analytics/components/DateRangeFilter.tsx`:
       - Two date inputs (HTML native `<input type="date">`) for "From" and "To"
       - A "Clear" button to reset both to empty (all time)
       - An "Apply" button that calls onChange callback with the date range
       - Styled with Tailwind: bg-zinc-800, border-zinc-700, text-white, inline-flex layout
       - Use shadcn Input component with type="date" for consistency

    4. Create `src/app/(dashboard)/admin/analytics/components/OverviewCards.tsx`:
       - Receives: `{ activeStudents: number, pendingReviews: number, totalStudents: number, totalCourses: number, loading: boolean }`
       - 4 stat cards in a responsive grid (grid-cols-2 md:grid-cols-4)
       - Each card: large number (text-3xl font-bold), label below (text-zinc-400)
       - Cards: "Active Students (7d)" with green accent, "Pending Reviews" with yellow accent, "Total Students" with purple accent, "Total Courses" with blue accent
       - Show Skeleton when loading
       - Follow the stat card pattern from admin/page.tsx
  </action>
  <verify>
    Run `npx tsc --noEmit` to verify no type errors. Navigate to /admin/analytics in browser to see overview cards.
  </verify>
  <done>
    Analytics page loads behind admin auth, shows date range filter and 4 overview stat cards with real data from API.
  </done>
</task>

<task type="auto">
  <name>Task 2: Data tables (completion, drop-off, at-risk, difficulty) and admin dashboard link</name>
  <files>
    src/app/(dashboard)/admin/analytics/components/CompletionTable.tsx
    src/app/(dashboard)/admin/analytics/components/DropoffTable.tsx
    src/app/(dashboard)/admin/analytics/components/AtRiskTable.tsx
    src/app/(dashboard)/admin/analytics/components/DifficultyTable.tsx
    src/app/(dashboard)/admin/page.tsx
  </files>
  <action>
    1. Create `src/app/(dashboard)/admin/analytics/components/CompletionTable.tsx`:
       - Receives: `{ data: Array<{ courseId, courseTitle, totalLessons, enrolledStudents, completedStudents, completionRate }>, loading: boolean }`
       - Styled table with columns: Course, Lessons, Enrolled, Completed, Completion Rate
       - Completion rate shown as percentage with a colored bar (green > 70%, yellow 40-70%, red < 40%)
       - Use a simple div with width percentage as visual progress bar (bg-green-500/bg-yellow-500/bg-red-500)
       - Empty state: "No course data available" in muted text
       - Show Skeleton rows when loading (3 rows of skeleton blocks)
       - Table styling: bg-zinc-800 rows, border-zinc-700, hover:bg-zinc-750

    2. Create `src/app/(dashboard)/admin/analytics/components/DropoffTable.tsx`:
       - Receives: `{ data: Array<{ lessonId, lessonTitle, moduleTitle, courseTitle, startedCount, completedCount, dropoffCount, dropoffRate }>, loading: boolean }`
       - Table columns: Lesson, Course > Module, Started, Completed, Drop-off Rate
       - Drop-off rate with color coding (red > 50%, yellow 25-50%, green < 25%)
       - Sorted by dropoffRate descending (API already sorts this way)
       - Same table styling as CompletionTable

    3. Create `src/app/(dashboard)/admin/analytics/components/AtRiskTable.tsx`:
       - Receives: `{ data: Array<{ userId, name, email, lastActivity, daysSinceActivity, totalLessonsCompleted }>, loading: boolean }`
       - Table columns: Student, Email, Last Activity, Days Inactive, Lessons Completed
       - Days inactive with color: red > 14 days, yellow 7-14 days
       - lastActivity formatted as relative time ("3 days ago", "2 weeks ago") or absolute date if > 30 days
       - Same table styling

    4. Create `src/app/(dashboard)/admin/analytics/components/DifficultyTable.tsx`:
       - Receives: `{ data: Array<{ lessonId, lessonTitle, moduleTitle, courseTitle, interactionCount, avgAttemptsToPass }>, loading: boolean }`
       - Table columns: Lesson, Course > Module, Interactions, Avg Attempts to Pass
       - Color code avgAttemptsToPass: red > 5, yellow 3-5, green < 3
       - Same table styling

    5. Update `src/app/(dashboard)/admin/page.tsx`:
       - Add a new navigation card for "Analytics" linking to `/admin/analytics`
       - Use BarChart3 icon from lucide-react (or similar chart icon)
       - Card description: "Student engagement, completion rates, and drop-off analysis"
       - Place it after the Students card in the grid
       - Accent color: amber/yellow (bg-amber-500/10, text-amber-400)

    All tables should use a consistent component pattern:
    - Responsive: horizontal scroll on mobile (overflow-x-auto wrapper)
    - Sticky header row
    - Zebra striping or hover states for readability
    - Empty state message when data array is empty
    - Loading skeleton state (3 placeholder rows with Skeleton blocks)
  </action>
  <verify>
    Run `npx tsc --noEmit` to verify no type errors. Run `npm run build` to confirm full build succeeds. Navigate to /admin to see Analytics card, click through to /admin/analytics to see all tables.
  </verify>
  <done>
    All 4 data tables render with color-coded metrics. Admin dashboard has Analytics navigation card. Full analytics page shows overview cards + 4 data tables + date range filter + CSV export buttons for each section. Build passes.
  </done>
</task>

</tasks>

<verification>
- `npm run build` passes with no errors
- /admin/analytics page is accessible only to admin users
- Overview cards show real numbers from database
- Completion table shows per-course completion rates
- Drop-off table shows lessons ranked by abandonment
- At-risk table shows inactive students
- Difficulty table shows average attempts per lesson
- Date range filter updates all sections when applied
- CSV export downloads a file for each metric
- Admin dashboard page has Analytics navigation card
</verification>

<success_criteria>
- ANLYT-01: Course completion rates visible in CompletionTable
- ANLYT-02: Lesson drop-off points visible in DropoffTable
- ANLYT-03: Active students count in OverviewCards
- ANLYT-04: Pending review count in OverviewCards
- ANLYT-05: At-risk students listed in AtRiskTable
- ANLYT-06: Average attempts per lesson in DifficultyTable
- ANLYT-07: Date range filter works across all sections
- ANLYT-08: CSV export button works for each metric section
</success_criteria>

<output>
After completion, create `.planning/phases/17-analytics/17-02-SUMMARY.md`
</output>
