---
phase: 59-media-responses-logic-backend
plan: 02
type: execute
wave: 2
depends_on: ["59-01"]
files_modified:
  - src/components/video-thread/VideoThreadPlayer.tsx
  - src/types/video-thread-player.ts
  - src/app/api/video-threads/[threadId]/respond/route.ts
autonomous: true

must_haves:
  truths:
    - "Student sees a Record Audio button when step responseType is audio, and recording uploads to Mux with playback ID stored in the response record"
    - "Student sees a Record Video button when step responseType is video, and recording uploads to Mux with playback ID stored in the response record"
    - "When a student submits a response to a step that connects to a logic node, the backend evaluates logicRules against the answer data and follows the matching path or fallback"
    - "Logic evaluation recursively traverses chained logic nodes until reaching a content node, with visited-set protection preventing infinite loops"
  artifacts:
    - path: "src/components/video-thread/VideoThreadPlayer.tsx"
      provides: "Audio and video recording UI integrated into player overlay"
      contains: "StudentMediaRecorder"
    - path: "src/types/video-thread-player.ts"
      provides: "Updated PlayerAction and state types for recording flow"
    - path: "src/app/api/video-threads/[threadId]/respond/route.ts"
      provides: "Response storage with muxPlaybackId in metadata for media responses"
      contains: "muxPlaybackId"
  key_links:
    - from: "src/components/video-thread/VideoThreadPlayer.tsx"
      to: "src/components/video-thread/StudentMediaRecorder.tsx"
      via: "renders StudentMediaRecorder for audio/video response types"
      pattern: "StudentMediaRecorder"
    - from: "src/components/video-thread/VideoThreadPlayer.tsx"
      to: "/api/video-threads/[threadId]/respond"
      via: "POSTs response with muxPlaybackId in metadata after upload completes"
      pattern: "muxPlaybackId.*metadata"
    - from: "src/app/api/video-threads/[threadId]/respond/route.ts"
      to: "src/lib/logic-engine.ts"
      via: "evaluateRules called during recursive logic traversal"
      pattern: "evaluateRules"
---

<objective>
Wire StudentMediaRecorder into the VideoThreadPlayer for audio and video response types, and ensure the respond API stores Mux playback IDs in response metadata. Verify logic engine evaluation works end-to-end.

Purpose: This completes the media response flow -- student records, uploads to Mux, submits response with playback ID, and the backend resolves the next step (including through logic nodes). All four Phase 59 success criteria are satisfied after this plan.

Output: Fully functional audio/video response collection in the player with Mux upload and logic routing
</objective>

<execution_context>
@/Users/sheldonho/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sheldonho/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-media-responses-logic-backend/59-01-SUMMARY.md

# Key source files
@src/components/video-thread/VideoThreadPlayer.tsx
@src/types/video-thread-player.ts
@src/app/api/video-threads/[threadId]/respond/route.ts
@src/components/video-thread/StudentMediaRecorder.tsx
@src/lib/logic-engine.ts
@src/db/schema/video-threads.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add audio and video response types to VideoThreadPlayer</name>
  <files>
    src/components/video-thread/VideoThreadPlayer.tsx
    src/types/video-thread-player.ts
    src/app/api/video-threads/[threadId]/respond/route.ts
  </files>
  <action>
**In `src/types/video-thread-player.ts`:**
- Add `SET_RECORDING_MODE` action to `PlayerAction` union: `{ type: "SET_RECORDING_MODE"; payload: "audio" | "video" | null }`
- Add `recordingMode: "audio" | "video" | null` to `VideoThreadPlayerState` (default: null)

**In `src/components/video-thread/VideoThreadPlayer.tsx`:**

1. Import `StudentMediaRecorder` from `./StudentMediaRecorder`

2. Add reducer case for `SET_RECORDING_MODE`:
   - Sets `recordingMode` to payload
   - Sets `status` to `"recording"` when payload is non-null, `"playing"` when null

3. Add `recordingMode: null` to `initialState`

4. In the interaction area (the `<div className="flex flex-wrap gap-3">` section), add two new response type branches:

   **For `responseType === "audio"`:**
   - If `state.recordingMode !== "audio"`: Show a "Record Audio" button (microphone icon from lucide-react `Mic`)
   - On click: dispatch `SET_RECORDING_MODE` with `"audio"`
   - If `state.recordingMode === "audio"`: Render `<StudentMediaRecorder mode="audio" threadId={thread.id} onUploadComplete={handleMediaUploadComplete} onCancel={handleRecordingCancel} disabled={state.isSubmitting} />`

   **For `responseType === "video"`:**
   - If `state.recordingMode !== "video"`: Show a "Record Video" button (camera icon from lucide-react `Video`)
   - On click: dispatch `SET_RECORDING_MODE` with `"video"`
   - If `state.recordingMode === "video"`: Render `<StudentMediaRecorder mode="video" threadId={thread.id} onUploadComplete={handleMediaUploadComplete} onCancel={handleRecordingCancel} disabled={state.isSubmitting} />`

5. Add `handleMediaUploadComplete` function:
   ```typescript
   const handleMediaUploadComplete = async (result: { muxPlaybackId: string; uploadId: string }) => {
     const responseType = state.recordingMode === "audio" ? "audio" : "video";
     dispatch({ type: "SET_RECORDING_MODE", payload: null });
     await handleResponse(result.muxPlaybackId, responseType as PlayerResponse["responseType"]);
   };
   ```

   **Update `handleResponse` to internally construct metadata for media types.** No new parameter needed. Inside `handleResponse`, where the fetch body is built, modify the `response` object to conditionally include `metadata` based on the `type` parameter:
   ```typescript
   body: JSON.stringify({
     stepId: currentStep.id,
     sessionId: state.sessionId,
     response: {
       type,
       content: value,
       // For audio/video: value IS the muxPlaybackId, store it in metadata too
       metadata: (type === "audio" || type === "video") ? { muxPlaybackId: value } : undefined,
     },
   }),
   ```
   This keeps the `handleResponse(value, type)` signature unchanged -- the function internally knows that when `type` is `"audio"` or `"video"`, the `value` argument is a muxPlaybackId and constructs the metadata object accordingly.

6. Add `handleRecordingCancel` function:
   ```typescript
   const handleRecordingCancel = () => {
     dispatch({ type: "SET_RECORDING_MODE", payload: null });
   };
   ```

7. Style the Record Audio/Video buttons consistently with the existing button/text styles: pill-shaped, backdrop-blur, white text on semi-transparent background, with the appropriate lucide icon.

8. **Add response storage documentation comment** in `src/app/api/video-threads/[threadId]/respond/route.ts`: At the top of the response storage section (above the `videoThreadResponses` insert), add:
   ```typescript
   // Response storage patterns:
   // - audio/video: content = muxPlaybackId, metadata = { muxPlaybackId }
   // - text: content = typed text, metadata = null
   // - button/multiple_choice: content = selected option value, metadata = null
   ```
   The respond route already stores `response.metadata` via `metadata: response.metadata || null` in the insert -- no logic change needed. The comment clarifies the contract for future developers.
  </action>
  <verify>
`grep -c "StudentMediaRecorder" src/components/video-thread/VideoThreadPlayer.tsx` returns at least 2 (import + render). `grep -c "SET_RECORDING_MODE" src/types/video-thread-player.ts` returns at least 1. `grep "muxPlaybackId" src/app/api/video-threads/\[threadId\]/respond/route.ts` shows the documentation comment.
  </verify>
  <done>
VideoThreadPlayer renders StudentMediaRecorder for audio and video response types, with recording mode state management, upload completion handler that submits responses with muxPlaybackId in both content and metadata, cancel support, and documentation comment in the respond route clarifying storage patterns for each response type.
  </done>
</task>

</tasks>

<verification>
1. VideoThreadPlayer renders a "Record Audio" button for steps with `responseType === "audio"`
2. VideoThreadPlayer renders a "Record Video" button for steps with `responseType === "video"`
3. Clicking the record button transitions to recording mode, showing StudentMediaRecorder
4. After recording and uploading, the response is submitted with muxPlaybackId as content and in metadata
5. The respond API stores the muxPlaybackId in the response record
6. Logic nodes in the path are evaluated via `evaluateRules()` with recursive traversal (pre-existing, verified)
7. The `visited` set in the traversal loop prevents infinite cycles (pre-existing, verified)
</verification>

<success_criteria>
- Student can record audio, upload to Mux, and the response record contains the muxPlaybackId (Phase 59 SC #1)
- Student can record webcam video, upload to Mux, and the response record contains the muxPlaybackId (Phase 59 SC #2)
- Logic node evaluation works when a response routes through a logic step (Phase 59 SC #3 -- pre-existing, verified)
- Recursive logic traversal with loop protection works for chained logic nodes (Phase 59 SC #4 -- pre-existing, verified)
</success_criteria>

<output>
After completion, create `.planning/phases/59-media-responses-logic-backend/59-02-SUMMARY.md`
</output>
