---
phase: 15-in-app-notifications
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema/notifications.ts
  - src/db/schema/index.ts
  - src/lib/notifications.ts
autonomous: true

must_haves:
  truths:
    - "Notifications table exists with type, title, body, read status, category, linkUrl, and soft delete"
    - "Notification preferences table exists with per-category mute support"
    - "createNotification() helper checks mute preferences before inserting"
    - "Database migration runs successfully against Neon"
  artifacts:
    - path: "src/db/schema/notifications.ts"
      provides: "notifications + notification_preferences tables with enums, indexes, relations, types"
      contains: "pgTable.*notifications"
    - path: "src/lib/notifications.ts"
      provides: "createNotification server-side helper"
      exports: ["createNotification"]
    - path: "src/db/schema/index.ts"
      provides: "barrel export including notification schema"
      contains: "notifications"
  key_links:
    - from: "src/lib/notifications.ts"
      to: "src/db/schema/notifications.ts"
      via: "import { notifications, notificationPreferences }"
      pattern: "import.*notifications.*from.*schema"
    - from: "src/db/schema/index.ts"
      to: "src/db/schema/notifications.ts"
      via: "re-export"
      pattern: "export.*from.*notifications"
---

<objective>
Create the database schema for notifications and notification preferences, plus a server-side helper to create notifications with preference-aware filtering.

Purpose: Establishes the data layer that all notification features depend on (NOTIF-01, NOTIF-07 partial).
Output: Two new DB tables (notifications, notification_preferences), Drizzle migration applied, createNotification() helper.
</objective>

<execution_context>
@/home/sheldon/.claude/get-shit-done/workflows/execute-plan.md
@/home/sheldon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-in-app-notifications/15-RESEARCH.md
@src/db/schema/index.ts
@src/db/schema/submissions.ts (reference pattern for pgTable, enums, relations, indexes)
@src/db/schema/users.ts (reference for users table FK)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create notifications schema and generate migration</name>
  <files>src/db/schema/notifications.ts, src/db/schema/index.ts</files>
  <action>
    Create `src/db/schema/notifications.ts` following the exact schema from RESEARCH.md:

    1. Define `notificationTypeEnum` pgEnum: "coach_feedback", "submission_graded", "course_access", "system"
    2. Define `notificationCategoryEnum` pgEnum: "feedback", "progress", "system"
    3. Define `notifications` pgTable with columns:
       - id: uuid PK with defaultRandom
       - userId: uuid FK to users.id with cascade delete, notNull
       - type: notificationTypeEnum, notNull
       - category: notificationCategoryEnum, notNull
       - title: text, notNull
       - body: text, notNull
       - read: boolean, notNull, default false
       - linkUrl: text (nullable)
       - metadata: text (nullable, JSON string)
       - createdAt: timestamp, notNull, defaultNow
       - readAt: timestamp (nullable)
       - deletedAt: timestamp (nullable, soft delete)
       - Indexes: composite on (userId, read), composite on (userId, createdAt)
    4. Define `notificationPreferences` pgTable with columns:
       - id: uuid PK with defaultRandom
       - userId: uuid FK to users.id with cascade delete, notNull
       - category: notificationCategoryEnum, notNull
       - muted: boolean, notNull, default false
       - updatedAt: timestamp, notNull, defaultNow, $onUpdate
       - Index on (userId)
    5. Define relations for both tables (one-to-many with users)
    6. Export inferred types: Notification, NewNotification, NotificationPreference, NewNotificationPreference

    Update `src/db/schema/index.ts`:
    - Add `export * from "./notifications";` at the end, following existing pattern

    Run `npm run db:generate` to generate the Drizzle migration.
    Run `npm run db:push` to apply schema (or `db:migrate` if that's the configured command -- check package.json).

    Follow exact patterns from existing schema files (submissions.ts, notes.ts) for style consistency.
  </action>
  <verify>
    - `npx drizzle-kit check` passes (or equivalent)
    - `npm run build` compiles without errors
    - Schema types are importable: `import { notifications, notificationPreferences } from "@/db/schema"`
  </verify>
  <done>notifications and notification_preferences tables exist in schema with proper indexes, enums, relations, and types exported</done>
</task>

<task type="auto">
  <name>Task 2: Create server-side createNotification helper</name>
  <files>src/lib/notifications.ts</files>
  <action>
    Create `src/lib/notifications.ts` with a `createNotification()` function:

    1. Import db from "@/db" and notifications, notificationPreferences from "@/db/schema"
    2. Import eq, and from "drizzle-orm"
    3. Define the function signature:
       ```typescript
       export async function createNotification(params: {
         userId: string;
         type: "coach_feedback" | "submission_graded" | "course_access" | "system";
         category: "feedback" | "progress" | "system";
         title: string;
         body: string;
         linkUrl?: string;
         metadata?: Record<string, unknown>;
       })
       ```
    4. BEFORE inserting, check notificationPreferences for this userId + category. If muted=true, return null (skip notification creation per Pitfall 4 from research).
    5. If not muted, insert into notifications table with all provided fields. If metadata provided, JSON.stringify it.
    6. Return the created notification row (using .returning()).

    This is a server-only module -- no "use client" directive.
  </action>
  <verify>
    - `npm run build` compiles without errors
    - Function is importable: `import { createNotification } from "@/lib/notifications"`
  </verify>
  <done>createNotification helper exists, checks mute preferences before insertion, returns created notification or null</done>
</task>

</tasks>

<verification>
- `npm run build` passes
- Schema file exports all types and tables
- index.ts barrel export includes notifications
- createNotification function handles muted preferences
</verification>

<success_criteria>
- Notifications schema with 2 tables, 2 enums, proper indexes, relations, and type exports
- createNotification helper with preference-aware filtering
- Clean build with no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/15-in-app-notifications/15-01-SUMMARY.md`
</output>
