---
phase: 15-in-app-notifications
plan: 05
type: execute
wave: 3
depends_on: ["15-02", "15-03"]
files_modified:
  - src/components/notifications/NotificationPreferences.tsx
  - src/app/(dashboard)/dashboard/page.tsx
autonomous: false

must_haves:
  truths:
    - "Students can see their notification preferences with category names and mute toggles"
    - "Toggling a mute switch calls PATCH /api/notifications/preferences"
    - "Muted categories are skipped when creating notifications (createNotification checks preferences)"
  artifacts:
    - path: "src/components/notifications/NotificationPreferences.tsx"
      provides: "Preferences UI with mute toggles per category"
      exports: ["NotificationPreferences"]
  key_links:
    - from: "src/components/notifications/NotificationPreferences.tsx"
      to: "/api/notifications/preferences"
      via: "fetch GET and PATCH"
      pattern: "fetch.*api/notifications/preferences"
---

<objective>
Build the notification preferences UI where students can mute non-urgent notification categories, and verify the full notification system works end-to-end.

Purpose: Delivers NOTIF-07 (mute preferences) and provides visual verification of the complete system.
Output: Preferences component accessible from notification panel, full system verification.
</objective>

<execution_context>
@/home/sheldon/.claude/get-shit-done/workflows/execute-plan.md
@/home/sheldon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/15-in-app-notifications/15-RESEARCH.md
@.planning/phases/15-in-app-notifications/15-01-SUMMARY.md
@.planning/phases/15-in-app-notifications/15-02-SUMMARY.md
@.planning/phases/15-in-app-notifications/15-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create NotificationPreferences component and integrate into panel</name>
  <files>src/components/notifications/NotificationPreferences.tsx, src/components/notifications/NotificationPanel.tsx</files>
  <action>
    Create `src/components/notifications/NotificationPreferences.tsx` ("use client"):
    - On mount, fetch GET /api/notifications/preferences to load current settings
    - Display 3 categories with human-readable labels:
      - "Feedback" (feedback) -- "Coach feedback and grading results"
      - "Progress" (progress) -- "Course access and milestone notifications"
      - "System" (system) -- "System announcements and maintenance"
    - Each category has a toggle/switch to mute/unmute
    - Toggle calls PATCH /api/notifications/preferences with { category, muted: boolean }
    - Show loading state while fetching preferences
    - Styling: dark theme (zinc backgrounds, zinc borders), compact layout

    Update `src/components/notifications/NotificationPanel.tsx`:
    - Add a Settings/gear icon button in the panel header (next to "Mark all read")
    - Clicking it toggles between notification list view and preferences view
    - Use a local state `showPreferences` boolean to toggle views
    - Import Settings icon from lucide-react
  </action>
  <verify>
    - `npm run build` compiles without errors
    - NotificationPreferences exports correctly
    - NotificationPanel has settings toggle
  </verify>
  <done>Preferences component shows 3 categories with mute toggles, accessible from notification panel gear icon</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete in-app notification system: database tables, API routes, polling hook, bell icon with badge, notification dropdown panel, mark-as-read actions, preferences with mute toggles, and coach feedback integration</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open the app
    2. Log in as a student -- verify the bell icon appears in the header
    3. The bell should show no badge (no unread notifications yet)
    4. Click the bell -- verify the popover opens showing "No notifications yet"
    5. Click the gear icon in the notification panel -- verify preferences show 3 categories with toggles
    6. Toggle "System" to muted, then toggle it back
    7. Log in as a coach in another browser/incognito
    8. Submit feedback on a student submission
    9. Switch back to student browser -- within 30 seconds the bell should show "1" badge
    10. Click the bell -- verify the notification appears with "New feedback from your coach" title
    11. Click "Mark all read" -- verify the badge disappears
    12. Switch tabs away and back -- verify the count refreshes (should stay 0)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Full notification system works end-to-end
- Preferences UI accessible from notification panel
- Bell icon visible on all dashboard pages
- Polling refreshes correctly
</verification>

<success_criteria>
- NotificationPreferences component with category mute toggles
- Preferences accessible from notification panel gear icon
- Full system verified by user
</success_criteria>

<output>
After completion, create `.planning/phases/15-in-app-notifications/15-05-SUMMARY.md`
</output>
