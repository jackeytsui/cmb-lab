---
phase: 15-in-app-notifications
plan: 03
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - src/hooks/useNotifications.ts
  - src/components/notifications/NotificationBell.tsx
  - src/components/notifications/NotificationPanel.tsx
  - src/components/notifications/NotificationItem.tsx
autonomous: true

must_haves:
  truths:
    - "useNotifications hook polls /api/notifications/count every 30 seconds"
    - "Polling pauses when tab is hidden and refreshes immediately on tab focus"
    - "Bell icon shows unread count badge (99+ cap)"
    - "Clicking bell opens Radix Popover with notification list"
    - "Each notification shows title, body, relative time, and read/unread state"
    - "User can mark individual notifications as read from the panel"
    - "User can mark all notifications as read from the panel"
  artifacts:
    - path: "src/hooks/useNotifications.ts"
      provides: "Polling hook with visibility-aware refresh"
      exports: ["useNotifications"]
    - path: "src/components/notifications/NotificationBell.tsx"
      provides: "Bell icon with badge and Popover trigger"
      exports: ["NotificationBell"]
    - path: "src/components/notifications/NotificationPanel.tsx"
      provides: "Notification list dropdown content"
      exports: ["NotificationPanel"]
    - path: "src/components/notifications/NotificationItem.tsx"
      provides: "Single notification row"
      exports: ["NotificationItem"]
  key_links:
    - from: "src/components/notifications/NotificationBell.tsx"
      to: "src/hooks/useNotifications.ts"
      via: "useNotifications() hook call"
      pattern: "useNotifications"
    - from: "src/hooks/useNotifications.ts"
      to: "/api/notifications/count"
      via: "fetch polling"
      pattern: "fetch.*api/notifications/count"
    - from: "src/components/notifications/NotificationPanel.tsx"
      to: "/api/notifications"
      via: "fetch on popover open"
      pattern: "fetch.*api/notifications"
---

<objective>
Build the client-side notification UI: polling hook, bell icon with badge, and notification dropdown panel with mark-as-read actions.

Purpose: Delivers the visible notification experience (NOTIF-02, NOTIF-03, NOTIF-04, NOTIF-06).
Output: 1 hook + 3 React components for the notification system.
</objective>

<execution_context>
@/home/sheldon/.claude/get-shit-done/workflows/execute-plan.md
@/home/sheldon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/15-in-app-notifications/15-RESEARCH.md
@.planning/phases/15-in-app-notifications/15-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useNotifications polling hook</name>
  <files>src/hooks/useNotifications.ts</files>
  <action>
    Create `src/hooks/useNotifications.ts` with "use client" directive:

    1. Export `useNotifications()` hook that returns `{ unreadCount, refresh }`:
       - `unreadCount`: number state, initially 0
       - `refresh`: callback to manually trigger a count fetch

    2. `fetchCount` function: GET /api/notifications/count, parse { unreadCount }, update state. Silently catch errors (non-critical).

    3. useEffect that:
       - Calls fetchCount() immediately on mount
       - Sets up setInterval(fetchCount, 30_000) for 30-second polling
       - Adds `visibilitychange` event listener that calls fetchCount() when document.visibilityState === "visible"
       - Cleanup: clearInterval + removeEventListener

    4. Use useRef for interval to avoid stale closure issues.
    5. Use useCallback for fetchCount to keep stable reference.

    Follow the exact pattern from RESEARCH.md Pattern 3, with the visibility-aware polling.
  </action>
  <verify>
    - `npm run build` compiles without errors
    - Hook exports useNotifications
  </verify>
  <done>useNotifications polls count every 30s, pauses on hidden tab, refreshes on focus, returns { unreadCount, refresh }</done>
</task>

<task type="auto">
  <name>Task 2: Create NotificationBell, NotificationPanel, and NotificationItem components</name>
  <files>src/components/notifications/NotificationBell.tsx, src/components/notifications/NotificationPanel.tsx, src/components/notifications/NotificationItem.tsx</files>
  <action>
    **Install @radix-ui/react-popover** first: `npm install @radix-ui/react-popover`

    Check which Radix import style the codebase uses. If other components use `import { X } from "radix-ui"`, use that. Otherwise use `import * as Popover from "@radix-ui/react-popover"`. Match existing patterns.

    **NotificationBell.tsx** ("use client"):
    - Uses useNotifications() hook for unreadCount and refresh
    - Renders Popover.Root > Popover.Trigger (button with Bell icon from lucide-react)
    - Badge: absolute-positioned span showing unreadCount (cap at "99+") -- only visible when > 0
    - Popover.Content: renders NotificationPanel, aligned to end, 320px wide, max-height 384px, overflow-y auto
    - Styling: dark theme (bg-zinc-900 border-zinc-800) matching existing app aesthetic
    - aria-label with unread count for accessibility

    **NotificationPanel.tsx** ("use client"):
    - Receives `onAction` callback prop (called after any action to refresh count)
    - On mount (or when popover opens), fetches GET /api/notifications to load full list
    - State: notifications array, loading boolean, panelOpenedAt timestamp (for mark-all-read boundary)
    - Header row: "Notifications" title + "Mark all read" button (calls POST /api/notifications/read-all with { before: panelOpenedAt })
    - Maps notifications to NotificationItem components
    - Empty state: "No notifications yet" message when list is empty
    - Loading state: simple loading indicator

    **NotificationItem.tsx** ("use client"):
    - Receives notification object + onMarkRead callback
    - Displays: title (bold if unread), body (truncated to 2 lines), relative time using date-fns formatDistanceToNow
    - Unread indicator: small dot or background highlight for unread notifications
    - Click handler: if notification has linkUrl, navigate to it. Mark as read via PATCH /api/notifications/[id]
    - Call onMarkRead callback after successful mark-read

    All components use Tailwind with dark theme classes (zinc-900, zinc-800, zinc-400 etc.) to match existing dashboard aesthetic.
  </action>
  <verify>
    - `npm run build` compiles without errors
    - All 3 components export correctly
    - @radix-ui/react-popover is in package.json dependencies
  </verify>
  <done>Bell shows badge, clicking opens popover panel with notification list, items show relative time, mark-read works on individual and all</done>
</task>

</tasks>

<verification>
- `npm run build` passes
- @radix-ui/react-popover installed
- All 4 files (hook + 3 components) compile and export correctly
- Components follow dark theme styling
</verification>

<success_criteria>
- Polling hook with 30s interval + visibility detection
- Bell icon with unread count badge
- Popover panel lists notifications with mark-as-read actions
- All components are "use client" with proper dark theme styling
</success_criteria>

<output>
After completion, create `.planning/phases/15-in-app-notifications/15-03-SUMMARY.md`
</output>
