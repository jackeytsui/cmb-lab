---
phase: 15-in-app-notifications
plan: 04
type: execute
wave: 3
depends_on: ["15-02", "15-03"]
files_modified:
  - src/components/layout/AppHeader.tsx
  - src/app/(dashboard)/dashboard/page.tsx
  - src/app/(dashboard)/courses/[courseId]/page.tsx
  - src/app/(dashboard)/my-feedback/page.tsx
  - src/app/(dashboard)/my-conversations/page.tsx
  - src/app/(dashboard)/coach/page.tsx
  - src/app/(dashboard)/coach/students/page.tsx
  - src/app/(dashboard)/coach/conversations/page.tsx
  - src/app/(dashboard)/admin/page.tsx
  - src/app/api/submissions/[submissionId]/feedback/route.ts
autonomous: true

must_haves:
  truths:
    - "AppHeader component renders on all dashboard pages with page title, notification bell, and Clerk UserButton"
    - "Bell icon appears consistently across student, coach, and admin pages"
    - "When coach submits feedback, student receives in-app notification alongside email"
    - "Notification links to /my-feedback page"
  artifacts:
    - path: "src/components/layout/AppHeader.tsx"
      provides: "Shared header with NotificationBell and UserButton"
      exports: ["AppHeader"]
  key_links:
    - from: "src/components/layout/AppHeader.tsx"
      to: "src/components/notifications/NotificationBell.tsx"
      via: "import and render"
      pattern: "NotificationBell"
    - from: "src/app/api/submissions/[submissionId]/feedback/route.ts"
      to: "src/lib/notifications.ts"
      via: "createNotification() call"
      pattern: "createNotification"
    - from: "src/app/(dashboard)/dashboard/page.tsx"
      to: "src/components/layout/AppHeader.tsx"
      via: "import and render"
      pattern: "AppHeader"
---

<objective>
Create a shared AppHeader component with the notification bell, integrate it into all dashboard pages (replacing inline headers), and wire coach feedback to create in-app notifications.

Purpose: Makes notifications visible across the entire app and connects the first notification trigger (NOTIF-02, NOTIF-05).
Output: Shared header on all pages, coach feedback creates in-app notifications.
</objective>

<execution_context>
@/home/sheldon/.claude/get-shit-done/workflows/execute-plan.md
@/home/sheldon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/15-in-app-notifications/15-RESEARCH.md
@.planning/phases/15-in-app-notifications/15-01-SUMMARY.md
@.planning/phases/15-in-app-notifications/15-02-SUMMARY.md
@.planning/phases/15-in-app-notifications/15-03-SUMMARY.md
@src/app/(dashboard)/dashboard/page.tsx (reference for current header pattern)
@src/app/(dashboard)/coach/page.tsx (reference for coach header pattern)
@src/app/api/submissions/[submissionId]/feedback/route.ts (integration point for NOTIF-05)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AppHeader and integrate into dashboard pages</name>
  <files>src/components/layout/AppHeader.tsx, src/app/(dashboard)/dashboard/page.tsx, src/app/(dashboard)/courses/[courseId]/page.tsx, src/app/(dashboard)/my-feedback/page.tsx, src/app/(dashboard)/my-conversations/page.tsx, src/app/(dashboard)/coach/page.tsx, src/app/(dashboard)/coach/students/page.tsx, src/app/(dashboard)/coach/conversations/page.tsx, src/app/(dashboard)/admin/page.tsx</files>
  <action>
    Create `src/components/layout/AppHeader.tsx` as a CLIENT component ("use client"):
    - Props: `{ title: string }` (page-specific heading)
    - Renders a `<header>` with flex layout: title on left, NotificationBell + UserButton on right
    - Import NotificationBell from "@/components/notifications/NotificationBell"
    - Import UserButton from "@clerk/nextjs"
    - Styling: matches existing header patterns (px-4 py-4, text-2xl font-bold, flex items-center justify-between)
    - Gap between bell and UserButton (gap-4)

    Then refactor the following pages to USE AppHeader instead of their inline `<header>` elements:

    **Student pages:**
    - `dashboard/page.tsx`: The greeting header area ("Welcome back, {name}"). Replace with AppHeader title="Dashboard" and keep the greeting text as a subtitle below AppHeader. The page is a SERVER component -- AppHeader is client, imported into server component (this is fine in Next.js).
    - `courses/[courseId]/page.tsx`: Replace inline header with AppHeader
    - `my-feedback/page.tsx`: Replace inline header with AppHeader
    - `my-conversations/page.tsx`: Replace inline header with AppHeader

    **Coach pages:**
    - `coach/page.tsx`: Replace inline header with AppHeader title="Coach Dashboard"
    - `coach/students/page.tsx`: Replace inline header with AppHeader
    - `coach/conversations/page.tsx`: Replace inline header with AppHeader

    **Admin pages:**
    - `admin/page.tsx`: Replace inline header with AppHeader title="Admin Dashboard"

    For each page:
    1. Read the current page to find the `<header>` element
    2. Replace it with `<AppHeader title="..." />`
    3. Preserve any existing content that was inside the header but NOT the title or UserButton (those are now in AppHeader)
    4. If the page had a UserButton already, remove it (AppHeader provides it)
    5. Do NOT touch admin sub-pages (courses/[id], prompts/[id], etc.) -- only top-level dashboard pages need the shared header

    IMPORTANT: Some pages may not have an inline `<header>` tag. In those cases, add AppHeader at the top of the page content. Be careful to read each page first and make minimal, targeted changes.
  </action>
  <verify>
    - `npm run build` compiles without errors
    - AppHeader component exists and exports
    - At least 8 dashboard pages import and use AppHeader
  </verify>
  <done>AppHeader with NotificationBell + UserButton renders consistently on all main dashboard pages</done>
</task>

<task type="auto">
  <name>Task 2: Wire coach feedback to create in-app notification</name>
  <files>src/app/api/submissions/[submissionId]/feedback/route.ts</files>
  <action>
    Read the existing feedback route at `src/app/api/submissions/[submissionId]/feedback/route.ts`.

    Find where the coach feedback is successfully saved (after the DB insert/update). Add a call to createNotification():

    ```typescript
    import { createNotification } from "@/lib/notifications";

    // After feedback is saved and email notification is triggered:
    await createNotification({
      userId: submission.userId,  // The student's DB user ID (not clerkId)
      type: "coach_feedback",
      category: "feedback",
      title: "New feedback from your coach",
      body: `${coachName} left feedback on your submission`,
      linkUrl: "/my-feedback",
    });
    ```

    Key considerations:
    - Find the student's DB user ID from the submission record (it should already be available in the route)
    - Find the coach's display name from the current user
    - The createNotification call should NOT block the response -- wrap in try/catch and log errors but don't fail the feedback submission
    - Place this AFTER the existing email webhook trigger so both notifications fire
    - Use "coach_feedback" type and "feedback" category
  </action>
  <verify>
    - `npm run build` compiles without errors
    - Feedback route imports and calls createNotification
    - The notification creation is non-blocking (wrapped in try/catch)
  </verify>
  <done>Coach feedback submission creates an in-app notification for the student alongside the existing email notification</done>
</task>

</tasks>

<verification>
- `npm run build` passes
- AppHeader appears on all main dashboard pages
- Coach feedback creates in-app notification
- No regressions in existing page functionality
</verification>

<success_criteria>
- Shared AppHeader component with NotificationBell on all dashboard pages
- Coach feedback triggers both email and in-app notification
- Clean build, no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/15-in-app-notifications/15-04-SUMMARY.md`
</output>
