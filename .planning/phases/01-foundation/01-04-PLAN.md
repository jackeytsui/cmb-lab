---
phase: 01-foundation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/seed.ts
  - package.json
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "At least one test course exists in database for webhook testing"
    - "Seed script can be run multiple times without duplicating data"
  artifacts:
    - path: "src/db/seed.ts"
      provides: "Database seed script with test course"
      min_lines: 30
  key_links:
    - from: "src/db/seed.ts"
      to: "src/db/schema/courses.ts"
      via: "courses.insert"
      pattern: "db\\.insert\\(courses\\)"
---

<objective>
Create database seed script with test course data to enable enrollment webhook end-to-end testing.

Purpose: The enrollment webhook at `/api/webhooks/enroll/route.ts` verifies course exists (line 34-40) before granting access. Without seed data, webhook always returns 404 because courses table is empty.

Output: `src/db/seed.ts` script that creates test course/module/lesson data, idempotent (safe to run multiple times).
</objective>

<execution_context>
@/home/sheldon/.claude/get-shit-done/workflows/execute-plan.md
@/home/sheldon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@src/db/schema/courses.ts
@src/db/schema/index.ts
@src/db/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create database seed script with test course</name>
  <files>src/db/seed.ts, package.json</files>
  <action>
    Create `src/db/seed.ts` that:
    1. Uses drizzle-orm and the existing db client from `@/db`
    2. Creates one test course:
       - id: Use a fixed UUID (e.g., `11111111-1111-1111-1111-111111111111`) for idempotency
       - title: "Beginner Cantonese"
       - description: "Introduction to Cantonese for beginners"
       - isPublished: true
       - previewLessonCount: 3
       - sortOrder: 0
    3. Creates one test module:
       - id: Use a fixed UUID (e.g., `22222222-2222-2222-2222-222222222222`)
       - courseId: references the course
       - title: "Module 1: Greetings"
       - sortOrder: 0
    4. Creates one test lesson:
       - id: Use a fixed UUID (e.g., `33333333-3333-3333-3333-333333333333`)
       - moduleId: references the module
       - title: "Lesson 1: Hello"
       - muxPlaybackId: null (or Mux demo playback ID if known)
       - sortOrder: 0
    5. Uses `.onConflictDoNothing()` on inserts to make script idempotent
    6. Logs success/skip messages for each entity

    Add to `package.json` scripts section:
    ```
    "db:seed": "npx tsx src/db/seed.ts"
    ```

    Note: Use `tsx` to run TypeScript directly (already available as drizzle-kit dependency).
  </action>
  <verify>
    Run `npm run db:seed` (requires DATABASE_URL configured).
    Should see log messages for each created/skipped entity.
    Run again - should report "already exists" without errors.
  </verify>
  <done>
    Test course with ID `11111111-1111-1111-1111-111111111111` exists in database.
    Enrollment webhook can now find this course and grant access.
  </done>
</task>

</tasks>

<verification>
1. Script exists at `src/db/seed.ts` and is executable via `npm run db:seed`
2. Running seed creates test course/module/lesson
3. Running seed again does not create duplicates (idempotent)
4. Course ID `11111111-1111-1111-1111-111111111111` can be used in enrollment webhook testing
</verification>

<success_criteria>
- [x] `src/db/seed.ts` created with test data
- [x] `npm run db:seed` command added to package.json
- [x] Script uses `onConflictDoNothing()` for idempotency
- [x] Test course exists with known UUID for webhook testing
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
