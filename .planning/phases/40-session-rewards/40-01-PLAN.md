---
phase: 40-session-rewards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/celebrations.ts
  - src/lib/confetti.ts
  - src/lib/unlock.ts
  - src/app/api/lessons/[lessonId]/next/route.ts
autonomous: true

must_haves:
  truths:
    - "Score tier classification returns correct tier for any percentage 0-100"
    - "Confetti presets fire distinct visual effects per tier (gold/silver/gentle/none)"
    - "getNextLesson returns next lesson in module or null for last lesson"
    - "API endpoint /api/lessons/[lessonId]/next returns next lesson info or null"
  artifacts:
    - path: "src/lib/celebrations.ts"
      provides: "Score tier types, tier configs, getScoreTier, getTierConfig, CELEBRATION_TIMING constants"
      exports: ["ScoreTier", "TierConfig", "TIER_CONFIGS", "getScoreTier", "getTierConfig", "CELEBRATION_TIMING"]
    - path: "src/lib/confetti.ts"
      provides: "Confetti preset functions for each score tier"
      exports: ["firePerfectConfetti", "fireExcellentConfetti", "fireGoodConfetti", "fireConfettiForTier"]
    - path: "src/lib/unlock.ts"
      provides: "getNextLesson utility (added to existing file)"
      exports: ["getNextLesson"]
    - path: "src/app/api/lessons/[lessonId]/next/route.ts"
      provides: "GET endpoint returning next lesson id+title or null"
      exports: ["GET"]
  key_links:
    - from: "src/lib/confetti.ts"
      to: "canvas-confetti"
      via: "import confetti from canvas-confetti"
      pattern: "import confetti from .canvas-confetti."
    - from: "src/lib/celebrations.ts"
      to: "CELEB-03 requirement"
      via: "tier thresholds: 95/80/60"
      pattern: "score >= 95.*perfect|score >= 80.*excellent|score >= 60.*good"
    - from: "src/app/api/lessons/[lessonId]/next/route.ts"
      to: "src/lib/unlock.ts"
      via: "getNextLesson import"
      pattern: "getNextLesson"
---

<objective>
Create the celebration foundation: score tier system, confetti presets, animation timing constants, and the getNextLesson server utility with API route.

Purpose: Provides all the shared logic and effects that the celebration overlay components (Plan 02) and integration plans (Plans 03-04) depend on. Separating this foundation ensures the tier classification, confetti effects, and CTA routing logic are defined once and reused consistently.

Output: Four files providing the celebration system's data layer and effects layer.
</objective>

<execution_context>
@/home/sheldon/.claude/get-shit-done/workflows/execute-plan.md
@/home/sheldon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-session-rewards/40-RESEARCH.md
@src/lib/unlock.ts
@src/lib/xp.ts
@src/db/schema/courses.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install canvas-confetti, create celebrations.ts and confetti.ts</name>
  <files>
    src/lib/celebrations.ts
    src/lib/confetti.ts
    package.json
  </files>
  <action>
    1. Install canvas-confetti and its types:
       `npm install canvas-confetti && npm install -D @types/canvas-confetti`

    2. Create `src/lib/celebrations.ts` with:
       - `ScoreTier` type: "perfect" | "excellent" | "good" | "keep_practicing"
       - `TierConfig` interface: { tier, label, message, color (Tailwind text color), bgColor (Tailwind bg + border), glowClass }
       - `TIER_CONFIGS` record with all 4 tiers:
         - perfect (95-100%): label "Perfect!", gold colors (text-yellow-400, bg-yellow-500/20 border-yellow-500/50), glow shadow
         - excellent (80-94%): label "Excellent!", silver colors (text-zinc-300, bg-zinc-400/20 border-zinc-400/50), no glow
         - good (60-79%): label "Good Job!", blue colors (text-blue-400, bg-blue-500/20 border-blue-500/50), no glow
         - keep_practicing (<60%): label "Keep Going!", muted colors (text-zinc-400, bg-zinc-600/20 border-zinc-600/50), no glow
       - `getScoreTier(score: number): ScoreTier` -- returns tier based on thresholds
       - `getTierConfig(score: number): TierConfig` -- returns full config for score
       - `CELEBRATION_TIMING` const object:
         - initialDelay: 0.3 (seconds before first element)
         - staggerInterval: 0.5 (seconds between each element)
         - stages: 4 (the 4 visible motion.div children in CelebrationOverlay: (1) ScoreReveal, (2) tier message + optional practice stats, (3) XP+streak badges row, (4) SmartCTAs)
         - NOTE: Confetti is NOT a stagger child. It fires separately via setTimeout in useCelebration hook at confettiDelayMs.
         - confettiDelayMs: 1300 (fires after score count-up completes at ~1.5s, timed to coincide with the tier message appearing: initialDelay 300ms + staggerInterval 500ms * 2 = 1300ms)
         - get totalDuration() => initialDelay + staggerInterval * (stages - 1)
       - `CelebrationOverlayProps` interface (exported for use by components):
         - type: "lesson" | "practice"
         - score: number (0-100)
         - xpEarned: number
         - streakCount: number
         - isFirstAttempt: boolean
         - correctCount?: number
         - totalExercises?: number
         - nextLesson?: { id: string; title: string } | null
         - courseId?: string
         - practiceSetId?: string
         - onDismiss: () => void
         - onRetry?: () => void

    3. Create `src/lib/confetti.ts` with:
       - Import confetti from "canvas-confetti"
       - GOLD_COLORS array: ["#FFD700", "#FFA500", "#FF8C00", "#FFEC8B", "#F0E68C"]
       - SILVER_COLORS array: ["#C0C0C0", "#A9A9A9", "#D3D3D3", "#E8E8E8", "#B8B8B8"]
       - `firePerfectConfetti()`: 150 particles, spread 360, startVelocity 45, gravity 0.8, ticks 300, colors GOLD_COLORS, shapes ["star","circle"], scalar 1.2, origin {x:0.5, y:0.4}, disableForReducedMotion true, zIndex 9999. Then after 250ms timeout: two side bursts (50 particles each, angle 60/120, spread 55, origin x:0/x:1, same gold colors and zIndex/reduced motion settings)
       - `fireExcellentConfetti()`: 80 particles, spread 120, startVelocity 35, gravity 1, ticks 200, colors SILVER_COLORS, shapes ["circle","square"], origin {x:0.5, y:0.5}, disableForReducedMotion true, zIndex 9999
       - `fireGoodConfetti()`: 30 particles, spread 80, startVelocity 20, gravity 1.2, decay 0.95, ticks 150, colors ["#87CEEB","#98D8C8","#B8D4E3"], origin {x:0.5, y:0.5}, disableForReducedMotion true, zIndex 9999
       - `fireConfettiForTier(tier: ScoreTier)`: switch on tier to call correct preset; "keep_practicing" is a no-op
       - Export a `resetConfetti()` helper that calls confetti.reset()
  </action>
  <verify>
    Run `npx tsc --noEmit` to confirm no type errors. Verify canvas-confetti appears in package.json dependencies and @types/canvas-confetti in devDependencies.
  </verify>
  <done>
    celebrations.ts exports ScoreTier, TierConfig, TIER_CONFIGS, getScoreTier, getTierConfig, CELEBRATION_TIMING, CelebrationOverlayProps. confetti.ts exports firePerfectConfetti, fireExcellentConfetti, fireGoodConfetti, fireConfettiForTier, resetConfetti. canvas-confetti installed. CELEBRATION_TIMING.stages is 4 (visible children only), confettiDelayMs is a fixed 1300ms (not derived from a confettiStage index).
  </done>
</task>

<task type="auto">
  <name>Task 2: Add getNextLesson to unlock.ts and create API route</name>
  <files>
    src/lib/unlock.ts
    src/app/api/lessons/[lessonId]/next/route.ts
  </files>
  <action>
    1. Add `getNextLesson` function to `src/lib/unlock.ts` (append to existing file, do NOT modify existing checkLessonUnlock function):
       - Signature: `async function getNextLesson(lessonId: string): Promise<{ id: string; title: string } | null>`
       - Query the current lesson to get its moduleId and sortOrder
       - Query lessons table for the next lesson in the same module: same moduleId, sortOrder > current sortOrder, deletedAt IS NULL, ordered by sortOrder ASC, limit 1
       - Return { id, title } or null if no next lesson exists
       - Import `gt`, `asc`, `isNull` from drizzle-orm (some may already be imported -- check existing imports)
       - Export the function

    2. Create `src/app/api/lessons/[lessonId]/next/route.ts`:
       - GET handler with auth check via Clerk
       - Extract lessonId from route params
       - Call getNextLesson(lessonId)
       - Return JSON: { nextLesson: result } where result is { id, title } | null
       - On error, return 500 with { error: "Failed to get next lesson" }
       - No course access check needed here -- the lesson page already validates access
  </action>
  <verify>
    Run `npx tsc --noEmit` to confirm no type errors. Verify the existing checkLessonUnlock function is unchanged. Verify the new route file exists at the correct path.
  </verify>
  <done>
    getNextLesson is exported from src/lib/unlock.ts. GET /api/lessons/[lessonId]/next returns { nextLesson: { id, title } | null }. Existing unlock.ts functionality unchanged.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `npm ls canvas-confetti` shows the package is installed
3. celebrations.ts exports are importable (verified by TypeScript compiler)
4. confetti.ts exports are importable
5. unlock.ts still exports checkLessonUnlock (no regression)
6. API route file exists at correct nested path
</verification>

<success_criteria>
- canvas-confetti and @types/canvas-confetti installed
- Score tier system classifies 95-100 as perfect, 80-94 as excellent, 60-79 as good, <60 as keep_practicing
- Each tier has distinct confetti preset with appropriate particle count, colors, and disableForReducedMotion
- CELEBRATION_TIMING has 4 stages (4 visible DOM children), confettiDelayMs is 1300ms (fires via hook, not as a stagger child)
- getNextLesson queries next lesson by sortOrder within module
- API route serves next lesson data for client-side CTA routing
</success_criteria>

<output>
After completion, create `.planning/phases/40-session-rewards/40-01-SUMMARY.md`
</output>
