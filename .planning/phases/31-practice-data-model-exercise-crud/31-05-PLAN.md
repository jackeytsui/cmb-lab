---
phase: 31-practice-data-model-exercise-crud
plan: 05
type: execute
wave: 2
depends_on: ["31-01"]
files_modified:
  - src/components/admin/exercises/ExercisePreview.tsx
  - src/components/admin/exercises/ExerciseList.tsx
autonomous: true

must_haves:
  truths:
    - "ExercisePreview renders a read-only student-perspective view for all 6 exercise types"
    - "Chinese text in previews uses PhoneticText component for phonetic annotation rendering"
    - "ExerciseList displays exercises in a practice set with type icons, language badges, and edit/delete actions"
    - "Preview shows how MCQ options look, how fill-in-blank placeholders render, how matching pairs display"
  artifacts:
    - path: "src/components/admin/exercises/ExercisePreview.tsx"
      provides: "Read-only student-view preview renderer for all 6 exercise types"
      min_lines: 100
    - path: "src/components/admin/exercises/ExerciseList.tsx"
      provides: "Exercise list within a practice set with type icons, actions, and ordering"
      min_lines: 60
  key_links:
    - from: "src/components/admin/exercises/ExercisePreview.tsx"
      to: "src/components/ui/PhoneticText.tsx"
      via: "PhoneticText wrapping Chinese text in previews"
      pattern: "PhoneticText"
    - from: "src/components/admin/exercises/ExercisePreview.tsx"
      to: "src/types/exercises.ts"
      via: "ExerciseDefinition type for rendering"
      pattern: "ExerciseDefinition"
---

<objective>
Create the exercise preview component (student-perspective view) and the exercise list component for managing exercises within a practice set.

Purpose: Preview lets coaches see exactly how students will experience each exercise type before publishing. List component provides exercise management within a practice set context.
Output: ExercisePreview (read-only renderer for all 6 types) + ExerciseList (sortable list with actions).
</objective>

<execution_context>
@/home/sheldon/.claude/get-shit-done/workflows/execute-plan.md
@/home/sheldon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-practice-data-model-exercise-crud/31-RESEARCH.md
@.planning/phases/31-practice-data-model-exercise-crud/31-01-SUMMARY.md
@src/types/exercises.ts
@src/components/ui/PhoneticText.tsx
@src/lib/practice.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ExercisePreview component for all 6 exercise types</name>
  <files>src/components/admin/exercises/ExercisePreview.tsx</files>
  <action>
Create `src/components/admin/exercises/ExercisePreview.tsx` as a "use client" component.

**Props:**
```typescript
interface ExercisePreviewProps {
  definition: ExerciseDefinition;
  language?: "cantonese" | "mandarin" | "both";
}
```

**Core structure:**
- The component renders a card-style preview with a "Student View" label at top
- Background: `rounded-lg border border-zinc-600 bg-zinc-900 p-4`
- Switch on `definition.type` to render type-specific preview

**Multiple Choice preview:**
- Show question text wrapped in `<PhoneticText>` (for Chinese text phonetic rendering)
- Show options as a vertical list of option cards (rounded, zinc-800 bg, hover state)
- Each option shows a radio circle (unfilled -- this is preview only) + option text in PhoneticText
- Do NOT highlight the correct answer in preview (student shouldn't see which is correct)

**Fill-in-Blank preview:**
- Import `parseBlankSentence` from "@/lib/practice"
- Parse the sentence template into segments
- Render text segments in PhoneticText, render blank segments as underlined input placeholders (styled as `border-b-2 border-zinc-500 inline-block min-w-[60px] mx-1 text-center` with placeholder "___")
- Show the full sentence inline with blanks appearing as underline slots

**Matching preview:**
- Two columns layout (flexbox)
- Left column: list of left items in randomized display order (use a shuffled copy, NOT the original order)
- Right column: list of right items in a different shuffled order
- Each item in a small card (zinc-800 bg, rounded, padding)
- Connect-line visual hint: show a small "drag to match" instruction text

**Ordering preview:**
- Show items in a SHUFFLED order (not the correct order -- that's the answer)
- Each item in a draggable-looking card with a grip handle icon (GripVertical from lucide-react)
- Instruction text: "Arrange these items in the correct order"

**Audio Recording preview:**
- Show target phrase in large text using PhoneticText
- Show a mock "Record" button (non-functional, just visual -- rounded red/zinc button with Mic icon)
- If referenceText exists, show it below in smaller text

**Free Text preview:**
- Show prompt text in PhoneticText
- Show a mock textarea (disabled, with placeholder "Type your answer here...")
- If minLength/maxLength set, show character count hint below

**PhoneticText usage:**
- Wrap all Chinese text content in `<PhoneticText>` component (imported from "@/components/ui/PhoneticText")
- This ensures phonetic annotations render correctly with the custom fonts from Phase 30
- For the language prop on PhoneticText: pass the `language` prop from ExercisePreview (defaults to "both")

Export the component as default and named export.
  </action>
  <verify>Verify ExercisePreview handles all 6 type cases via switch/conditional rendering. Verify PhoneticText is imported and used for Chinese text content. Verify `npx tsc --noEmit` passes.</verify>
  <done>ExercisePreview.tsx renders read-only student-perspective previews for all 6 exercise types. Chinese text uses PhoneticText for phonetic annotation. Fill-in-blank uses parseBlankSentence utility. Matching and ordering show shuffled items.</done>
</task>

<task type="auto">
  <name>Task 2: Create ExerciseList component for managing exercises in a set</name>
  <files>src/components/admin/exercises/ExerciseList.tsx</files>
  <action>
Create `src/components/admin/exercises/ExerciseList.tsx` as a "use client" component.

**Props:**
```typescript
interface ExerciseListProps {
  exercises: PracticeExercise[];
  onEdit: (exercise: PracticeExercise) => void;
  onDelete: (exerciseId: string) => void;
  isDeleting?: string; // ID of exercise being deleted (for loading state)
}
```

**Layout:**
- If exercises is empty: show empty state card with message "No exercises yet. Add your first exercise to get started." and a BookOpen icon
- If exercises exist: show a vertical list of exercise cards ordered by sortOrder

**Each exercise card:**
- Container: `rounded-lg border border-zinc-700 bg-zinc-800 p-4 flex items-start justify-between gap-3`
- Left side:
  - Exercise type icon (same icon mapping as ExerciseForm: ListChecks for MCQ, TextCursorInput for fill-blank, etc.) with type label text
  - Language badge: small colored badge ("Cantonese" = teal, "Mandarin" = amber, "Both" = zinc) -- use `cn()` for conditional styling
  - Brief content preview: for MCQ show question text (truncated to 80 chars), for fill-blank show sentence template (truncated), for matching show "N pairs", for ordering show "N items", for audio show target phrase (truncated), for free text show prompt (truncated)
- Right side:
  - Edit button (Pencil icon, ghost variant) -- calls `onEdit(exercise)`
  - Delete button (Trash2 icon, ghost variant, text-red-400) -- calls `onDelete(exercise.id)`
  - If isDeleting matches this exercise ID, show a spinner instead of delete button

**Delete confirmation:**
- Use AlertDialog from Radix (same pattern as existing admin components)
- Confirmation message: "Are you sure you want to delete this exercise? This action cannot be undone."
- Cancel + Delete buttons

Export component.
  </action>
  <verify>Verify ExerciseList shows exercise cards with type icons and language badges. Verify empty state renders correctly. Verify edit/delete actions are wired to callbacks. Verify `npx tsc --noEmit` passes.</verify>
  <done>ExerciseList.tsx renders exercise cards with type icons, language badges, content previews, and edit/delete actions. Empty state shows a helpful message. Delete uses AlertDialog for confirmation.</done>
</task>

</tasks>

<verification>
1. ExercisePreview renders all 6 exercise types correctly
2. Chinese text in previews is wrapped with PhoneticText component
3. Fill-in-blank preview uses parseBlankSentence to render inline blanks
4. Matching and ordering previews show shuffled items (not answer order)
5. ExerciseList shows exercise cards with correct type icons and language badges
6. ExerciseList handles empty state gracefully
7. Delete action uses AlertDialog confirmation
</verification>

<success_criteria>
- Coach can preview how any exercise type will appear to students (Success Criteria #5)
- Exercise list provides overview of all exercises in a practice set with management actions
- PhoneticText ensures Chinese text renders with phonetic annotations
- All components compile without TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/31-practice-data-model-exercise-crud/31-05-SUMMARY.md`
</output>
